{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"xpyrment \ud83e\uddea","text":"<p><code>xpyrment</code> is an enterprise-grade, low-code Python library for experiment design, classical Design of Experiments (DoE), and statistical analysis. </p> <p>It is designed to bring the user-friendly, high-level API style of PyCaret to the domain of digital experimentation (A/B testing), combined with the rigorous statistical techniques of modern, enterprise-scale platforms (like tea-tasting). It features native support for CUPED (variance reduction), ratio metrics via the Delta method, multiple comparison corrections, Sample Ratio Mismatch (SRM) diagnostics, mixture SPRT continuous monitoring (mSPRT), Bayesian inference, and classical industrial DoE design matrices.</p>"},{"location":"#key-features","title":"\ud83c\udf1f Key Features","text":"<ul> <li>Low-Code PyCaret-Style API: Set up your experiment, define your metrics, run your analysis, and print beautiful summaries or plot results in just a few lines of code.</li> <li>Rigorous Variance Reduction (CUPED): Native support for standard CUPED (continuous metrics) and Ratio CUPED (numerator and denominator adjustment). Reduces sample size requirements by up to 80%.</li> <li>Ratio Metric Precision: First-order Taylor expansion (Delta method) for precise variance estimation of ratio metrics (e.g., CTR, revenue per click) where both numerator and denominator are stochastic.</li> <li>Classical Design of Experiments (DoE): Full and Fractional Factorial, Plackett-Burman, Taguchi, Definitive Screening Designs (DSD), Response Surface Methodologies (CCD &amp; Box-Behnken), and D-Optimal algorithms.</li> <li>Experimental Diagnostics: Built-in automated Chi-square tests to detect Sample Ratio Mismatch (SRM), pre-experiment covariate balance validation, and time-series novelty/primacy effect detectors.</li> <li>Sequential Monitoring &amp; Early Stopping: Sequential monitoring bounds and always-valid confidence intervals via mixture SPRT (mSPRT) and Pocock/O'Brien-Fleming alpha-spending functions.</li> <li>Multi-Testing Correction: Avoid \"p-hacking\" by automatically adjusting p-values for multiple metric runs using Holm-Bonferroni, Bonferroni, or Benjamini-Hochberg (FDR).</li> <li>Premium Visualizations: Publication-ready, color-coded forest plots (confidence intervals) and power curves built with <code>matplotlib</code> and <code>seaborn</code>.</li> </ul>"},{"location":"#quickstart-tutorial","title":"\ud83d\ude80 Quickstart Tutorial","text":"<p>This quickstart guides you through the entire A/B testing lifecycle: designing, simulating, configuring, and analyzing.</p>"},{"location":"#1-experiment-design-power-analysis","title":"1. Experiment Design (Power Analysis)","text":"<p>Before launching your test, calculate the sample size required to detect a \\(5\\%\\) relative lift in a key continuous metric (e.g., Average Order Value = $100, standard deviation = $35).</p> <pre><code>import xpyrment as xp\n\n# Calculate sample size for a standard t-test\ndesign = xp.design_experiment(\n    metric_type=\"mean\",\n    baseline_value=100.0,\n    standard_deviation=35.0,\n    mde=0.05,                  # 5% relative lift\n    mde_type=\"relative\",\n    alpha=0.05,                # Significance level (Type I error)\n    power=0.80,                # Target power (1 - Type II error)\n    pre_post_correlation=0.75, # Optional: Pre-Post correlation to calculate CUPED savings!\n    daily_traffic=5000         # Optional: Daily user traffic to calculate duration\n)\n\nprint(design)\n</code></pre> <p>Output: <pre><code>=========================================\n       Experiment Design Summary        \n=========================================\nMetric Type                   : Mean\nBaseline Value                : 100.0000\nTarget MDE (Absolute)         : 5.0000\nTarget MDE (Relative)         : 5.00%\nSignificance Level (Alpha)    : 5.00%\nStatistical Power (1-Beta)    : 80.00%\nSample Size Per Variant       : 1,537\nTotal Sample Size Required    : 3,074\nPre-Post Correlation          : 0.75\nCUPED Sample Size Per Variant : 672\nCUPED Total Sample Size       : 1,344\nCUPED Sample Size Savings     : 43.8%\nDaily Traffic                 : 5,000/day\nEstimated Duration (Standard) : 0.6 days\nEstimated Duration (CUPED)    : 0.3 days\n=========================================\n</code></pre></p>"},{"location":"#visualizing-power-curves","title":"Visualizing Power Curves","text":"<p>Generate coordinates and plot required sample sizes against a range of MDEs to see the impact of CUPED:</p> <pre><code># Generate power curve coordinates\ncurve_data = xp.generate_power_curve_data(\n    metric_type=\"mean\",\n    baseline_value=100.0,\n    standard_deviation=35.0,\n    pre_post_correlation=0.75\n)\n\n# Plot standard vs. CUPED required sample sizes\nxp.plot_power_curve(curve_data)\n</code></pre>"},{"location":"#2-generate-synthetic-ab-test-data","title":"2. Generate Synthetic A/B Test Data","text":"<p>Let's generate simulated experimental data of 10,000 users split 50/50, complete with pre-period covariates so we can demonstrate CUPED and ratio metric evaluations:</p> <pre><code>df = xp.generate_ab_data(\n    n_samples=10000,\n    treatment_effect_revenue=2.5,        # +$2.50 absolute lift\n    treatment_effect_conversion=0.015,    # +1.5% absolute lift\n    treatment_effect_clicks=0.06,         # +6% relative lift in click ratios\n    pre_period_correlation=0.82,          # Correlation between pre- and post- period\n    random_seed=42\n)\n\nprint(df.head())\n</code></pre> user_id variant pre_revenue revenue converted pre_clicks pre_impressions clicks impressions USER_000001 control 4.47 4.42 0 4 93 5 96 USER_000002 treatment 56.78 61.22 1 6 112 8 108 USER_000003 control 51.12 48.91 0 5 105 3 99 USER_000004 treatment 32.54 36.90 0 3 82 4 88"},{"location":"#3-setup-and-run-analysis-pycaret-style","title":"3. Setup and Run Analysis (PyCaret Style!)","text":"<p>Initialize the experiment environment using the <code>setup</code> function, define your metrics (with pre-period specifications for automatic CUPED), and run your analysis!</p> <pre><code># 1. Initialize PyCaret style setup\nexp = xp.setup(\n    data=df, \n    treatment_col=\"variant\", \n    id_col=\"user_id\"\n)\n\n# 2. Define your metrics\n# Continuous metric (Average revenue) with automatic CUPED!\nrevenue = xp.MeanMetric(\n    name=\"Average Revenue per User\", \n    value_col=\"revenue\", \n    pre_period_col=\"pre_revenue\"\n)\n\n# Proportion metric (Conversion rate)\nconversion = xp.ProportionMetric(\n    name=\"Purchase Conversion Rate\", \n    value_col=\"converted\"\n)\n\n# Ratio metric (Click-Through-Rate = sum(clicks)/sum(impressions)) with ratio CUPED!\nctr = xp.RatioMetric(\n    name=\"Click-Through-Rate (CTR)\", \n    numerator_col=\"clicks\", \n    denominator_col=\"impressions\",\n    pre_numerator_col=\"pre_clicks\",\n    pre_denominator_col=\"pre_impressions\"\n)\n\n# 3. Add metrics to the experiment container\nexp.add_metrics([revenue, conversion, ctr])\n\n# 4. Run Analysis (optionally apply multi-test corrections like 'fdr_bh')\nresults = exp.run_analysis(\n    control=\"control\", \n    treatment=\"treatment\",\n    multi_test_correction=\"fdr_bh\"\n)\n</code></pre>"},{"location":"#4-review-and-visualize-results","title":"4. Review and Visualize Results","text":""},{"location":"#standard-summary-dataframe","title":"Standard Summary DataFrame","text":"<p>Call <code>.summary()</code> to get a polished, publication-ready pandas DataFrame with automatic statistical significance annotations (<code>*</code> for \\(p &lt; 0.05\\), <code>**</code> for \\(p &lt; 0.01\\), <code>***</code> for \\(p &lt; 0.001\\)).</p> <pre><code>summary_df = results.summary()\nprint(summary_df)\n</code></pre> Metric Type Control Mean Treatment Mean Relative Lift 95% CI (Rel) p-value Post-hoc Power CUPED Var Reduction Average Revenue per User Mean 49.9542 52.4712 +5.04% [+3.78%, +6.30%] 0.0000*** 100.0% Yes 68.3% Purchase Conversion Rate Proportion 0.0990 0.1172 +18.42% [+4.12%, +32.72%] 0.0112* 73.1% No - Click-Through-Rate (CTR) Ratio 0.0498 0.0528 +5.95% [+4.11%, +7.78%] 0.0000*** 100.0% Yes 71.2% <p>[!TIP] CUPED was automatically applied to both Average Revenue and Click-Through-Rate, achieving over \\(68\\%\\) and \\(71\\%\\) variance reduction respectively! This dramatically narrowed our confidence intervals and amplified our statistical power.</p>"},{"location":"#forest-plot-visualization","title":"Forest Plot Visualization","text":"<p>Call <code>.plot()</code> to render a gorgeous forest plot representing confidence intervals. Statistically significant lifts are automatically rendered in vibrant teal, while others are shown in subtle gray.</p> <pre><code># Render the forest plot\nresults.plot()\n</code></pre>"},{"location":"#redesigned-subpackage-taxonomy-dependency-flow","title":"\ud83d\udd2c Redesigned Subpackage Taxonomy &amp; Dependency Flow","text":"<p>To support industrial-scale digital tests and classical DoE, the package has been fully restructured under <code>src/xpyrment</code> following a one-way dependency gating layout:</p> <pre><code>metrics/     \u2190 no upstream imports. Houses core metric taxonomy and guardrail thresholds.\ncore/        \u2190 depends on metrics/. Powers the phase gating lifecycle &amp; spec registries.\nplan/        \u2190 depends on core/, metrics/. Computes pre-registration power/durations.\ndesign/      \u2190 depends on core/, metrics/. Handles randomizations, splits &amp; DoE matrices.\nvalidate/    \u2190 depends on core/, metrics/. Houses SRM checks and covariate balance tests.\nrun/         \u2190 depends on core/, design/, validate/. Handles ingestion &amp; mSPRT monitors.\nanalyze/     \u2190 depends on core/, metrics/, run/. Orchestrates frequentist/Bayesian engines.\ninteractions/\u2190 depends on analyze/, design/. Decomposes multi-factor ANOVA interaction terms.\ninterpret/   \u2190 depends on analyze/, interactions/, metrics/. Infers ship/no-ship decisions.\nreport/      \u2190 terminal consumer of all phases. Compiles audit trails &amp; exportable reports.\n</code></pre>"},{"location":"#mathematical-framework","title":"\ud83d\udcd6 Mathematical Framework","text":""},{"location":"#welchs-t-test","title":"Welch's t-test","text":"<p>For simple continuous metrics without a pre-period covariate, we calculate the standard error of the mean difference as: $\\(SE = \\sqrt{\\frac{s_C^2}{n_C} + \\frac{s_T^2}{n_T}}\\)$ Degrees of freedom are computed via the Welch-Satterthwaite equation to handle unequal sample sizes and variances.</p>"},{"location":"#delta-method-ratio-metrics","title":"Delta Method (Ratio Metrics)","text":"<p>Because user click-through-rates or revenue-per-order ratios are calculated as: $\\(R = \\frac{\\sum_i X_i}{\\sum_i Y_i} = \\frac{\\bar{X}}{\\bar{Y}}\\)$ the variance of the ratio cannot be computed using standard methods because the denominator \\(Y\\) is a random variable. We employ a first-order Taylor expansion (Delta method) to estimate variance: $\\(Var(R) \\approx \\frac{1}{\\mu_Y^2} Var(X) + \\frac{\\mu_X^2}{\\mu_Y^4} Var(Y) - 2\\frac{\\mu_X}{\\mu_Y^3} Cov(X, Y)\\)$</p>"},{"location":"#cuped-controlled-experiments-using-pre-experiment-data","title":"CUPED (Controlled-experiments Using Pre-Experiment Data)","text":"<p>CUPED adjusts post-period metrics by subtracting the portion of variance explained by pre-period performance: $\\(Y_i^* = Y_i - \\theta (X_i - \\mu_{X, global})\\)$ where \\(\\theta = \\frac{Cov(Y, X)}{Var(X)}\\) is computed across the pooled data. The variance of the CUPED-adjusted metric is reduced by a factor of \\(1 - \\rho^2\\) (where \\(\\rho\\) is the correlation coefficient): $\\(Var(Y^*) = Var(Y) (1 - \\rho^2)\\)$</p> <p>For ratio metrics, <code>xpyrment</code> applies CUPED adjustment separately to the numerator and denominator before applying the Delta method on adjusted vectors\u2014a technique pioneered by Netflix and Uber.</p>"},{"location":"#sample-ratio-mismatch-srm-goodness-of-fit","title":"Sample Ratio Mismatch (SRM) Goodness-of-Fit","text":"<p>A Pearson Chi-square test is calculated on the observed sample counts against the expected design weights to flag assignment bugs early: $\\(\\chi^2 = \\sum_i \\frac{(O_i - E_i)^2}{E_i}\\)$ If the test p-value \\(&lt; 0.001\\), an <code>SRMError</code> is raised.</p>"},{"location":"#local-development-testing","title":"\ud83d\udee0\ufe0f Local Development &amp; Testing","text":"<p>We use <code>pytest</code> for unit testing. To set up your local environment:</p> <ol> <li> <p>Create a virtual environment and activate it:    <pre><code>python -m venv .venv\n.venv\\Scripts\\activate  # On Windows\nsource .venv/bin/activate  # On macOS/Linux\n</code></pre></p> </li> <li> <p>Install the package in editable mode with development dependencies:    <pre><code>pip install -e .[dev]\n</code></pre></p> </li> <li> <p>Run the unit test suite:    <pre><code>pytest\n</code></pre></p> </li> </ol>"},{"location":"#license","title":"\ud83d\udcc4 License","text":"<p>Distributed under the MIT License. See <code>LICENSE</code> for more information.</p>"},{"location":"api/","title":"API Reference","text":"<p>Welcome to the xpyrment API Reference. Select a submodule from the menu to inspect classes, methods, and functions.</p>"},{"location":"api/simulation/","title":"Simulation","text":""},{"location":"api/simulation/#xpyrment.simulation","title":"<code>xpyrment.simulation</code>","text":""},{"location":"api/simulation/#xpyrment.simulation.generate_ab_data","title":"<code>generate_ab_data(n_samples=10000, treatment_fraction=0.5, baseline_revenue=10.0, treatment_effect_revenue=0.5, baseline_conversion=0.15, treatment_effect_conversion=0.02, baseline_clicks_mean=5.0, baseline_impressions_mean=100.0, treatment_effect_clicks=0.3, pre_period_correlation=0.7, random_seed=42)</code>","text":"<p>Generates synthetic A/B test data simulating continuous, binary, and ratio metrics.</p> <p>Includes correlated pre-period values for CUPED analysis.</p> Source code in <code>src\\xpyrment\\simulation.py</code> <pre><code>def generate_ab_data(\n    n_samples: int = 10000,\n    treatment_fraction: float = 0.5,\n    baseline_revenue: float = 10.0,\n    treatment_effect_revenue: float = 0.5,\n    baseline_conversion: float = 0.15,\n    treatment_effect_conversion: float = 0.02,\n    baseline_clicks_mean: float = 5.0,\n    baseline_impressions_mean: float = 100.0,\n    treatment_effect_clicks: float = 0.3,\n    pre_period_correlation: float = 0.70,\n    random_seed: int = 42,\n) -&gt; pd.DataFrame:\n    \"\"\"Generates synthetic A/B test data simulating continuous, binary, and ratio metrics.\n\n    Includes correlated pre-period values for CUPED analysis.\n    \"\"\"\n    rng = np.random.default_rng(random_seed)\n\n    # 1. Assign User IDs and Treatment groups\n    user_ids = [f\"USER_{i:06d}\" for i in range(1, n_samples + 1)]\n    variants = [\"control\", \"treatment\"]\n    treatment_probs = [1.0 - treatment_fraction, treatment_fraction]\n    variant_assignments = rng.choice(variants, size=n_samples, p=treatment_probs)\n\n    # 2. Continuous Metrics (Revenue) with Pre-Period Correlation\n    # Simulating using a Bivariate Normal Distribution transformed to be positive\n    mean_pre = baseline_revenue\n    mean_post_c = baseline_revenue\n    mean_post_t = baseline_revenue + treatment_effect_revenue\n\n    std_val = 5.0\n\n    cov_matrix = [\n        [std_val**2, pre_period_correlation * (std_val**2)],\n        [pre_period_correlation * (std_val**2), std_val**2],\n    ]\n\n    # Generate for Control\n    n_c = int(np.sum(variant_assignments == \"control\"))\n    bivariate_c = rng.multivariate_normal([mean_pre, mean_post_c], cov_matrix, size=n_c)\n\n    # Generate for Treatment\n    n_t = n_samples - n_c\n    bivariate_t = rng.multivariate_normal([mean_pre, mean_post_t], cov_matrix, size=n_t)\n\n    # Recombine results preserving index alignments\n    pre_revenue = np.zeros(n_samples)\n    revenue = np.zeros(n_samples)\n\n    c_idx = 0\n    t_idx = 0\n    for i, var in enumerate(variant_assignments):\n        if var == \"control\":\n            pre_revenue[i] = bivariate_c[c_idx, 0]\n            revenue[i] = bivariate_c[c_idx, 1]\n            c_idx += 1\n        else:\n            pre_revenue[i] = bivariate_t[t_idx, 0]\n            revenue[i] = bivariate_t[t_idx, 1]\n            t_idx += 1\n\n    # Keep numbers positive\n    pre_revenue = np.clip(pre_revenue, 0, None)\n    revenue = np.clip(revenue, 0, None)\n\n    # 3. Binary Metrics (Conversions)\n    converted = np.zeros(n_samples, dtype=int)\n    for i, var in enumerate(variant_assignments):\n        prob = baseline_conversion if var == \"control\" else (baseline_conversion + treatment_effect_conversion)\n        prob = np.clip(prob, 0.0, 1.0)\n        converted[i] = rng.binomial(n=1, p=prob)\n\n    # 4. Ratio Metrics (Clicks and Impressions for CTR)\n    # Both clicks and impressions are correlated stochastically, with positive variance\n    pre_impressions = rng.poisson(lam=baseline_impressions_mean, size=n_samples)\n    pre_impressions = np.clip(pre_impressions, 1, None)\n\n    impressions = rng.poisson(lam=baseline_impressions_mean, size=n_samples)\n    impressions = np.clip(impressions, 1, None)\n\n    # Click proportions\n    base_ctr = baseline_clicks_mean / baseline_impressions_mean\n    treatment_ctr = (baseline_clicks_mean + treatment_effect_clicks) / baseline_impressions_mean\n\n    pre_clicks = np.zeros(n_samples, dtype=int)\n    clicks = np.zeros(n_samples, dtype=int)\n\n    for i, var in enumerate(variant_assignments):\n        pre_ctr_user = rng.beta(base_ctr * 10, (1.0 - base_ctr) * 10)\n        pre_clicks[i] = rng.binomial(n=int(pre_impressions[i]), p=pre_ctr_user)\n\n        user_ctr_prob = base_ctr if var == \"control\" else treatment_ctr\n        ctr_user = rng.beta(user_ctr_prob * 10, (1.0 - user_ctr_prob) * 10)\n        clicks[i] = rng.binomial(n=int(impressions[i]), p=ctr_user)\n\n    # Compile dataset\n    df = pd.DataFrame(\n        {\n            \"user_id\": user_ids,\n            \"variant\": variant_assignments,\n            \"pre_revenue\": pre_revenue,\n            \"revenue\": revenue,\n            \"converted\": converted,\n            \"pre_impressions\": pre_impressions,\n            \"pre_clicks\": pre_clicks,\n            \"impressions\": impressions,\n            \"clicks\": clicks,\n        }\n    )\n\n    return df\n</code></pre>"},{"location":"api/analyze/","title":"Analyze Module","text":"<p>The <code>xpyrment.analyze</code> module contains submodules and components for analyze.</p>"},{"location":"api/analyze/#xpyrment.analyze","title":"<code>xpyrment.analyze</code>","text":""},{"location":"api/analyze/#xpyrment.analyze.AnalysisResult","title":"<code>AnalysisResult</code>","text":"<p>Holds results from an experiment analysis.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>class AnalysisResult:\n    \"\"\"Holds results from an experiment analysis.\"\"\"\n\n    def __init__(self, raw_results: List[dict], alpha: float = 0.05):\n        self.raw_results = raw_results\n        self.alpha = alpha\n        self.df_raw = pd.DataFrame(raw_results)\n\n    def summary(self, formatted: bool = True) -&gt; pd.DataFrame:\n        \"\"\"Returns a summarized DataFrame of the analysis.\"\"\"\n        df = self.df_raw.copy()\n\n        if not formatted:\n            return df\n\n        summary_data = []\n        for _, row in df.iterrows():\n            lift_val = row[\"relative_lift\"]\n            lift_str = f\"{lift_val:+.2%}\" if not pd.isna(lift_val) else \"N/A\"\n\n            p_val = row[\"p_value\"]\n            sig_symbol = \"\"\n            if p_val &lt; 0.001:\n                sig_symbol = \"***\"\n            elif p_val &lt; 0.01:\n                sig_symbol = \"**\"\n            elif p_val &lt; 0.05:\n                sig_symbol = \"*\"\n\n            p_str = f\"{p_val:.4f}{sig_symbol}\" if not pd.isna(p_val) else \"N/A\"\n\n            lower_pct = row[\"rel_ci_lower\"]\n            upper_pct = row[\"rel_ci_upper\"]\n            ci_str = f\"[{lower_pct:+.2%}, {upper_pct:+.2%}]\" if not (pd.isna(lower_pct) or pd.isna(upper_pct)) else \"N/A\"\n\n            power_val = row[\"power\"]\n            power_str = f\"{power_val:.1%}\" if not pd.isna(power_val) else \"N/A\"\n\n            cuped_str = \"Yes\" if row[\"cuped_applied\"] else \"No\"\n            var_red_val = row[\"variance_reduction\"]\n            var_red_str = f\"{var_red_val:.1%}\" if row[\"cuped_applied\"] and not pd.isna(var_red_val) else \"-\"\n\n            summary_data.append(\n                {\n                    \"Metric\": row[\"metric_name\"],\n                    \"Type\": row[\"metric_type\"],\n                    \"Control Mean\": f\"{row['control_mean']:.4f}\",\n                    \"Treatment Mean\": f\"{row['treatment_mean']:.4f}\",\n                    \"Relative Lift\": lift_str,\n                    \"95% CI (Rel)\": ci_str,\n                    \"p-value\": p_str,\n                    \"Post-hoc Power\": power_str,\n                    \"CUPED\": cuped_str,\n                    \"Var Reduction\": var_red_str,\n                }\n            )\n\n        return pd.DataFrame(summary_data)\n\n    def plot(self, **kwargs):\n        \"\"\"Generates a forest plot of results.\"\"\"\n        # Re-routed to the reporting/export layer dynamically\n        from xpyrment.report.export import plot_forest\n        return plot_forest(self.df_raw, alpha=self.alpha, **kwargs)\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.AnalysisResult.plot","title":"<code>plot(**kwargs)</code>","text":"<p>Generates a forest plot of results.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def plot(self, **kwargs):\n    \"\"\"Generates a forest plot of results.\"\"\"\n    # Re-routed to the reporting/export layer dynamically\n    from xpyrment.report.export import plot_forest\n    return plot_forest(self.df_raw, alpha=self.alpha, **kwargs)\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.AnalysisResult.summary","title":"<code>summary(formatted=True)</code>","text":"<p>Returns a summarized DataFrame of the analysis.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def summary(self, formatted: bool = True) -&gt; pd.DataFrame:\n    \"\"\"Returns a summarized DataFrame of the analysis.\"\"\"\n    df = self.df_raw.copy()\n\n    if not formatted:\n        return df\n\n    summary_data = []\n    for _, row in df.iterrows():\n        lift_val = row[\"relative_lift\"]\n        lift_str = f\"{lift_val:+.2%}\" if not pd.isna(lift_val) else \"N/A\"\n\n        p_val = row[\"p_value\"]\n        sig_symbol = \"\"\n        if p_val &lt; 0.001:\n            sig_symbol = \"***\"\n        elif p_val &lt; 0.01:\n            sig_symbol = \"**\"\n        elif p_val &lt; 0.05:\n            sig_symbol = \"*\"\n\n        p_str = f\"{p_val:.4f}{sig_symbol}\" if not pd.isna(p_val) else \"N/A\"\n\n        lower_pct = row[\"rel_ci_lower\"]\n        upper_pct = row[\"rel_ci_upper\"]\n        ci_str = f\"[{lower_pct:+.2%}, {upper_pct:+.2%}]\" if not (pd.isna(lower_pct) or pd.isna(upper_pct)) else \"N/A\"\n\n        power_val = row[\"power\"]\n        power_str = f\"{power_val:.1%}\" if not pd.isna(power_val) else \"N/A\"\n\n        cuped_str = \"Yes\" if row[\"cuped_applied\"] else \"No\"\n        var_red_val = row[\"variance_reduction\"]\n        var_red_str = f\"{var_red_val:.1%}\" if row[\"cuped_applied\"] and not pd.isna(var_red_val) else \"-\"\n\n        summary_data.append(\n            {\n                \"Metric\": row[\"metric_name\"],\n                \"Type\": row[\"metric_type\"],\n                \"Control Mean\": f\"{row['control_mean']:.4f}\",\n                \"Treatment Mean\": f\"{row['treatment_mean']:.4f}\",\n                \"Relative Lift\": lift_str,\n                \"95% CI (Rel)\": ci_str,\n                \"p-value\": p_str,\n                \"Post-hoc Power\": power_str,\n                \"CUPED\": cuped_str,\n                \"Var Reduction\": var_red_str,\n            }\n        )\n\n    return pd.DataFrame(summary_data)\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.apply_cuped","title":"<code>apply_cuped(df, target_col, pre_col)</code>","text":"<p>Applies Controlled-experiments Using Pre-Experiment Data (CUPED) on a series.</p> Source code in <code>src\\xpyrment\\analyze\\variance_reduction.py</code> <pre><code>def apply_cuped(df: pd.DataFrame, target_col: str, pre_col: str) -&gt; pd.Series:\n    \"\"\"Applies Controlled-experiments Using Pre-Experiment Data (CUPED) on a series.\"\"\"\n    # This is a core transformation utility, but currently handled inline inside taxonomy.py.\n    # We can write a general placeholder here.\n    return df[target_col]\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.apply_multiple_testing_correction","title":"<code>apply_multiple_testing_correction(p_values, alpha=0.05, method='fdr_bh')</code>","text":"<p>Applies multiple testing corrections on p-values using statsmodels.</p> <p>Supported methods include: 'bonferroni', 'holm', 'fdr_bh' (Benjamini-Hochberg).</p> Source code in <code>src\\xpyrment\\analyze\\corrections.py</code> <pre><code>def apply_multiple_testing_correction(\n    p_values: List[float], alpha: float = 0.05, method: str = \"fdr_bh\"\n) -&gt; List[float]:\n    \"\"\"Applies multiple testing corrections on p-values using statsmodels.\n\n    Supported methods include: 'bonferroni', 'holm', 'fdr_bh' (Benjamini-Hochberg).\n    \"\"\"\n    if not p_values:\n        return []\n\n    # Handle NaNs\n    import numpy as np\n    p_array = np.array(p_values)\n    mask = ~np.isnan(p_array)\n\n    if not np.any(mask):\n        return p_values\n\n    adjusted_p = p_array.copy()\n    _, adj, _, _ = multipletests(p_array[mask], alpha=alpha, method=method)\n    adjusted_p[mask] = adj\n\n    return adjusted_p.tolist()\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.run_analysis","title":"<code>run_analysis(experiment, control='control', treatment='treatment', alpha=0.05, multi_test_correction=None)</code>","text":"<p>Executes the statistical analysis across all metrics in an Experiment.</p> <p>Parameters:</p> Name Type Description Default <code>experiment</code> <code>Experiment</code> <p>The setup container.</p> required <code>control</code> <code>str</code> <p>Control variant name.</p> <code>'control'</code> <code>treatment</code> <code>str</code> <p>Treatment variant name.</p> <code>'treatment'</code> <code>alpha</code> <code>float</code> <p>Confidence level.</p> <code>0.05</code> <code>multi_test_correction</code> <code>str</code> <p>Correction method.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnalysisResult</code> <code>AnalysisResult</code> <p>Polished summary and plots.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def run_analysis(\n    experiment: Experiment,\n    control: str = \"control\",\n    treatment: str = \"treatment\",\n    alpha: float = 0.05,\n    multi_test_correction: Optional[str] = None,\n) -&gt; AnalysisResult:\n    \"\"\"Executes the statistical analysis across all metrics in an Experiment.\n\n    Args:\n        experiment (Experiment): The setup container.\n        control (str): Control variant name.\n        treatment (str): Treatment variant name.\n        alpha (float): Confidence level.\n        multi_test_correction (str, optional): Correction method.\n\n    Returns:\n        AnalysisResult: Polished summary and plots.\n    \"\"\"\n    if not experiment.metrics:\n        raise ValueError(\"No metrics have been added to the experiment.\")\n\n    unique_variants = experiment.data[experiment.treatment_col].unique()\n    if control not in unique_variants:\n        raise ValueError(f\"Control label '{control}' not found.\")\n    if treatment not in unique_variants:\n        raise ValueError(f\"Treatment label '{treatment}' not found.\")\n\n    results = []\n    for metric in experiment.metrics:\n        res = metric.calculate(\n            experiment.data,\n            treatment_col=experiment.treatment_col,\n            control=control,\n            treatment=treatment,\n            alpha=alpha,\n        )\n        results.append(res)\n\n    # Apply corrections if requested\n    if multi_test_correction and len(results) &gt; 1:\n        p_vals = [res[\"p_value\"] for res in results]\n        adjusted_p = apply_multiple_testing_correction(p_vals, alpha=alpha, method=multi_test_correction)\n        for i, val in enumerate(adjusted_p):\n            results[i][\"p_value\"] = val\n\n    experiment.transition_to(ExperimentState.ANALYZED)\n    return AnalysisResult(results, alpha=alpha)\n</code></pre>"},{"location":"api/analyze/#xpyrment.analyze.setup","title":"<code>setup(data, treatment_col, id_col=None)</code>","text":"<p>Initializes the experimental setup container.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def setup(\n    data: pd.DataFrame,\n    treatment_col: str,\n    id_col: Optional[str] = None,\n) -&gt; Experiment:\n    \"\"\"Initializes the experimental setup container.\"\"\"\n    print(\"==========================================\")\n    print(\"      Initializing xpyrment Setup         \")\n    print(\"==========================================\")\n    print(f\"Total rows in dataset:  {len(data)}\")\n    print(f\"Treatment column:      {treatment_col}\")\n    if id_col:\n        print(f\"ID column:             {id_col}\")\n\n    exp = Experiment(data, treatment_col, id_col)\n    return exp\n</code></pre>"},{"location":"api/analyze/corrections/","title":"Corrections","text":""},{"location":"api/analyze/corrections/#xpyrment.analyze.corrections","title":"<code>xpyrment.analyze.corrections</code>","text":""},{"location":"api/analyze/corrections/#xpyrment.analyze.corrections.apply_multiple_testing_correction","title":"<code>apply_multiple_testing_correction(p_values, alpha=0.05, method='fdr_bh')</code>","text":"<p>Applies multiple testing corrections on p-values using statsmodels.</p> <p>Supported methods include: 'bonferroni', 'holm', 'fdr_bh' (Benjamini-Hochberg).</p> Source code in <code>src\\xpyrment\\analyze\\corrections.py</code> <pre><code>def apply_multiple_testing_correction(\n    p_values: List[float], alpha: float = 0.05, method: str = \"fdr_bh\"\n) -&gt; List[float]:\n    \"\"\"Applies multiple testing corrections on p-values using statsmodels.\n\n    Supported methods include: 'bonferroni', 'holm', 'fdr_bh' (Benjamini-Hochberg).\n    \"\"\"\n    if not p_values:\n        return []\n\n    # Handle NaNs\n    import numpy as np\n    p_array = np.array(p_values)\n    mask = ~np.isnan(p_array)\n\n    if not np.any(mask):\n        return p_values\n\n    adjusted_p = p_array.copy()\n    _, adj, _, _ = multipletests(p_array[mask], alpha=alpha, method=method)\n    adjusted_p[mask] = adj\n\n    return adjusted_p.tolist()\n</code></pre>"},{"location":"api/analyze/orchestrator/","title":"Orchestrator","text":""},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator","title":"<code>xpyrment.analyze.orchestrator</code>","text":""},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator.AnalysisResult","title":"<code>AnalysisResult</code>","text":"<p>Holds results from an experiment analysis.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>class AnalysisResult:\n    \"\"\"Holds results from an experiment analysis.\"\"\"\n\n    def __init__(self, raw_results: List[dict], alpha: float = 0.05):\n        self.raw_results = raw_results\n        self.alpha = alpha\n        self.df_raw = pd.DataFrame(raw_results)\n\n    def summary(self, formatted: bool = True) -&gt; pd.DataFrame:\n        \"\"\"Returns a summarized DataFrame of the analysis.\"\"\"\n        df = self.df_raw.copy()\n\n        if not formatted:\n            return df\n\n        summary_data = []\n        for _, row in df.iterrows():\n            lift_val = row[\"relative_lift\"]\n            lift_str = f\"{lift_val:+.2%}\" if not pd.isna(lift_val) else \"N/A\"\n\n            p_val = row[\"p_value\"]\n            sig_symbol = \"\"\n            if p_val &lt; 0.001:\n                sig_symbol = \"***\"\n            elif p_val &lt; 0.01:\n                sig_symbol = \"**\"\n            elif p_val &lt; 0.05:\n                sig_symbol = \"*\"\n\n            p_str = f\"{p_val:.4f}{sig_symbol}\" if not pd.isna(p_val) else \"N/A\"\n\n            lower_pct = row[\"rel_ci_lower\"]\n            upper_pct = row[\"rel_ci_upper\"]\n            ci_str = f\"[{lower_pct:+.2%}, {upper_pct:+.2%}]\" if not (pd.isna(lower_pct) or pd.isna(upper_pct)) else \"N/A\"\n\n            power_val = row[\"power\"]\n            power_str = f\"{power_val:.1%}\" if not pd.isna(power_val) else \"N/A\"\n\n            cuped_str = \"Yes\" if row[\"cuped_applied\"] else \"No\"\n            var_red_val = row[\"variance_reduction\"]\n            var_red_str = f\"{var_red_val:.1%}\" if row[\"cuped_applied\"] and not pd.isna(var_red_val) else \"-\"\n\n            summary_data.append(\n                {\n                    \"Metric\": row[\"metric_name\"],\n                    \"Type\": row[\"metric_type\"],\n                    \"Control Mean\": f\"{row['control_mean']:.4f}\",\n                    \"Treatment Mean\": f\"{row['treatment_mean']:.4f}\",\n                    \"Relative Lift\": lift_str,\n                    \"95% CI (Rel)\": ci_str,\n                    \"p-value\": p_str,\n                    \"Post-hoc Power\": power_str,\n                    \"CUPED\": cuped_str,\n                    \"Var Reduction\": var_red_str,\n                }\n            )\n\n        return pd.DataFrame(summary_data)\n\n    def plot(self, **kwargs):\n        \"\"\"Generates a forest plot of results.\"\"\"\n        # Re-routed to the reporting/export layer dynamically\n        from xpyrment.report.export import plot_forest\n        return plot_forest(self.df_raw, alpha=self.alpha, **kwargs)\n</code></pre>"},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator.AnalysisResult.plot","title":"<code>plot(**kwargs)</code>","text":"<p>Generates a forest plot of results.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def plot(self, **kwargs):\n    \"\"\"Generates a forest plot of results.\"\"\"\n    # Re-routed to the reporting/export layer dynamically\n    from xpyrment.report.export import plot_forest\n    return plot_forest(self.df_raw, alpha=self.alpha, **kwargs)\n</code></pre>"},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator.AnalysisResult.summary","title":"<code>summary(formatted=True)</code>","text":"<p>Returns a summarized DataFrame of the analysis.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def summary(self, formatted: bool = True) -&gt; pd.DataFrame:\n    \"\"\"Returns a summarized DataFrame of the analysis.\"\"\"\n    df = self.df_raw.copy()\n\n    if not formatted:\n        return df\n\n    summary_data = []\n    for _, row in df.iterrows():\n        lift_val = row[\"relative_lift\"]\n        lift_str = f\"{lift_val:+.2%}\" if not pd.isna(lift_val) else \"N/A\"\n\n        p_val = row[\"p_value\"]\n        sig_symbol = \"\"\n        if p_val &lt; 0.001:\n            sig_symbol = \"***\"\n        elif p_val &lt; 0.01:\n            sig_symbol = \"**\"\n        elif p_val &lt; 0.05:\n            sig_symbol = \"*\"\n\n        p_str = f\"{p_val:.4f}{sig_symbol}\" if not pd.isna(p_val) else \"N/A\"\n\n        lower_pct = row[\"rel_ci_lower\"]\n        upper_pct = row[\"rel_ci_upper\"]\n        ci_str = f\"[{lower_pct:+.2%}, {upper_pct:+.2%}]\" if not (pd.isna(lower_pct) or pd.isna(upper_pct)) else \"N/A\"\n\n        power_val = row[\"power\"]\n        power_str = f\"{power_val:.1%}\" if not pd.isna(power_val) else \"N/A\"\n\n        cuped_str = \"Yes\" if row[\"cuped_applied\"] else \"No\"\n        var_red_val = row[\"variance_reduction\"]\n        var_red_str = f\"{var_red_val:.1%}\" if row[\"cuped_applied\"] and not pd.isna(var_red_val) else \"-\"\n\n        summary_data.append(\n            {\n                \"Metric\": row[\"metric_name\"],\n                \"Type\": row[\"metric_type\"],\n                \"Control Mean\": f\"{row['control_mean']:.4f}\",\n                \"Treatment Mean\": f\"{row['treatment_mean']:.4f}\",\n                \"Relative Lift\": lift_str,\n                \"95% CI (Rel)\": ci_str,\n                \"p-value\": p_str,\n                \"Post-hoc Power\": power_str,\n                \"CUPED\": cuped_str,\n                \"Var Reduction\": var_red_str,\n            }\n        )\n\n    return pd.DataFrame(summary_data)\n</code></pre>"},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator.run_analysis","title":"<code>run_analysis(experiment, control='control', treatment='treatment', alpha=0.05, multi_test_correction=None)</code>","text":"<p>Executes the statistical analysis across all metrics in an Experiment.</p> <p>Parameters:</p> Name Type Description Default <code>experiment</code> <code>Experiment</code> <p>The setup container.</p> required <code>control</code> <code>str</code> <p>Control variant name.</p> <code>'control'</code> <code>treatment</code> <code>str</code> <p>Treatment variant name.</p> <code>'treatment'</code> <code>alpha</code> <code>float</code> <p>Confidence level.</p> <code>0.05</code> <code>multi_test_correction</code> <code>str</code> <p>Correction method.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnalysisResult</code> <code>AnalysisResult</code> <p>Polished summary and plots.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def run_analysis(\n    experiment: Experiment,\n    control: str = \"control\",\n    treatment: str = \"treatment\",\n    alpha: float = 0.05,\n    multi_test_correction: Optional[str] = None,\n) -&gt; AnalysisResult:\n    \"\"\"Executes the statistical analysis across all metrics in an Experiment.\n\n    Args:\n        experiment (Experiment): The setup container.\n        control (str): Control variant name.\n        treatment (str): Treatment variant name.\n        alpha (float): Confidence level.\n        multi_test_correction (str, optional): Correction method.\n\n    Returns:\n        AnalysisResult: Polished summary and plots.\n    \"\"\"\n    if not experiment.metrics:\n        raise ValueError(\"No metrics have been added to the experiment.\")\n\n    unique_variants = experiment.data[experiment.treatment_col].unique()\n    if control not in unique_variants:\n        raise ValueError(f\"Control label '{control}' not found.\")\n    if treatment not in unique_variants:\n        raise ValueError(f\"Treatment label '{treatment}' not found.\")\n\n    results = []\n    for metric in experiment.metrics:\n        res = metric.calculate(\n            experiment.data,\n            treatment_col=experiment.treatment_col,\n            control=control,\n            treatment=treatment,\n            alpha=alpha,\n        )\n        results.append(res)\n\n    # Apply corrections if requested\n    if multi_test_correction and len(results) &gt; 1:\n        p_vals = [res[\"p_value\"] for res in results]\n        adjusted_p = apply_multiple_testing_correction(p_vals, alpha=alpha, method=multi_test_correction)\n        for i, val in enumerate(adjusted_p):\n            results[i][\"p_value\"] = val\n\n    experiment.transition_to(ExperimentState.ANALYZED)\n    return AnalysisResult(results, alpha=alpha)\n</code></pre>"},{"location":"api/analyze/orchestrator/#xpyrment.analyze.orchestrator.setup","title":"<code>setup(data, treatment_col, id_col=None)</code>","text":"<p>Initializes the experimental setup container.</p> Source code in <code>src\\xpyrment\\analyze\\orchestrator.py</code> <pre><code>def setup(\n    data: pd.DataFrame,\n    treatment_col: str,\n    id_col: Optional[str] = None,\n) -&gt; Experiment:\n    \"\"\"Initializes the experimental setup container.\"\"\"\n    print(\"==========================================\")\n    print(\"      Initializing xpyrment Setup         \")\n    print(\"==========================================\")\n    print(f\"Total rows in dataset:  {len(data)}\")\n    print(f\"Treatment column:      {treatment_col}\")\n    if id_col:\n        print(f\"ID column:             {id_col}\")\n\n    exp = Experiment(data, treatment_col, id_col)\n    return exp\n</code></pre>"},{"location":"api/analyze/variance_reduction/","title":"Variance Reduction","text":""},{"location":"api/analyze/variance_reduction/#xpyrment.analyze.variance_reduction","title":"<code>xpyrment.analyze.variance_reduction</code>","text":""},{"location":"api/analyze/variance_reduction/#xpyrment.analyze.variance_reduction.apply_cuped","title":"<code>apply_cuped(df, target_col, pre_col)</code>","text":"<p>Applies Controlled-experiments Using Pre-Experiment Data (CUPED) on a series.</p> Source code in <code>src\\xpyrment\\analyze\\variance_reduction.py</code> <pre><code>def apply_cuped(df: pd.DataFrame, target_col: str, pre_col: str) -&gt; pd.Series:\n    \"\"\"Applies Controlled-experiments Using Pre-Experiment Data (CUPED) on a series.\"\"\"\n    # This is a core transformation utility, but currently handled inline inside taxonomy.py.\n    # We can write a general placeholder here.\n    return df[target_col]\n</code></pre>"},{"location":"api/analyze/inference/","title":"Inference Module","text":"<p>The <code>xpyrment.analyze.inference</code> module contains submodules and components for inference.</p>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference","title":"<code>xpyrment.analyze.inference</code>","text":""},{"location":"api/analyze/inference/#xpyrment.analyze.inference.BayesianInference","title":"<code>BayesianInference</code>","text":"<p>Computes Bayesian posterior parameters, probability of being best, expected loss, and ROPE.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bayesian.py</code> <pre><code>class BayesianInference:\n    \"\"\"Computes Bayesian posterior parameters, probability of being best, expected loss, and ROPE.\"\"\"\n\n    def __init__(self, model_type: str = \"beta_binomial\"):\n        self.model_type = model_type\n\n    def estimate_posterior(self, prior_params: dict, observed_data: dict) -&gt; dict:\n        \"\"\"Estimates conjugate posterior distributions.\"\"\"\n        # TODO: Implement conjugate Bayesian engines (Beta-Binomial, Normal-IG, Gamma-Poisson)\n        return {}\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.BayesianInference.estimate_posterior","title":"<code>estimate_posterior(prior_params, observed_data)</code>","text":"<p>Estimates conjugate posterior distributions.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bayesian.py</code> <pre><code>def estimate_posterior(self, prior_params: dict, observed_data: dict) -&gt; dict:\n    \"\"\"Estimates conjugate posterior distributions.\"\"\"\n    # TODO: Implement conjugate Bayesian engines (Beta-Binomial, Normal-IG, Gamma-Poisson)\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.SequentialInference","title":"<code>SequentialInference</code>","text":"<p>Computes sequential monitoring bounds and always-valid confidence intervals.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\sequential.py</code> <pre><code>class SequentialInference:\n    \"\"\"Computes sequential monitoring bounds and always-valid confidence intervals.\"\"\"\n\n    def calculate_always_valid_ci(self, sample_size: int, alpha: float) -&gt; tuple:\n        \"\"\"Computes continuous monitoring boundaries to prevent alpha inflation from peeking.\"\"\"\n        # TODO: Implement sequential boundary functions (mSPRT or Pocock)\n        return (-1.0, 1.0)\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.SequentialInference.calculate_always_valid_ci","title":"<code>calculate_always_valid_ci(sample_size, alpha)</code>","text":"<p>Computes continuous monitoring boundaries to prevent alpha inflation from peeking.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\sequential.py</code> <pre><code>def calculate_always_valid_ci(self, sample_size: int, alpha: float) -&gt; tuple:\n    \"\"\"Computes continuous monitoring boundaries to prevent alpha inflation from peeking.\"\"\"\n    # TODO: Implement sequential boundary functions (mSPRT or Pocock)\n    return (-1.0, 1.0)\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.route_inference_engine","title":"<code>route_inference_engine(metric, design_type)</code>","text":"<p>Routes to the correct statistical engine based on metric and design types.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Engine label (e.g., 'frequentist_t_test', 'bayesian_beta_binomial', etc.)</p> Source code in <code>src\\xpyrment\\analyze\\inference\\router.py</code> <pre><code>def route_inference_engine(metric: BaseMetric, design_type: str) -&gt; str:\n    \"\"\"Routes to the correct statistical engine based on metric and design types.\n\n    Returns:\n        str: Engine label (e.g., 'frequentist_t_test', 'bayesian_beta_binomial', etc.)\n    \"\"\"\n    # TODO: Implement full intelligent router\n    return \"frequentist_t_test\"\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.run_bootstrap_ci","title":"<code>run_bootstrap_ci(data_group, num_resamples=2000, confidence_level=0.95)</code>","text":"<p>Computes nonparametric bootstrap confidence intervals for arbitrary complex metrics.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bootstrap.py</code> <pre><code>def run_bootstrap_ci(data_group: np.ndarray, num_resamples: int = 2000, confidence_level: float = 0.95) -&gt; tuple:\n    \"\"\"Computes nonparametric bootstrap confidence intervals for arbitrary complex metrics.\"\"\"\n    # TODO: Implement bootstrap resampler (using percentile or BCa methods)\n    return (float(np.percentile(data_group, 2.5)), float(np.percentile(data_group, 97.5)))\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.run_mann_whitney_u","title":"<code>run_mann_whitney_u(group_a, group_b)</code>","text":"<p>Performs nonparametric Mann-Whitney U test for ordinal or non-normal continuous data.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\frequentist.py</code> <pre><code>def run_mann_whitney_u(group_a: np.ndarray, group_b: np.ndarray) -&gt; dict:\n    \"\"\"Performs nonparametric Mann-Whitney U test for ordinal or non-normal continuous data.\"\"\"\n    # TODO: Implement full scipy Mann-Whitney U integration\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/#xpyrment.analyze.inference.run_welch_t_test","title":"<code>run_welch_t_test(group_a, group_b)</code>","text":"<p>Performs Welch's t-test for difference of means with unequal variances.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\frequentist.py</code> <pre><code>def run_welch_t_test(group_a: np.ndarray, group_b: np.ndarray) -&gt; dict:\n    \"\"\"Performs Welch's t-test for difference of means with unequal variances.\"\"\"\n    # This is currently implemented inline within taxonomy.py\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/bayesian/","title":"Bayesian","text":""},{"location":"api/analyze/inference/bayesian/#xpyrment.analyze.inference.bayesian","title":"<code>xpyrment.analyze.inference.bayesian</code>","text":""},{"location":"api/analyze/inference/bayesian/#xpyrment.analyze.inference.bayesian.BayesianInference","title":"<code>BayesianInference</code>","text":"<p>Computes Bayesian posterior parameters, probability of being best, expected loss, and ROPE.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bayesian.py</code> <pre><code>class BayesianInference:\n    \"\"\"Computes Bayesian posterior parameters, probability of being best, expected loss, and ROPE.\"\"\"\n\n    def __init__(self, model_type: str = \"beta_binomial\"):\n        self.model_type = model_type\n\n    def estimate_posterior(self, prior_params: dict, observed_data: dict) -&gt; dict:\n        \"\"\"Estimates conjugate posterior distributions.\"\"\"\n        # TODO: Implement conjugate Bayesian engines (Beta-Binomial, Normal-IG, Gamma-Poisson)\n        return {}\n</code></pre>"},{"location":"api/analyze/inference/bayesian/#xpyrment.analyze.inference.bayesian.BayesianInference.estimate_posterior","title":"<code>estimate_posterior(prior_params, observed_data)</code>","text":"<p>Estimates conjugate posterior distributions.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bayesian.py</code> <pre><code>def estimate_posterior(self, prior_params: dict, observed_data: dict) -&gt; dict:\n    \"\"\"Estimates conjugate posterior distributions.\"\"\"\n    # TODO: Implement conjugate Bayesian engines (Beta-Binomial, Normal-IG, Gamma-Poisson)\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/bootstrap/","title":"Bootstrap","text":""},{"location":"api/analyze/inference/bootstrap/#xpyrment.analyze.inference.bootstrap","title":"<code>xpyrment.analyze.inference.bootstrap</code>","text":""},{"location":"api/analyze/inference/bootstrap/#xpyrment.analyze.inference.bootstrap.run_bootstrap_ci","title":"<code>run_bootstrap_ci(data_group, num_resamples=2000, confidence_level=0.95)</code>","text":"<p>Computes nonparametric bootstrap confidence intervals for arbitrary complex metrics.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\bootstrap.py</code> <pre><code>def run_bootstrap_ci(data_group: np.ndarray, num_resamples: int = 2000, confidence_level: float = 0.95) -&gt; tuple:\n    \"\"\"Computes nonparametric bootstrap confidence intervals for arbitrary complex metrics.\"\"\"\n    # TODO: Implement bootstrap resampler (using percentile or BCa methods)\n    return (float(np.percentile(data_group, 2.5)), float(np.percentile(data_group, 97.5)))\n</code></pre>"},{"location":"api/analyze/inference/frequentist/","title":"Frequentist","text":""},{"location":"api/analyze/inference/frequentist/#xpyrment.analyze.inference.frequentist","title":"<code>xpyrment.analyze.inference.frequentist</code>","text":""},{"location":"api/analyze/inference/frequentist/#xpyrment.analyze.inference.frequentist.run_mann_whitney_u","title":"<code>run_mann_whitney_u(group_a, group_b)</code>","text":"<p>Performs nonparametric Mann-Whitney U test for ordinal or non-normal continuous data.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\frequentist.py</code> <pre><code>def run_mann_whitney_u(group_a: np.ndarray, group_b: np.ndarray) -&gt; dict:\n    \"\"\"Performs nonparametric Mann-Whitney U test for ordinal or non-normal continuous data.\"\"\"\n    # TODO: Implement full scipy Mann-Whitney U integration\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/frequentist/#xpyrment.analyze.inference.frequentist.run_welch_t_test","title":"<code>run_welch_t_test(group_a, group_b)</code>","text":"<p>Performs Welch's t-test for difference of means with unequal variances.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\frequentist.py</code> <pre><code>def run_welch_t_test(group_a: np.ndarray, group_b: np.ndarray) -&gt; dict:\n    \"\"\"Performs Welch's t-test for difference of means with unequal variances.\"\"\"\n    # This is currently implemented inline within taxonomy.py\n    return {}\n</code></pre>"},{"location":"api/analyze/inference/router/","title":"Router","text":""},{"location":"api/analyze/inference/router/#xpyrment.analyze.inference.router","title":"<code>xpyrment.analyze.inference.router</code>","text":""},{"location":"api/analyze/inference/router/#xpyrment.analyze.inference.router.route_inference_engine","title":"<code>route_inference_engine(metric, design_type)</code>","text":"<p>Routes to the correct statistical engine based on metric and design types.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Engine label (e.g., 'frequentist_t_test', 'bayesian_beta_binomial', etc.)</p> Source code in <code>src\\xpyrment\\analyze\\inference\\router.py</code> <pre><code>def route_inference_engine(metric: BaseMetric, design_type: str) -&gt; str:\n    \"\"\"Routes to the correct statistical engine based on metric and design types.\n\n    Returns:\n        str: Engine label (e.g., 'frequentist_t_test', 'bayesian_beta_binomial', etc.)\n    \"\"\"\n    # TODO: Implement full intelligent router\n    return \"frequentist_t_test\"\n</code></pre>"},{"location":"api/analyze/inference/sequential/","title":"Sequential","text":""},{"location":"api/analyze/inference/sequential/#xpyrment.analyze.inference.sequential","title":"<code>xpyrment.analyze.inference.sequential</code>","text":""},{"location":"api/analyze/inference/sequential/#xpyrment.analyze.inference.sequential.SequentialInference","title":"<code>SequentialInference</code>","text":"<p>Computes sequential monitoring bounds and always-valid confidence intervals.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\sequential.py</code> <pre><code>class SequentialInference:\n    \"\"\"Computes sequential monitoring bounds and always-valid confidence intervals.\"\"\"\n\n    def calculate_always_valid_ci(self, sample_size: int, alpha: float) -&gt; tuple:\n        \"\"\"Computes continuous monitoring boundaries to prevent alpha inflation from peeking.\"\"\"\n        # TODO: Implement sequential boundary functions (mSPRT or Pocock)\n        return (-1.0, 1.0)\n</code></pre>"},{"location":"api/analyze/inference/sequential/#xpyrment.analyze.inference.sequential.SequentialInference.calculate_always_valid_ci","title":"<code>calculate_always_valid_ci(sample_size, alpha)</code>","text":"<p>Computes continuous monitoring boundaries to prevent alpha inflation from peeking.</p> Source code in <code>src\\xpyrment\\analyze\\inference\\sequential.py</code> <pre><code>def calculate_always_valid_ci(self, sample_size: int, alpha: float) -&gt; tuple:\n    \"\"\"Computes continuous monitoring boundaries to prevent alpha inflation from peeking.\"\"\"\n    # TODO: Implement sequential boundary functions (mSPRT or Pocock)\n    return (-1.0, 1.0)\n</code></pre>"},{"location":"api/core/","title":"Core Module","text":"<p>The <code>xpyrment.core</code> module contains submodules and components for core.</p>"},{"location":"api/core/#xpyrment.core","title":"<code>xpyrment.core</code>","text":""},{"location":"api/core/#xpyrment.core.AliasError","title":"<code>AliasError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when fractional factorial alias confounding is violated or misconfigured.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class AliasError(Exception):\n    \"\"\"Raised when fractional factorial alias confounding is violated or misconfigured.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/#xpyrment.core.Experiment","title":"<code>Experiment</code>","text":"<p>The central orchestration class for setting up and running experiments.</p> <p>Enforces strict state transitions across the experimental lifecycle.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>class Experiment:\n    \"\"\"The central orchestration class for setting up and running experiments.\n\n    Enforces strict state transitions across the experimental lifecycle.\n    \"\"\"\n\n    def __init__(self, data: pd.DataFrame, treatment_col: str, id_col: Optional[str] = None):\n        self.data = data.copy()\n        self.treatment_col = treatment_col\n        self.id_col = id_col\n        self.metrics: List[BaseMetric] = []\n        self.state = ExperimentState.CREATED\n\n        if treatment_col not in self.data.columns:\n            raise ValueError(f\"Treatment column '{treatment_col}' not found in DataFrame.\")\n        if id_col and id_col not in self.data.columns:\n            raise ValueError(f\"ID column '{id_col}' not found in DataFrame.\")\n\n    def transition_to(self, target_state: ExperimentState):\n        \"\"\"Enforces state transition logic to guarantee the phase-gated execution flow.\"\"\"\n        current_val = list(ExperimentState).index(self.state)\n        target_val = list(ExperimentState).index(target_state)\n\n        # Allow transitioning forward, or re-running analysis\n        if target_val &lt; current_val and not (\n            self.state == ExperimentState.ANALYZED and target_state == ExperimentState.ANALYZED\n        ):\n            raise PhaseOrderError(\n                f\"Cannot transition backwards from {self.state} to {target_state}.\"\n            )\n\n        self.state = target_state\n\n    def add_metrics(self, metrics: Union[BaseMetric, List[BaseMetric]]) -&gt; \"Experiment\":\n        \"\"\"Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.\"\"\"\n        if self.state not in [ExperimentState.CREATED, ExperimentState.PLANNED]:\n            raise PhaseOrderError(\n                f\"Cannot add metrics while in state {self.state}. Must be in CREATED or PLANNED.\"\n            )\n\n        if isinstance(metrics, list):\n            self.metrics.extend(metrics)\n        else:\n            self.metrics.append(metrics)\n\n        return self\n</code></pre>"},{"location":"api/core/#xpyrment.core.Experiment.add_metrics","title":"<code>add_metrics(metrics)</code>","text":"<p>Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>def add_metrics(self, metrics: Union[BaseMetric, List[BaseMetric]]) -&gt; \"Experiment\":\n    \"\"\"Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.\"\"\"\n    if self.state not in [ExperimentState.CREATED, ExperimentState.PLANNED]:\n        raise PhaseOrderError(\n            f\"Cannot add metrics while in state {self.state}. Must be in CREATED or PLANNED.\"\n        )\n\n    if isinstance(metrics, list):\n        self.metrics.extend(metrics)\n    else:\n        self.metrics.append(metrics)\n\n    return self\n</code></pre>"},{"location":"api/core/#xpyrment.core.Experiment.transition_to","title":"<code>transition_to(target_state)</code>","text":"<p>Enforces state transition logic to guarantee the phase-gated execution flow.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>def transition_to(self, target_state: ExperimentState):\n    \"\"\"Enforces state transition logic to guarantee the phase-gated execution flow.\"\"\"\n    current_val = list(ExperimentState).index(self.state)\n    target_val = list(ExperimentState).index(target_state)\n\n    # Allow transitioning forward, or re-running analysis\n    if target_val &lt; current_val and not (\n        self.state == ExperimentState.ANALYZED and target_state == ExperimentState.ANALYZED\n    ):\n        raise PhaseOrderError(\n            f\"Cannot transition backwards from {self.state} to {target_state}.\"\n        )\n\n    self.state = target_state\n</code></pre>"},{"location":"api/core/#xpyrment.core.ExperimentRegistry","title":"<code>ExperimentRegistry</code>","text":"<p>Manages immutable experiment specifications to prevent post-hoc changes (pre-registration).</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>class ExperimentRegistry:\n    \"\"\"Manages immutable experiment specifications to prevent post-hoc changes (pre-registration).\"\"\"\n\n    def __init__(self):\n        self._registry: Dict[str, Dict[str, Any]] = {}\n\n    def register_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; str:\n        \"\"\"Serializes the experiment specification, hashes it, and stores it in the registry.\n\n        Args:\n            experiment_id (str): Unique ID of the experiment.\n            spec_dict (Dict[str, Any]): Spec configurations.\n\n        Returns:\n            str: SHA-256 signature hash of the pre-registration spec.\n        \"\"\"\n        serialized = json.dumps(spec_dict, sort_keys=True)\n        spec_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n        self._registry[experiment_id] = {\n            \"spec\": spec_dict,\n            \"hash\": spec_hash,\n        }\n        return spec_hash\n\n    def verify_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; bool:\n        \"\"\"Verifies if the spec_dict matches the registered hash to prevent p-hacking.\n\n        Args:\n            experiment_id (str): Registered ID.\n            spec_dict (Dict[str, Any]): Current Spec configuration.\n\n        Returns:\n            bool: True if verified, False if there is a mismatch.\n        \"\"\"\n        if experiment_id not in self._registry:\n            return False\n\n        serialized = json.dumps(spec_dict, sort_keys=True)\n        current_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n        return current_hash == self._registry[experiment_id][\"hash\"]\n</code></pre>"},{"location":"api/core/#xpyrment.core.ExperimentRegistry.register_spec","title":"<code>register_spec(experiment_id, spec_dict)</code>","text":"<p>Serializes the experiment specification, hashes it, and stores it in the registry.</p> <p>Parameters:</p> Name Type Description Default <code>experiment_id</code> <code>str</code> <p>Unique ID of the experiment.</p> required <code>spec_dict</code> <code>Dict[str, Any]</code> <p>Spec configurations.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>SHA-256 signature hash of the pre-registration spec.</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>def register_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; str:\n    \"\"\"Serializes the experiment specification, hashes it, and stores it in the registry.\n\n    Args:\n        experiment_id (str): Unique ID of the experiment.\n        spec_dict (Dict[str, Any]): Spec configurations.\n\n    Returns:\n        str: SHA-256 signature hash of the pre-registration spec.\n    \"\"\"\n    serialized = json.dumps(spec_dict, sort_keys=True)\n    spec_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n    self._registry[experiment_id] = {\n        \"spec\": spec_dict,\n        \"hash\": spec_hash,\n    }\n    return spec_hash\n</code></pre>"},{"location":"api/core/#xpyrment.core.ExperimentRegistry.verify_spec","title":"<code>verify_spec(experiment_id, spec_dict)</code>","text":"<p>Verifies if the spec_dict matches the registered hash to prevent p-hacking.</p> <p>Parameters:</p> Name Type Description Default <code>experiment_id</code> <code>str</code> <p>Registered ID.</p> required <code>spec_dict</code> <code>Dict[str, Any]</code> <p>Current Spec configuration.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if verified, False if there is a mismatch.</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>def verify_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; bool:\n    \"\"\"Verifies if the spec_dict matches the registered hash to prevent p-hacking.\n\n    Args:\n        experiment_id (str): Registered ID.\n        spec_dict (Dict[str, Any]): Current Spec configuration.\n\n    Returns:\n        bool: True if verified, False if there is a mismatch.\n    \"\"\"\n    if experiment_id not in self._registry:\n        return False\n\n    serialized = json.dumps(spec_dict, sort_keys=True)\n    current_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n    return current_hash == self._registry[experiment_id][\"hash\"]\n</code></pre>"},{"location":"api/core/#xpyrment.core.ExperimentState","title":"<code>ExperimentState</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Enforces the phase-gated state of the experimental lifecycle.</p> Source code in <code>src\\xpyrment\\core\\state.py</code> <pre><code>class ExperimentState(Enum):\n    \"\"\"Enforces the phase-gated state of the experimental lifecycle.\"\"\"\n\n    CREATED = \"CREATED\"\n    PLANNED = \"PLANNED\"\n    DESIGNED = \"DESIGNED\"\n    RUNNING = \"RUNNING\"\n    ANALYZED = \"ANALYZED\"\n    REPORTED = \"REPORTED\"\n</code></pre>"},{"location":"api/core/#xpyrment.core.PhaseOrderError","title":"<code>PhaseOrderError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when an operation is performed in an invalid state/phase.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class PhaseOrderError(Exception):\n    \"\"\"Raised when an operation is performed in an invalid state/phase.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/#xpyrment.core.SRMError","title":"<code>SRMError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when Sample Ratio Mismatch (SRM) is detected.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class SRMError(Exception):\n    \"\"\"Raised when Sample Ratio Mismatch (SRM) is detected.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/exceptions/","title":"Exceptions","text":""},{"location":"api/core/exceptions/#xpyrment.core.exceptions","title":"<code>xpyrment.core.exceptions</code>","text":""},{"location":"api/core/exceptions/#xpyrment.core.exceptions.AliasError","title":"<code>AliasError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when fractional factorial alias confounding is violated or misconfigured.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class AliasError(Exception):\n    \"\"\"Raised when fractional factorial alias confounding is violated or misconfigured.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/exceptions/#xpyrment.core.exceptions.PhaseOrderError","title":"<code>PhaseOrderError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when an operation is performed in an invalid state/phase.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class PhaseOrderError(Exception):\n    \"\"\"Raised when an operation is performed in an invalid state/phase.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/exceptions/#xpyrment.core.exceptions.SRMError","title":"<code>SRMError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when Sample Ratio Mismatch (SRM) is detected.</p> Source code in <code>src\\xpyrment\\core\\exceptions.py</code> <pre><code>class SRMError(Exception):\n    \"\"\"Raised when Sample Ratio Mismatch (SRM) is detected.\"\"\"\n    pass\n</code></pre>"},{"location":"api/core/experiment/","title":"Experiment","text":""},{"location":"api/core/experiment/#xpyrment.core.experiment","title":"<code>xpyrment.core.experiment</code>","text":""},{"location":"api/core/experiment/#xpyrment.core.experiment.Experiment","title":"<code>Experiment</code>","text":"<p>The central orchestration class for setting up and running experiments.</p> <p>Enforces strict state transitions across the experimental lifecycle.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>class Experiment:\n    \"\"\"The central orchestration class for setting up and running experiments.\n\n    Enforces strict state transitions across the experimental lifecycle.\n    \"\"\"\n\n    def __init__(self, data: pd.DataFrame, treatment_col: str, id_col: Optional[str] = None):\n        self.data = data.copy()\n        self.treatment_col = treatment_col\n        self.id_col = id_col\n        self.metrics: List[BaseMetric] = []\n        self.state = ExperimentState.CREATED\n\n        if treatment_col not in self.data.columns:\n            raise ValueError(f\"Treatment column '{treatment_col}' not found in DataFrame.\")\n        if id_col and id_col not in self.data.columns:\n            raise ValueError(f\"ID column '{id_col}' not found in DataFrame.\")\n\n    def transition_to(self, target_state: ExperimentState):\n        \"\"\"Enforces state transition logic to guarantee the phase-gated execution flow.\"\"\"\n        current_val = list(ExperimentState).index(self.state)\n        target_val = list(ExperimentState).index(target_state)\n\n        # Allow transitioning forward, or re-running analysis\n        if target_val &lt; current_val and not (\n            self.state == ExperimentState.ANALYZED and target_state == ExperimentState.ANALYZED\n        ):\n            raise PhaseOrderError(\n                f\"Cannot transition backwards from {self.state} to {target_state}.\"\n            )\n\n        self.state = target_state\n\n    def add_metrics(self, metrics: Union[BaseMetric, List[BaseMetric]]) -&gt; \"Experiment\":\n        \"\"\"Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.\"\"\"\n        if self.state not in [ExperimentState.CREATED, ExperimentState.PLANNED]:\n            raise PhaseOrderError(\n                f\"Cannot add metrics while in state {self.state}. Must be in CREATED or PLANNED.\"\n            )\n\n        if isinstance(metrics, list):\n            self.metrics.extend(metrics)\n        else:\n            self.metrics.append(metrics)\n\n        return self\n</code></pre>"},{"location":"api/core/experiment/#xpyrment.core.experiment.Experiment.add_metrics","title":"<code>add_metrics(metrics)</code>","text":"<p>Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>def add_metrics(self, metrics: Union[BaseMetric, List[BaseMetric]]) -&gt; \"Experiment\":\n    \"\"\"Adds metrics to the experiment. Allowed in CREATED and PLANNED phases.\"\"\"\n    if self.state not in [ExperimentState.CREATED, ExperimentState.PLANNED]:\n        raise PhaseOrderError(\n            f\"Cannot add metrics while in state {self.state}. Must be in CREATED or PLANNED.\"\n        )\n\n    if isinstance(metrics, list):\n        self.metrics.extend(metrics)\n    else:\n        self.metrics.append(metrics)\n\n    return self\n</code></pre>"},{"location":"api/core/experiment/#xpyrment.core.experiment.Experiment.transition_to","title":"<code>transition_to(target_state)</code>","text":"<p>Enforces state transition logic to guarantee the phase-gated execution flow.</p> Source code in <code>src\\xpyrment\\core\\experiment.py</code> <pre><code>def transition_to(self, target_state: ExperimentState):\n    \"\"\"Enforces state transition logic to guarantee the phase-gated execution flow.\"\"\"\n    current_val = list(ExperimentState).index(self.state)\n    target_val = list(ExperimentState).index(target_state)\n\n    # Allow transitioning forward, or re-running analysis\n    if target_val &lt; current_val and not (\n        self.state == ExperimentState.ANALYZED and target_state == ExperimentState.ANALYZED\n    ):\n        raise PhaseOrderError(\n            f\"Cannot transition backwards from {self.state} to {target_state}.\"\n        )\n\n    self.state = target_state\n</code></pre>"},{"location":"api/core/registry/","title":"Registry","text":""},{"location":"api/core/registry/#xpyrment.core.registry","title":"<code>xpyrment.core.registry</code>","text":""},{"location":"api/core/registry/#xpyrment.core.registry.ExperimentRegistry","title":"<code>ExperimentRegistry</code>","text":"<p>Manages immutable experiment specifications to prevent post-hoc changes (pre-registration).</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>class ExperimentRegistry:\n    \"\"\"Manages immutable experiment specifications to prevent post-hoc changes (pre-registration).\"\"\"\n\n    def __init__(self):\n        self._registry: Dict[str, Dict[str, Any]] = {}\n\n    def register_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; str:\n        \"\"\"Serializes the experiment specification, hashes it, and stores it in the registry.\n\n        Args:\n            experiment_id (str): Unique ID of the experiment.\n            spec_dict (Dict[str, Any]): Spec configurations.\n\n        Returns:\n            str: SHA-256 signature hash of the pre-registration spec.\n        \"\"\"\n        serialized = json.dumps(spec_dict, sort_keys=True)\n        spec_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n        self._registry[experiment_id] = {\n            \"spec\": spec_dict,\n            \"hash\": spec_hash,\n        }\n        return spec_hash\n\n    def verify_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; bool:\n        \"\"\"Verifies if the spec_dict matches the registered hash to prevent p-hacking.\n\n        Args:\n            experiment_id (str): Registered ID.\n            spec_dict (Dict[str, Any]): Current Spec configuration.\n\n        Returns:\n            bool: True if verified, False if there is a mismatch.\n        \"\"\"\n        if experiment_id not in self._registry:\n            return False\n\n        serialized = json.dumps(spec_dict, sort_keys=True)\n        current_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n        return current_hash == self._registry[experiment_id][\"hash\"]\n</code></pre>"},{"location":"api/core/registry/#xpyrment.core.registry.ExperimentRegistry.register_spec","title":"<code>register_spec(experiment_id, spec_dict)</code>","text":"<p>Serializes the experiment specification, hashes it, and stores it in the registry.</p> <p>Parameters:</p> Name Type Description Default <code>experiment_id</code> <code>str</code> <p>Unique ID of the experiment.</p> required <code>spec_dict</code> <code>Dict[str, Any]</code> <p>Spec configurations.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>SHA-256 signature hash of the pre-registration spec.</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>def register_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; str:\n    \"\"\"Serializes the experiment specification, hashes it, and stores it in the registry.\n\n    Args:\n        experiment_id (str): Unique ID of the experiment.\n        spec_dict (Dict[str, Any]): Spec configurations.\n\n    Returns:\n        str: SHA-256 signature hash of the pre-registration spec.\n    \"\"\"\n    serialized = json.dumps(spec_dict, sort_keys=True)\n    spec_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n    self._registry[experiment_id] = {\n        \"spec\": spec_dict,\n        \"hash\": spec_hash,\n    }\n    return spec_hash\n</code></pre>"},{"location":"api/core/registry/#xpyrment.core.registry.ExperimentRegistry.verify_spec","title":"<code>verify_spec(experiment_id, spec_dict)</code>","text":"<p>Verifies if the spec_dict matches the registered hash to prevent p-hacking.</p> <p>Parameters:</p> Name Type Description Default <code>experiment_id</code> <code>str</code> <p>Registered ID.</p> required <code>spec_dict</code> <code>Dict[str, Any]</code> <p>Current Spec configuration.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if verified, False if there is a mismatch.</p> Source code in <code>src\\xpyrment\\core\\registry.py</code> <pre><code>def verify_spec(self, experiment_id: str, spec_dict: Dict[str, Any]) -&gt; bool:\n    \"\"\"Verifies if the spec_dict matches the registered hash to prevent p-hacking.\n\n    Args:\n        experiment_id (str): Registered ID.\n        spec_dict (Dict[str, Any]): Current Spec configuration.\n\n    Returns:\n        bool: True if verified, False if there is a mismatch.\n    \"\"\"\n    if experiment_id not in self._registry:\n        return False\n\n    serialized = json.dumps(spec_dict, sort_keys=True)\n    current_hash = hashlib.sha256(serialized.encode(\"utf-8\")).hexdigest()\n\n    return current_hash == self._registry[experiment_id][\"hash\"]\n</code></pre>"},{"location":"api/core/state/","title":"State","text":""},{"location":"api/core/state/#xpyrment.core.state","title":"<code>xpyrment.core.state</code>","text":""},{"location":"api/core/state/#xpyrment.core.state.ExperimentState","title":"<code>ExperimentState</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Enforces the phase-gated state of the experimental lifecycle.</p> Source code in <code>src\\xpyrment\\core\\state.py</code> <pre><code>class ExperimentState(Enum):\n    \"\"\"Enforces the phase-gated state of the experimental lifecycle.\"\"\"\n\n    CREATED = \"CREATED\"\n    PLANNED = \"PLANNED\"\n    DESIGNED = \"DESIGNED\"\n    RUNNING = \"RUNNING\"\n    ANALYZED = \"ANALYZED\"\n    REPORTED = \"REPORTED\"\n</code></pre>"},{"location":"api/core/types/","title":"Types","text":""},{"location":"api/core/types/#xpyrment.core.types","title":"<code>xpyrment.core.types</code>","text":""},{"location":"api/design/","title":"Design Module","text":"<p>The <code>xpyrment.design</code> module contains submodules and components for design.</p>"},{"location":"api/design/#xpyrment.design","title":"<code>xpyrment.design</code>","text":""},{"location":"api/design/#xpyrment.design.TrafficSplitter","title":"<code>TrafficSplitter</code>","text":"<p>Manages traffic fractions, holdout configurations, and ramp-up schedules.</p> Source code in <code>src\\xpyrment\\design\\splits.py</code> <pre><code>class TrafficSplitter:\n    \"\"\"Manages traffic fractions, holdout configurations, and ramp-up schedules.\"\"\"\n\n    def __init__(self, allocations: Dict[str, float], holdout_percentage: float = 0.0):\n        self.allocations = allocations\n        self.holdout_percentage = holdout_percentage\n        total_alloc = sum(allocations.values()) + holdout_percentage\n        if abs(total_alloc - 1.0) &gt; 1e-5:\n            raise ValueError(\"Total allocations including holdout must equal 1.0.\")\n\n    def get_ramp_schedule(self) -&gt; List[float]:\n        \"\"\"Returns the ramp schedule configuration.\"\"\"\n        # TODO: Implement full ramp-up schedule generator\n        return [0.01, 0.10, 0.50, 1.0]\n</code></pre>"},{"location":"api/design/#xpyrment.design.TrafficSplitter.get_ramp_schedule","title":"<code>get_ramp_schedule()</code>","text":"<p>Returns the ramp schedule configuration.</p> Source code in <code>src\\xpyrment\\design\\splits.py</code> <pre><code>def get_ramp_schedule(self) -&gt; List[float]:\n    \"\"\"Returns the ramp schedule configuration.\"\"\"\n    # TODO: Implement full ramp-up schedule generator\n    return [0.01, 0.10, 0.50, 1.0]\n</code></pre>"},{"location":"api/design/#xpyrment.design.hash_assign","title":"<code>hash_assign(unit_id, salt, variants)</code>","text":"<p>Assigns a unit to a variant deterministically using MD5/SHA-256 hashing.</p> <p>Parameters:</p> Name Type Description Default <code>unit_id</code> <code>Union[str, int]</code> <p>User/unit identifier.</p> required <code>salt</code> <code>str</code> <p>Salt representing the experiment ID to ensure orthogonal assignments.</p> required <code>variants</code> <code>List[str]</code> <p>List of variants to split across.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Assigned variant label.</p> Source code in <code>src\\xpyrment\\design\\randomization.py</code> <pre><code>def hash_assign(unit_id: Union[str, int], salt: str, variants: List[str]) -&gt; str:\n    \"\"\"Assigns a unit to a variant deterministically using MD5/SHA-256 hashing.\n\n    Args:\n        unit_id: User/unit identifier.\n        salt: Salt representing the experiment ID to ensure orthogonal assignments.\n        variants: List of variants to split across.\n\n    Returns:\n        str: Assigned variant label.\n    \"\"\"\n    # Simple hash-based assignment\n    hash_input = f\"{salt}:{unit_id}\".encode(\"utf-8\")\n    hash_hex = hashlib.md5(hash_input).hexdigest()\n    # Take first 8 chars, convert to int, and modulo by variants length\n    hash_val = int(hash_hex[:8], 16)\n    idx = hash_val % len(variants)\n    return variants[idx]\n</code></pre>"},{"location":"api/design/#xpyrment.design.stratified_randomization","title":"<code>stratified_randomization(df, strata_cols)</code>","text":"<p>Performs stratified randomization to ensure balance on continuous/categorical covariates.</p> Source code in <code>src\\xpyrment\\design\\stratification.py</code> <pre><code>def stratified_randomization(df: pd.DataFrame, strata_cols: list) -&gt; pd.DataFrame:\n    \"\"\"Performs stratified randomization to ensure balance on continuous/categorical covariates.\"\"\"\n    # TODO: Implement full stratified assignment\n    return df\n</code></pre>"},{"location":"api/design/randomization/","title":"Randomization","text":""},{"location":"api/design/randomization/#xpyrment.design.randomization","title":"<code>xpyrment.design.randomization</code>","text":""},{"location":"api/design/randomization/#xpyrment.design.randomization.hash_assign","title":"<code>hash_assign(unit_id, salt, variants)</code>","text":"<p>Assigns a unit to a variant deterministically using MD5/SHA-256 hashing.</p> <p>Parameters:</p> Name Type Description Default <code>unit_id</code> <code>Union[str, int]</code> <p>User/unit identifier.</p> required <code>salt</code> <code>str</code> <p>Salt representing the experiment ID to ensure orthogonal assignments.</p> required <code>variants</code> <code>List[str]</code> <p>List of variants to split across.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Assigned variant label.</p> Source code in <code>src\\xpyrment\\design\\randomization.py</code> <pre><code>def hash_assign(unit_id: Union[str, int], salt: str, variants: List[str]) -&gt; str:\n    \"\"\"Assigns a unit to a variant deterministically using MD5/SHA-256 hashing.\n\n    Args:\n        unit_id: User/unit identifier.\n        salt: Salt representing the experiment ID to ensure orthogonal assignments.\n        variants: List of variants to split across.\n\n    Returns:\n        str: Assigned variant label.\n    \"\"\"\n    # Simple hash-based assignment\n    hash_input = f\"{salt}:{unit_id}\".encode(\"utf-8\")\n    hash_hex = hashlib.md5(hash_input).hexdigest()\n    # Take first 8 chars, convert to int, and modulo by variants length\n    hash_val = int(hash_hex[:8], 16)\n    idx = hash_val % len(variants)\n    return variants[idx]\n</code></pre>"},{"location":"api/design/splits/","title":"Splits","text":""},{"location":"api/design/splits/#xpyrment.design.splits","title":"<code>xpyrment.design.splits</code>","text":""},{"location":"api/design/splits/#xpyrment.design.splits.TrafficSplitter","title":"<code>TrafficSplitter</code>","text":"<p>Manages traffic fractions, holdout configurations, and ramp-up schedules.</p> Source code in <code>src\\xpyrment\\design\\splits.py</code> <pre><code>class TrafficSplitter:\n    \"\"\"Manages traffic fractions, holdout configurations, and ramp-up schedules.\"\"\"\n\n    def __init__(self, allocations: Dict[str, float], holdout_percentage: float = 0.0):\n        self.allocations = allocations\n        self.holdout_percentage = holdout_percentage\n        total_alloc = sum(allocations.values()) + holdout_percentage\n        if abs(total_alloc - 1.0) &gt; 1e-5:\n            raise ValueError(\"Total allocations including holdout must equal 1.0.\")\n\n    def get_ramp_schedule(self) -&gt; List[float]:\n        \"\"\"Returns the ramp schedule configuration.\"\"\"\n        # TODO: Implement full ramp-up schedule generator\n        return [0.01, 0.10, 0.50, 1.0]\n</code></pre>"},{"location":"api/design/splits/#xpyrment.design.splits.TrafficSplitter.get_ramp_schedule","title":"<code>get_ramp_schedule()</code>","text":"<p>Returns the ramp schedule configuration.</p> Source code in <code>src\\xpyrment\\design\\splits.py</code> <pre><code>def get_ramp_schedule(self) -&gt; List[float]:\n    \"\"\"Returns the ramp schedule configuration.\"\"\"\n    # TODO: Implement full ramp-up schedule generator\n    return [0.01, 0.10, 0.50, 1.0]\n</code></pre>"},{"location":"api/design/stratification/","title":"Stratification","text":""},{"location":"api/design/stratification/#xpyrment.design.stratification","title":"<code>xpyrment.design.stratification</code>","text":""},{"location":"api/design/stratification/#xpyrment.design.stratification.stratified_randomization","title":"<code>stratified_randomization(df, strata_cols)</code>","text":"<p>Performs stratified randomization to ensure balance on continuous/categorical covariates.</p> Source code in <code>src\\xpyrment\\design\\stratification.py</code> <pre><code>def stratified_randomization(df: pd.DataFrame, strata_cols: list) -&gt; pd.DataFrame:\n    \"\"\"Performs stratified randomization to ensure balance on continuous/categorical covariates.\"\"\"\n    # TODO: Implement full stratified assignment\n    return df\n</code></pre>"},{"location":"api/design/doe/","title":"DoE Module","text":"<p>The <code>xpyrment.design.doe</code> module contains submodules and components for doe.</p>"},{"location":"api/design/doe/#xpyrment.design.doe","title":"<code>xpyrment.design.doe</code>","text":""},{"location":"api/design/doe/#xpyrment.design.doe.BoxBehnkenDesign","title":"<code>BoxBehnkenDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Box-Behnken Designs to estimate non-linear response surfaces in fewer runs.</p> Source code in <code>src\\xpyrment\\design\\doe\\box_behnken.py</code> <pre><code>class BoxBehnkenDesign(DesignMatrix):\n    \"\"\"Generates Box-Behnken Designs to estimate non-linear response surfaces in fewer runs.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Box-Behnken matrix builder\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.CentralCompositeDesign","title":"<code>CentralCompositeDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Central Composite Designs (CCD) for response surface modeling.</p> Source code in <code>src\\xpyrment\\design\\doe\\ccd.py</code> <pre><code>class CentralCompositeDesign(DesignMatrix):\n    \"\"\"Generates Central Composite Designs (CCD) for response surface modeling.\"\"\"\n\n    def __init__(self, factors: dict, alpha: str = \"orthogonal\"):\n        super().__init__(factors)\n        self.alpha_type = alpha\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement CCD factorial, axial, and center points generation\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.DOptimalDesign","title":"<code>DOptimalDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Optimizes the design matrix determinant |X'X| using coordinate exchange algorithms.</p> Source code in <code>src\\xpyrment\\design\\doe\\d_optimal.py</code> <pre><code>class DOptimalDesign(DesignMatrix):\n    \"\"\"Optimizes the design matrix determinant |X'X| using coordinate exchange algorithms.\"\"\"\n\n    def __init__(self, factors: dict, num_runs: int):\n        super().__init__(factors)\n        self.num_runs = num_runs\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement D-optimal coordinate exchange algorithm\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.DefinitiveScreeningDesign","title":"<code>DefinitiveScreeningDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates a Definitive Screening Design (DSD) to estimate linear, quadratic, and 2-way terms.</p> Source code in <code>src\\xpyrment\\design\\doe\\dsd.py</code> <pre><code>class DefinitiveScreeningDesign(DesignMatrix):\n    \"\"\"Generates a Definitive Screening Design (DSD) to estimate linear, quadratic, and 2-way terms.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement DSD algorithm (using conference matrices)\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.DesignMatrix","title":"<code>DesignMatrix</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class representing a Design Matrix in Classical Design of Experiments (DoE).</p> Source code in <code>src\\xpyrment\\design\\doe\\base.py</code> <pre><code>class DesignMatrix(ABC):\n    \"\"\"Abstract base class representing a Design Matrix in Classical Design of Experiments (DoE).\"\"\"\n\n    def __init__(self, factors: Dict[str, List[float]]):\n        self.factors = factors\n\n    @abstractmethod\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates and returns the DoE design matrix.\"\"\"\n        pass\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.DesignMatrix.generate","title":"<code>generate()</code>  <code>abstractmethod</code>","text":"<p>Generates and returns the DoE design matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\base.py</code> <pre><code>@abstractmethod\ndef generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates and returns the DoE design matrix.\"\"\"\n    pass\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.EVOPDesign","title":"<code>EVOPDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Manages Evolutionary Operation (EVOP) structures for live process improvements.</p> Source code in <code>src\\xpyrment\\design\\doe\\evop.py</code> <pre><code>class EVOPDesign(DesignMatrix):\n    \"\"\"Manages Evolutionary Operation (EVOP) structures for live process improvements.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement EVOP phase matrix loops\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.FractionalFactorialDesign","title":"<code>FractionalFactorialDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates fractional factorial experimental designs, showing resolution and aliasing.</p> Source code in <code>src\\xpyrment\\design\\doe\\fractional_factorial.py</code> <pre><code>class FractionalFactorialDesign(DesignMatrix):\n    \"\"\"Generates fractional factorial experimental designs, showing resolution and aliasing.\"\"\"\n\n    def __init__(self, factors: dict, generator_string: str):\n        super().__init__(factors)\n        self.generator_string = generator_string\n\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates fractional factorial matrix.\"\"\"\n        # TODO: Implement generator parsing and fractional matrix construction\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.FractionalFactorialDesign.generate","title":"<code>generate()</code>","text":"<p>Generates fractional factorial matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\fractional_factorial.py</code> <pre><code>def generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates fractional factorial matrix.\"\"\"\n    # TODO: Implement generator parsing and fractional matrix construction\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.FullFactorialDesign","title":"<code>FullFactorialDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates a full factorial experimental design matrix for all factor combinations.</p> Source code in <code>src\\xpyrment\\design\\doe\\full_factorial.py</code> <pre><code>class FullFactorialDesign(DesignMatrix):\n    \"\"\"Generates a full factorial experimental design matrix for all factor combinations.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates full factorial matrix.\"\"\"\n        # Simple placeholder implementation\n        import itertools\n\n        keys = list(self.factors.keys())\n        values = list(self.factors.values())\n        combinations = list(itertools.product(*values))\n\n        df = pd.DataFrame(combinations, columns=keys)\n        return df\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.FullFactorialDesign.generate","title":"<code>generate()</code>","text":"<p>Generates full factorial matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\full_factorial.py</code> <pre><code>def generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates full factorial matrix.\"\"\"\n    # Simple placeholder implementation\n    import itertools\n\n    keys = list(self.factors.keys())\n    values = list(self.factors.values())\n    combinations = list(itertools.product(*values))\n\n    df = pd.DataFrame(combinations, columns=keys)\n    return df\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.LatinHypercubeDesign","title":"<code>LatinHypercubeDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Latin Hypercube Sampling designs for multidimensional factors.</p> Source code in <code>src\\xpyrment\\design\\doe\\lhs.py</code> <pre><code>class LatinHypercubeDesign(DesignMatrix):\n    \"\"\"Generates Latin Hypercube Sampling designs for multidimensional factors.\"\"\"\n\n    def __init__(self, factors: dict, num_samples: int):\n        super().__init__(factors)\n        self.num_samples = num_samples\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement LHS random/maximim stratification\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.MixtureDesign","title":"<code>MixtureDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates designs for mixture factor spaces where components must sum to 1.0 (e.g., recipe mixes).</p> Source code in <code>src\\xpyrment\\design\\doe\\mixture.py</code> <pre><code>class MixtureDesign(DesignMatrix):\n    \"\"\"Generates designs for mixture factor spaces where components must sum to 1.0 (e.g., recipe mixes).\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Simplex Lattice or Simplex Centroid mixture designs\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.PlackettBurmanDesign","title":"<code>PlackettBurmanDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Plackett-Burman screening design matrices to identify main effects in few runs.</p> Source code in <code>src\\xpyrment\\design\\doe\\plackett_burman.py</code> <pre><code>class PlackettBurmanDesign(DesignMatrix):\n    \"\"\"Generates Plackett-Burman screening design matrices to identify main effects in few runs.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Plackett-Burman matrix builder\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.SwitchbackDesign","title":"<code>SwitchbackDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates time/geo crossover switchback experimental designs for marketplace tests.</p> Source code in <code>src\\xpyrment\\design\\doe\\switchback.py</code> <pre><code>class SwitchbackDesign(DesignMatrix):\n    \"\"\"Generates time/geo crossover switchback experimental designs for marketplace tests.\"\"\"\n\n    def __init__(self, factors: dict, unit_window_hours: int = 2):\n        super().__init__(factors)\n        self.unit_window_hours = unit_window_hours\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement switchback crossover matrix allocations\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/#xpyrment.design.doe.TaguchiDesign","title":"<code>TaguchiDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Taguchi orthogonal array designs and S/N ratio mapping specifications.</p> Source code in <code>src\\xpyrment\\design\\doe\\taguchi.py</code> <pre><code>class TaguchiDesign(DesignMatrix):\n    \"\"\"Generates Taguchi orthogonal array designs and S/N ratio mapping specifications.\"\"\"\n\n    def __init__(self, factors: dict, array_name: str):\n        super().__init__(factors)\n        self.array_name = array_name\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Taguchi orthogonal array database selection\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/base/","title":"Base","text":""},{"location":"api/design/doe/base/#xpyrment.design.doe.base","title":"<code>xpyrment.design.doe.base</code>","text":""},{"location":"api/design/doe/base/#xpyrment.design.doe.base.DesignMatrix","title":"<code>DesignMatrix</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class representing a Design Matrix in Classical Design of Experiments (DoE).</p> Source code in <code>src\\xpyrment\\design\\doe\\base.py</code> <pre><code>class DesignMatrix(ABC):\n    \"\"\"Abstract base class representing a Design Matrix in Classical Design of Experiments (DoE).\"\"\"\n\n    def __init__(self, factors: Dict[str, List[float]]):\n        self.factors = factors\n\n    @abstractmethod\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates and returns the DoE design matrix.\"\"\"\n        pass\n</code></pre>"},{"location":"api/design/doe/base/#xpyrment.design.doe.base.DesignMatrix.generate","title":"<code>generate()</code>  <code>abstractmethod</code>","text":"<p>Generates and returns the DoE design matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\base.py</code> <pre><code>@abstractmethod\ndef generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates and returns the DoE design matrix.\"\"\"\n    pass\n</code></pre>"},{"location":"api/design/doe/box_behnken/","title":"Box Behnken","text":""},{"location":"api/design/doe/box_behnken/#xpyrment.design.doe.box_behnken","title":"<code>xpyrment.design.doe.box_behnken</code>","text":""},{"location":"api/design/doe/box_behnken/#xpyrment.design.doe.box_behnken.BoxBehnkenDesign","title":"<code>BoxBehnkenDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Box-Behnken Designs to estimate non-linear response surfaces in fewer runs.</p> Source code in <code>src\\xpyrment\\design\\doe\\box_behnken.py</code> <pre><code>class BoxBehnkenDesign(DesignMatrix):\n    \"\"\"Generates Box-Behnken Designs to estimate non-linear response surfaces in fewer runs.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Box-Behnken matrix builder\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/ccd/","title":"CCD","text":""},{"location":"api/design/doe/ccd/#xpyrment.design.doe.ccd","title":"<code>xpyrment.design.doe.ccd</code>","text":""},{"location":"api/design/doe/ccd/#xpyrment.design.doe.ccd.CentralCompositeDesign","title":"<code>CentralCompositeDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Central Composite Designs (CCD) for response surface modeling.</p> Source code in <code>src\\xpyrment\\design\\doe\\ccd.py</code> <pre><code>class CentralCompositeDesign(DesignMatrix):\n    \"\"\"Generates Central Composite Designs (CCD) for response surface modeling.\"\"\"\n\n    def __init__(self, factors: dict, alpha: str = \"orthogonal\"):\n        super().__init__(factors)\n        self.alpha_type = alpha\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement CCD factorial, axial, and center points generation\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/d_optimal/","title":"D Optimal","text":""},{"location":"api/design/doe/d_optimal/#xpyrment.design.doe.d_optimal","title":"<code>xpyrment.design.doe.d_optimal</code>","text":""},{"location":"api/design/doe/d_optimal/#xpyrment.design.doe.d_optimal.DOptimalDesign","title":"<code>DOptimalDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Optimizes the design matrix determinant |X'X| using coordinate exchange algorithms.</p> Source code in <code>src\\xpyrment\\design\\doe\\d_optimal.py</code> <pre><code>class DOptimalDesign(DesignMatrix):\n    \"\"\"Optimizes the design matrix determinant |X'X| using coordinate exchange algorithms.\"\"\"\n\n    def __init__(self, factors: dict, num_runs: int):\n        super().__init__(factors)\n        self.num_runs = num_runs\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement D-optimal coordinate exchange algorithm\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/dsd/","title":"DSD","text":""},{"location":"api/design/doe/dsd/#xpyrment.design.doe.dsd","title":"<code>xpyrment.design.doe.dsd</code>","text":""},{"location":"api/design/doe/dsd/#xpyrment.design.doe.dsd.DefinitiveScreeningDesign","title":"<code>DefinitiveScreeningDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates a Definitive Screening Design (DSD) to estimate linear, quadratic, and 2-way terms.</p> Source code in <code>src\\xpyrment\\design\\doe\\dsd.py</code> <pre><code>class DefinitiveScreeningDesign(DesignMatrix):\n    \"\"\"Generates a Definitive Screening Design (DSD) to estimate linear, quadratic, and 2-way terms.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement DSD algorithm (using conference matrices)\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/evop/","title":"EVOP","text":""},{"location":"api/design/doe/evop/#xpyrment.design.doe.evop","title":"<code>xpyrment.design.doe.evop</code>","text":""},{"location":"api/design/doe/evop/#xpyrment.design.doe.evop.EVOPDesign","title":"<code>EVOPDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Manages Evolutionary Operation (EVOP) structures for live process improvements.</p> Source code in <code>src\\xpyrment\\design\\doe\\evop.py</code> <pre><code>class EVOPDesign(DesignMatrix):\n    \"\"\"Manages Evolutionary Operation (EVOP) structures for live process improvements.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement EVOP phase matrix loops\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/fractional_factorial/","title":"Fractional Factorial","text":""},{"location":"api/design/doe/fractional_factorial/#xpyrment.design.doe.fractional_factorial","title":"<code>xpyrment.design.doe.fractional_factorial</code>","text":""},{"location":"api/design/doe/fractional_factorial/#xpyrment.design.doe.fractional_factorial.FractionalFactorialDesign","title":"<code>FractionalFactorialDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates fractional factorial experimental designs, showing resolution and aliasing.</p> Source code in <code>src\\xpyrment\\design\\doe\\fractional_factorial.py</code> <pre><code>class FractionalFactorialDesign(DesignMatrix):\n    \"\"\"Generates fractional factorial experimental designs, showing resolution and aliasing.\"\"\"\n\n    def __init__(self, factors: dict, generator_string: str):\n        super().__init__(factors)\n        self.generator_string = generator_string\n\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates fractional factorial matrix.\"\"\"\n        # TODO: Implement generator parsing and fractional matrix construction\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/fractional_factorial/#xpyrment.design.doe.fractional_factorial.FractionalFactorialDesign.generate","title":"<code>generate()</code>","text":"<p>Generates fractional factorial matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\fractional_factorial.py</code> <pre><code>def generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates fractional factorial matrix.\"\"\"\n    # TODO: Implement generator parsing and fractional matrix construction\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/full_factorial/","title":"Full Factorial","text":""},{"location":"api/design/doe/full_factorial/#xpyrment.design.doe.full_factorial","title":"<code>xpyrment.design.doe.full_factorial</code>","text":""},{"location":"api/design/doe/full_factorial/#xpyrment.design.doe.full_factorial.FullFactorialDesign","title":"<code>FullFactorialDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates a full factorial experimental design matrix for all factor combinations.</p> Source code in <code>src\\xpyrment\\design\\doe\\full_factorial.py</code> <pre><code>class FullFactorialDesign(DesignMatrix):\n    \"\"\"Generates a full factorial experimental design matrix for all factor combinations.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        \"\"\"Generates full factorial matrix.\"\"\"\n        # Simple placeholder implementation\n        import itertools\n\n        keys = list(self.factors.keys())\n        values = list(self.factors.values())\n        combinations = list(itertools.product(*values))\n\n        df = pd.DataFrame(combinations, columns=keys)\n        return df\n</code></pre>"},{"location":"api/design/doe/full_factorial/#xpyrment.design.doe.full_factorial.FullFactorialDesign.generate","title":"<code>generate()</code>","text":"<p>Generates full factorial matrix.</p> Source code in <code>src\\xpyrment\\design\\doe\\full_factorial.py</code> <pre><code>def generate(self) -&gt; pd.DataFrame:\n    \"\"\"Generates full factorial matrix.\"\"\"\n    # Simple placeholder implementation\n    import itertools\n\n    keys = list(self.factors.keys())\n    values = list(self.factors.values())\n    combinations = list(itertools.product(*values))\n\n    df = pd.DataFrame(combinations, columns=keys)\n    return df\n</code></pre>"},{"location":"api/design/doe/lhs/","title":"LHS","text":""},{"location":"api/design/doe/lhs/#xpyrment.design.doe.lhs","title":"<code>xpyrment.design.doe.lhs</code>","text":""},{"location":"api/design/doe/lhs/#xpyrment.design.doe.lhs.LatinHypercubeDesign","title":"<code>LatinHypercubeDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Latin Hypercube Sampling designs for multidimensional factors.</p> Source code in <code>src\\xpyrment\\design\\doe\\lhs.py</code> <pre><code>class LatinHypercubeDesign(DesignMatrix):\n    \"\"\"Generates Latin Hypercube Sampling designs for multidimensional factors.\"\"\"\n\n    def __init__(self, factors: dict, num_samples: int):\n        super().__init__(factors)\n        self.num_samples = num_samples\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement LHS random/maximim stratification\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/mixture/","title":"Mixture","text":""},{"location":"api/design/doe/mixture/#xpyrment.design.doe.mixture","title":"<code>xpyrment.design.doe.mixture</code>","text":""},{"location":"api/design/doe/mixture/#xpyrment.design.doe.mixture.MixtureDesign","title":"<code>MixtureDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates designs for mixture factor spaces where components must sum to 1.0 (e.g., recipe mixes).</p> Source code in <code>src\\xpyrment\\design\\doe\\mixture.py</code> <pre><code>class MixtureDesign(DesignMatrix):\n    \"\"\"Generates designs for mixture factor spaces where components must sum to 1.0 (e.g., recipe mixes).\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Simplex Lattice or Simplex Centroid mixture designs\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/plackett_burman/","title":"Plackett Burman","text":""},{"location":"api/design/doe/plackett_burman/#xpyrment.design.doe.plackett_burman","title":"<code>xpyrment.design.doe.plackett_burman</code>","text":""},{"location":"api/design/doe/plackett_burman/#xpyrment.design.doe.plackett_burman.PlackettBurmanDesign","title":"<code>PlackettBurmanDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Plackett-Burman screening design matrices to identify main effects in few runs.</p> Source code in <code>src\\xpyrment\\design\\doe\\plackett_burman.py</code> <pre><code>class PlackettBurmanDesign(DesignMatrix):\n    \"\"\"Generates Plackett-Burman screening design matrices to identify main effects in few runs.\"\"\"\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Plackett-Burman matrix builder\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/switchback/","title":"Switchback","text":""},{"location":"api/design/doe/switchback/#xpyrment.design.doe.switchback","title":"<code>xpyrment.design.doe.switchback</code>","text":""},{"location":"api/design/doe/switchback/#xpyrment.design.doe.switchback.SwitchbackDesign","title":"<code>SwitchbackDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates time/geo crossover switchback experimental designs for marketplace tests.</p> Source code in <code>src\\xpyrment\\design\\doe\\switchback.py</code> <pre><code>class SwitchbackDesign(DesignMatrix):\n    \"\"\"Generates time/geo crossover switchback experimental designs for marketplace tests.\"\"\"\n\n    def __init__(self, factors: dict, unit_window_hours: int = 2):\n        super().__init__(factors)\n        self.unit_window_hours = unit_window_hours\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement switchback crossover matrix allocations\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/design/doe/taguchi/","title":"Taguchi","text":""},{"location":"api/design/doe/taguchi/#xpyrment.design.doe.taguchi","title":"<code>xpyrment.design.doe.taguchi</code>","text":""},{"location":"api/design/doe/taguchi/#xpyrment.design.doe.taguchi.TaguchiDesign","title":"<code>TaguchiDesign</code>","text":"<p>               Bases: <code>DesignMatrix</code></p> <p>Generates Taguchi orthogonal array designs and S/N ratio mapping specifications.</p> Source code in <code>src\\xpyrment\\design\\doe\\taguchi.py</code> <pre><code>class TaguchiDesign(DesignMatrix):\n    \"\"\"Generates Taguchi orthogonal array designs and S/N ratio mapping specifications.\"\"\"\n\n    def __init__(self, factors: dict, array_name: str):\n        super().__init__(factors)\n        self.array_name = array_name\n\n    def generate(self) -&gt; pd.DataFrame:\n        # TODO: Implement Taguchi orthogonal array database selection\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/interactions/","title":"Interactions Module","text":"<p>The <code>xpyrment.interactions</code> module contains submodules and components for interactions.</p>"},{"location":"api/interactions/#xpyrment.interactions","title":"<code>xpyrment.interactions</code>","text":""},{"location":"api/interactions/#xpyrment.interactions.InteractionDetector","title":"<code>InteractionDetector</code>","text":"<p>Dispatches multi-factor, covariate-treatment, and non-linear interactions.</p> Source code in <code>src\\xpyrment\\interactions\\detector.py</code> <pre><code>class InteractionDetector:\n    \"\"\"Dispatches multi-factor, covariate-treatment, and non-linear interactions.\"\"\"\n\n    def __init__(self, experiment):\n        self.experiment = experiment\n\n    def detect_all(self) -&gt; dict:\n        \"\"\"Runs ANOVA and regression checks to identify interaction terms.\"\"\"\n        # TODO: Implement dispatcher\n        return {}\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.InteractionDetector.detect_all","title":"<code>detect_all()</code>","text":"<p>Runs ANOVA and regression checks to identify interaction terms.</p> Source code in <code>src\\xpyrment\\interactions\\detector.py</code> <pre><code>def detect_all(self) -&gt; dict:\n    \"\"\"Runs ANOVA and regression checks to identify interaction terms.\"\"\"\n    # TODO: Implement dispatcher\n    return {}\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.calculate_shap_interactions","title":"<code>calculate_shap_interactions(model, X_data)</code>","text":"<p>Computes SHAP interaction values to decompose multi-factor combinations (expensive/gated).</p> Source code in <code>src\\xpyrment\\interactions\\shap.py</code> <pre><code>def calculate_shap_interactions(model, X_data) -&gt; list:\n    \"\"\"Computes SHAP interaction values to decompose multi-factor combinations (expensive/gated).\"\"\"\n    # TODO: Implement optional shap dependency check and interaction calculation\n    return []\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.check_treatment_covariate_interaction","title":"<code>check_treatment_covariate_interaction(df, treatment_col, covariate_col, target_col)</code>","text":"<p>Computes Likelihood Ratio Tests (LRT) to check if covariate balances interact with treatment splits.</p> Source code in <code>src\\xpyrment\\interactions\\regression.py</code> <pre><code>def check_treatment_covariate_interaction(df: pd.DataFrame, treatment_col: str, covariate_col: str, target_col: str) -&gt; float:\n    \"\"\"Computes Likelihood Ratio Tests (LRT) to check if covariate balances interact with treatment splits.\"\"\"\n    # TODO: Implement interactive regression model\n    return 1.0\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.compute_friedman_h_statistic","title":"<code>compute_friedman_h_statistic(model, X_data, feature_i, feature_j)</code>","text":"<p>Computes model-agnostic Friedman H-statistic representing degree of interaction.</p> Source code in <code>src\\xpyrment\\interactions\\hstat.py</code> <pre><code>def compute_friedman_h_statistic(model, X_data, feature_i: str, feature_j: str) -&gt; float:\n    \"\"\"Computes model-agnostic Friedman H-statistic representing degree of interaction.\"\"\"\n    # TODO: Implement H-statistic calculations\n    return 0.0\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.plot_interaction_heatmap","title":"<code>plot_interaction_heatmap(df_interactions)</code>","text":"<p>Generates interaction term heatmaps using matplotlib.</p> Source code in <code>src\\xpyrment\\interactions\\plots.py</code> <pre><code>def plot_interaction_heatmap(df_interactions: pd.DataFrame) -&gt; tuple:\n    \"\"\"Generates interaction term heatmaps using matplotlib.\"\"\"\n    # TODO: Implement plotting code\n    fig, ax = plt.subplots()\n    return fig, ax\n</code></pre>"},{"location":"api/interactions/#xpyrment.interactions.run_factorial_anova","title":"<code>run_factorial_anova(df, formula)</code>","text":"<p>Computes factorial ANOVA tables with interaction terms for DoE factors.</p> Source code in <code>src\\xpyrment\\interactions\\anova.py</code> <pre><code>def run_factorial_anova(df: pd.DataFrame, formula: str) -&gt; pd.DataFrame:\n    \"\"\"Computes factorial ANOVA tables with interaction terms for DoE factors.\"\"\"\n    # TODO: Implement statsmodels OLS and anova_lm integration\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/interactions/anova/","title":"ANOVA","text":""},{"location":"api/interactions/anova/#xpyrment.interactions.anova","title":"<code>xpyrment.interactions.anova</code>","text":""},{"location":"api/interactions/anova/#xpyrment.interactions.anova.run_factorial_anova","title":"<code>run_factorial_anova(df, formula)</code>","text":"<p>Computes factorial ANOVA tables with interaction terms for DoE factors.</p> Source code in <code>src\\xpyrment\\interactions\\anova.py</code> <pre><code>def run_factorial_anova(df: pd.DataFrame, formula: str) -&gt; pd.DataFrame:\n    \"\"\"Computes factorial ANOVA tables with interaction terms for DoE factors.\"\"\"\n    # TODO: Implement statsmodels OLS and anova_lm integration\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/interactions/detector/","title":"Detector","text":""},{"location":"api/interactions/detector/#xpyrment.interactions.detector","title":"<code>xpyrment.interactions.detector</code>","text":""},{"location":"api/interactions/detector/#xpyrment.interactions.detector.InteractionDetector","title":"<code>InteractionDetector</code>","text":"<p>Dispatches multi-factor, covariate-treatment, and non-linear interactions.</p> Source code in <code>src\\xpyrment\\interactions\\detector.py</code> <pre><code>class InteractionDetector:\n    \"\"\"Dispatches multi-factor, covariate-treatment, and non-linear interactions.\"\"\"\n\n    def __init__(self, experiment):\n        self.experiment = experiment\n\n    def detect_all(self) -&gt; dict:\n        \"\"\"Runs ANOVA and regression checks to identify interaction terms.\"\"\"\n        # TODO: Implement dispatcher\n        return {}\n</code></pre>"},{"location":"api/interactions/detector/#xpyrment.interactions.detector.InteractionDetector.detect_all","title":"<code>detect_all()</code>","text":"<p>Runs ANOVA and regression checks to identify interaction terms.</p> Source code in <code>src\\xpyrment\\interactions\\detector.py</code> <pre><code>def detect_all(self) -&gt; dict:\n    \"\"\"Runs ANOVA and regression checks to identify interaction terms.\"\"\"\n    # TODO: Implement dispatcher\n    return {}\n</code></pre>"},{"location":"api/interactions/hstat/","title":"Hstat","text":""},{"location":"api/interactions/hstat/#xpyrment.interactions.hstat","title":"<code>xpyrment.interactions.hstat</code>","text":""},{"location":"api/interactions/hstat/#xpyrment.interactions.hstat.compute_friedman_h_statistic","title":"<code>compute_friedman_h_statistic(model, X_data, feature_i, feature_j)</code>","text":"<p>Computes model-agnostic Friedman H-statistic representing degree of interaction.</p> Source code in <code>src\\xpyrment\\interactions\\hstat.py</code> <pre><code>def compute_friedman_h_statistic(model, X_data, feature_i: str, feature_j: str) -&gt; float:\n    \"\"\"Computes model-agnostic Friedman H-statistic representing degree of interaction.\"\"\"\n    # TODO: Implement H-statistic calculations\n    return 0.0\n</code></pre>"},{"location":"api/interactions/plots/","title":"Plots","text":""},{"location":"api/interactions/plots/#xpyrment.interactions.plots","title":"<code>xpyrment.interactions.plots</code>","text":""},{"location":"api/interactions/plots/#xpyrment.interactions.plots.plot_interaction_heatmap","title":"<code>plot_interaction_heatmap(df_interactions)</code>","text":"<p>Generates interaction term heatmaps using matplotlib.</p> Source code in <code>src\\xpyrment\\interactions\\plots.py</code> <pre><code>def plot_interaction_heatmap(df_interactions: pd.DataFrame) -&gt; tuple:\n    \"\"\"Generates interaction term heatmaps using matplotlib.\"\"\"\n    # TODO: Implement plotting code\n    fig, ax = plt.subplots()\n    return fig, ax\n</code></pre>"},{"location":"api/interactions/regression/","title":"Regression","text":""},{"location":"api/interactions/regression/#xpyrment.interactions.regression","title":"<code>xpyrment.interactions.regression</code>","text":""},{"location":"api/interactions/regression/#xpyrment.interactions.regression.check_treatment_covariate_interaction","title":"<code>check_treatment_covariate_interaction(df, treatment_col, covariate_col, target_col)</code>","text":"<p>Computes Likelihood Ratio Tests (LRT) to check if covariate balances interact with treatment splits.</p> Source code in <code>src\\xpyrment\\interactions\\regression.py</code> <pre><code>def check_treatment_covariate_interaction(df: pd.DataFrame, treatment_col: str, covariate_col: str, target_col: str) -&gt; float:\n    \"\"\"Computes Likelihood Ratio Tests (LRT) to check if covariate balances interact with treatment splits.\"\"\"\n    # TODO: Implement interactive regression model\n    return 1.0\n</code></pre>"},{"location":"api/interactions/shap/","title":"SHAP","text":""},{"location":"api/interactions/shap/#xpyrment.interactions.shap","title":"<code>xpyrment.interactions.shap</code>","text":""},{"location":"api/interactions/shap/#xpyrment.interactions.shap.calculate_shap_interactions","title":"<code>calculate_shap_interactions(model, X_data)</code>","text":"<p>Computes SHAP interaction values to decompose multi-factor combinations (expensive/gated).</p> Source code in <code>src\\xpyrment\\interactions\\shap.py</code> <pre><code>def calculate_shap_interactions(model, X_data) -&gt; list:\n    \"\"\"Computes SHAP interaction values to decompose multi-factor combinations (expensive/gated).\"\"\"\n    # TODO: Implement optional shap dependency check and interaction calculation\n    return []\n</code></pre>"},{"location":"api/interpret/","title":"Interpret Module","text":"<p>The <code>xpyrment.interpret</code> module contains submodules and components for interpret.</p>"},{"location":"api/interpret/#xpyrment.interpret","title":"<code>xpyrment.interpret</code>","text":""},{"location":"api/interpret/#xpyrment.interpret.check_practical_significance","title":"<code>check_practical_significance(relative_lift, min_valuable_effect)</code>","text":"<p>Verifies if the measured lift satisfies the minimal valuable business effect (MVE).</p> Source code in <code>src\\xpyrment\\interpret\\significance.py</code> <pre><code>def check_practical_significance(relative_lift: float, min_valuable_effect: float) -&gt; bool:\n    \"\"\"Verifies if the measured lift satisfies the minimal valuable business effect (MVE).\"\"\"\n    return relative_lift &gt;= min_valuable_effect\n</code></pre>"},{"location":"api/interpret/#xpyrment.interpret.compute_cohens_d","title":"<code>compute_cohens_d(group_a, group_b)</code>","text":"<p>Computes standard standardized effect size using Cohen's d formula.</p> Source code in <code>src\\xpyrment\\interpret\\effect_size.py</code> <pre><code>def compute_cohens_d(group_a: np.ndarray, group_b: np.ndarray) -&gt; float:\n    \"\"\"Computes standard standardized effect size using Cohen's d formula.\"\"\"\n    mean_a, mean_b = np.mean(group_a), np.mean(group_b)\n    var_a, var_b = np.var(group_a, ddof=1), np.var(group_b, ddof=1)\n    n_a, n_b = len(group_a), len(group_b)\n\n    pooled_std = np.sqrt(((n_a - 1) * var_a + (n_b - 1) * var_b) / (n_a + n_b - 2))\n    if pooled_std &gt; 0:\n        return (mean_b - mean_a) / pooled_std\n    return 0.0\n</code></pre>"},{"location":"api/interpret/#xpyrment.interpret.generate_launch_recommendation","title":"<code>generate_launch_recommendation(p_value, relative_lift, cost_threshold=0.0)</code>","text":"<p>Generates automated ship, no-ship, or inconclusive launch recommendations.</p> Source code in <code>src\\xpyrment\\interpret\\decision.py</code> <pre><code>def generate_launch_recommendation(p_value: float, relative_lift: float, cost_threshold: float = 0.0) -&gt; str:\n    \"\"\"Generates automated ship, no-ship, or inconclusive launch recommendations.\"\"\"\n    if p_value &lt; 0.05:\n        if relative_lift &gt; cost_threshold:\n            return \"SHIP: Lifts are statistically significant and exceed deployment costs.\"\n        else:\n            return \"NO-SHIP: Statistically significant but falls below economic margins.\"\n    return \"INCONCLUSIVE: No statistical evidence to assert a positive lift.\"\n</code></pre>"},{"location":"api/interpret/#xpyrment.interpret.scan_subgroups_for_hte","title":"<code>scan_subgroups_for_hte(df, treatment_col, metric_col, segment_cols)</code>","text":"<p>Scans demographics/segments to detect Heterogeneous Treatment Effects (HTE) across cohorts.</p> Source code in <code>src\\xpyrment\\interpret\\hte.py</code> <pre><code>def scan_subgroups_for_hte(df: pd.DataFrame, treatment_col: str, metric_col: str, segment_cols: list) -&gt; dict:\n    \"\"\"Scans demographics/segments to detect Heterogeneous Treatment Effects (HTE) across cohorts.\"\"\"\n    # TODO: Implement causal tree or subgroup t-test sweep\n    return {}\n</code></pre>"},{"location":"api/interpret/decision/","title":"Decision","text":""},{"location":"api/interpret/decision/#xpyrment.interpret.decision","title":"<code>xpyrment.interpret.decision</code>","text":""},{"location":"api/interpret/decision/#xpyrment.interpret.decision.generate_launch_recommendation","title":"<code>generate_launch_recommendation(p_value, relative_lift, cost_threshold=0.0)</code>","text":"<p>Generates automated ship, no-ship, or inconclusive launch recommendations.</p> Source code in <code>src\\xpyrment\\interpret\\decision.py</code> <pre><code>def generate_launch_recommendation(p_value: float, relative_lift: float, cost_threshold: float = 0.0) -&gt; str:\n    \"\"\"Generates automated ship, no-ship, or inconclusive launch recommendations.\"\"\"\n    if p_value &lt; 0.05:\n        if relative_lift &gt; cost_threshold:\n            return \"SHIP: Lifts are statistically significant and exceed deployment costs.\"\n        else:\n            return \"NO-SHIP: Statistically significant but falls below economic margins.\"\n    return \"INCONCLUSIVE: No statistical evidence to assert a positive lift.\"\n</code></pre>"},{"location":"api/interpret/effect_size/","title":"Effect Size","text":""},{"location":"api/interpret/effect_size/#xpyrment.interpret.effect_size","title":"<code>xpyrment.interpret.effect_size</code>","text":""},{"location":"api/interpret/effect_size/#xpyrment.interpret.effect_size.compute_cohens_d","title":"<code>compute_cohens_d(group_a, group_b)</code>","text":"<p>Computes standard standardized effect size using Cohen's d formula.</p> Source code in <code>src\\xpyrment\\interpret\\effect_size.py</code> <pre><code>def compute_cohens_d(group_a: np.ndarray, group_b: np.ndarray) -&gt; float:\n    \"\"\"Computes standard standardized effect size using Cohen's d formula.\"\"\"\n    mean_a, mean_b = np.mean(group_a), np.mean(group_b)\n    var_a, var_b = np.var(group_a, ddof=1), np.var(group_b, ddof=1)\n    n_a, n_b = len(group_a), len(group_b)\n\n    pooled_std = np.sqrt(((n_a - 1) * var_a + (n_b - 1) * var_b) / (n_a + n_b - 2))\n    if pooled_std &gt; 0:\n        return (mean_b - mean_a) / pooled_std\n    return 0.0\n</code></pre>"},{"location":"api/interpret/hte/","title":"HTE","text":""},{"location":"api/interpret/hte/#xpyrment.interpret.hte","title":"<code>xpyrment.interpret.hte</code>","text":""},{"location":"api/interpret/hte/#xpyrment.interpret.hte.scan_subgroups_for_hte","title":"<code>scan_subgroups_for_hte(df, treatment_col, metric_col, segment_cols)</code>","text":"<p>Scans demographics/segments to detect Heterogeneous Treatment Effects (HTE) across cohorts.</p> Source code in <code>src\\xpyrment\\interpret\\hte.py</code> <pre><code>def scan_subgroups_for_hte(df: pd.DataFrame, treatment_col: str, metric_col: str, segment_cols: list) -&gt; dict:\n    \"\"\"Scans demographics/segments to detect Heterogeneous Treatment Effects (HTE) across cohorts.\"\"\"\n    # TODO: Implement causal tree or subgroup t-test sweep\n    return {}\n</code></pre>"},{"location":"api/interpret/significance/","title":"Significance","text":""},{"location":"api/interpret/significance/#xpyrment.interpret.significance","title":"<code>xpyrment.interpret.significance</code>","text":""},{"location":"api/interpret/significance/#xpyrment.interpret.significance.check_practical_significance","title":"<code>check_practical_significance(relative_lift, min_valuable_effect)</code>","text":"<p>Verifies if the measured lift satisfies the minimal valuable business effect (MVE).</p> Source code in <code>src\\xpyrment\\interpret\\significance.py</code> <pre><code>def check_practical_significance(relative_lift: float, min_valuable_effect: float) -&gt; bool:\n    \"\"\"Verifies if the measured lift satisfies the minimal valuable business effect (MVE).\"\"\"\n    return relative_lift &gt;= min_valuable_effect\n</code></pre>"},{"location":"api/metrics/","title":"Metrics Module","text":"<p>The <code>xpyrment.metrics</code> module contains submodules and components for metrics.</p>"},{"location":"api/metrics/#xpyrment.metrics","title":"<code>xpyrment.metrics</code>","text":""},{"location":"api/metrics/#xpyrment.metrics.BaseMetric","title":"<code>BaseMetric</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for all experiment metrics.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class BaseMetric(ABC):\n    \"\"\"Abstract base class for all experiment metrics.\"\"\"\n\n    def __init__(self, name: str):\n        self.name = name\n\n    @abstractmethod\n    def calculate(\n        self, df: pd.DataFrame, treatment_col: str, control: str, treatment: str\n    ) -&gt; Dict[str, Any]:\n        \"\"\"Calculates statistics for control and treatment groups.\"\"\"\n        pass\n\n    def _calculate_p_and_ci(\n        self,\n        mean_c: float,\n        mean_t: float,\n        var_c: float,\n        var_t: float,\n        n_c: int,\n        n_t: int,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, float]:\n        \"\"\"Computes statistical p-value, confidence intervals, and power using Welch's t-test.\"\"\"\n        num = (var_c / n_c + var_t / n_t) ** 2\n        den = ((var_c / n_c) ** 2) / (n_c - 1) + ((var_t / n_t) ** 2) / (n_t - 1)\n        df = num / den if den &gt; 0 else (n_c + n_t - 2)\n\n        se_diff = np.sqrt(var_c / n_c + var_t / n_t)\n        diff = mean_t - mean_c\n\n        if se_diff &gt; 0:\n            t_stat = diff / se_diff\n            p_val = 2 * (1 - stats.t.cdf(np.abs(t_stat), df=df))\n\n            t_crit = stats.t.ppf(1 - alpha / 2, df=df)\n            ci_lower = diff - t_crit * se_diff\n            ci_upper = diff + t_crit * se_diff\n\n            ncp = np.abs(diff) / se_diff\n            t_crit_alpha = stats.t.ppf(1 - alpha / 2, df=df)\n            power = 1 - stats.t.cdf(t_crit_alpha, df=df, loc=ncp) + stats.t.cdf(-t_crit_alpha, df=df, loc=ncp)\n        else:\n            p_val = 1.0\n            ci_lower = diff\n            ci_upper = diff\n            power = 0.0\n\n        relative_lift = diff / mean_c if mean_c != 0 else 0.0\n        rel_ci_lower = ci_lower / mean_c if mean_c != 0 else 0.0\n        rel_ci_upper = ci_upper / mean_c if mean_c != 0 else 0.0\n\n        return {\n            \"p_value\": p_val,\n            \"ci_lower\": ci_lower,\n            \"ci_upper\": ci_upper,\n            \"rel_ci_lower\": rel_ci_lower,\n            \"rel_ci_upper\": rel_ci_upper,\n            \"power\": power,\n        }\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.BaseMetric.calculate","title":"<code>calculate(df, treatment_col, control, treatment)</code>  <code>abstractmethod</code>","text":"<p>Calculates statistics for control and treatment groups.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>@abstractmethod\ndef calculate(\n    self, df: pd.DataFrame, treatment_col: str, control: str, treatment: str\n) -&gt; Dict[str, Any]:\n    \"\"\"Calculates statistics for control and treatment groups.\"\"\"\n    pass\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.GuardrailMetric","title":"<code>GuardrailMetric</code>","text":"<p>Defines a guardrail metric with specific breach thresholds.</p> Source code in <code>src\\xpyrment\\metrics\\guardrails.py</code> <pre><code>class GuardrailMetric:\n    \"\"\"Defines a guardrail metric with specific breach thresholds.\"\"\"\n\n    def __init__(self, metric: BaseMetric, max_allowed_change: float = 0.01):\n        self.metric = metric\n        self.max_allowed_change = max_allowed_change\n\n    def check_breach(self, calculation_result: Dict[str, Any]) -&gt; bool:\n        \"\"\"Determines if the calculated lift breaches the guardrail thresholds.\n\n        Args:\n            calculation_result (dict): Output from metric.calculate().\n\n        Returns:\n            bool: True if there is a breach, False otherwise.\n        \"\"\"\n        lift = calculation_result.get(\"relative_lift\", 0.0)\n        # Breach occurs if metric deteriorates beyond max_allowed_change\n        return abs(lift) &gt; self.max_allowed_change\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.GuardrailMetric.check_breach","title":"<code>check_breach(calculation_result)</code>","text":"<p>Determines if the calculated lift breaches the guardrail thresholds.</p> <p>Parameters:</p> Name Type Description Default <code>calculation_result</code> <code>dict</code> <p>Output from metric.calculate().</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there is a breach, False otherwise.</p> Source code in <code>src\\xpyrment\\metrics\\guardrails.py</code> <pre><code>def check_breach(self, calculation_result: Dict[str, Any]) -&gt; bool:\n    \"\"\"Determines if the calculated lift breaches the guardrail thresholds.\n\n    Args:\n        calculation_result (dict): Output from metric.calculate().\n\n    Returns:\n        bool: True if there is a breach, False otherwise.\n    \"\"\"\n    lift = calculation_result.get(\"relative_lift\", 0.0)\n    # Breach occurs if metric deteriorates beyond max_allowed_change\n    return abs(lift) &gt; self.max_allowed_change\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.MeanMetric","title":"<code>MeanMetric</code>","text":"<p>               Bases: <code>BaseMetric</code></p> <p>A metric representing a continuous or numeric value (e.g., average revenue, sessions).</p> <p>Supports CUPED adjustment if pre-period data is provided.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class MeanMetric(BaseMetric):\n    \"\"\"A metric representing a continuous or numeric value (e.g., average revenue, sessions).\n\n    Supports CUPED adjustment if pre-period data is provided.\n    \"\"\"\n\n    def __init__(\n        self,\n        name: str,\n        value_col: str,\n        pre_period_col: Optional[str] = None,\n    ):\n        super().__init__(name)\n        self.value_col = value_col\n        self.pre_period_col = pre_period_col\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        df_clean = df.dropna(subset=[self.value_col]).copy()\n\n        c_mask = df_clean[treatment_col] == control\n        t_mask = df_clean[treatment_col] == treatment\n\n        n_c = int(np.sum(c_mask))\n        n_t = int(np.sum(t_mask))\n\n        if n_c == 0 or n_t == 0:\n            raise ValueError(f\"Control or treatment group is empty for metric {self.name}.\")\n\n        y = df_clean[self.value_col].to_numpy()\n\n        cuped_achieved = False\n        variance_reduction = 0.0\n\n        if self.pre_period_col and self.pre_period_col in df_clean.columns:\n            df_clean = df_clean.dropna(subset=[self.pre_period_col])\n            c_mask = df_clean[treatment_col] == control\n            t_mask = df_clean[treatment_col] == treatment\n            n_c = int(np.sum(c_mask))\n            n_t = int(np.sum(t_mask))\n\n            y = df_clean[self.value_col].to_numpy()\n            x = df_clean[self.pre_period_col].to_numpy()\n\n            var_x = np.var(x, ddof=1)\n            if var_x &gt; 0:\n                cov_yx = np.cov(y, x, ddof=1)[0, 1]\n                theta = cov_yx / var_x\n                mean_x_global = np.mean(x)\n\n                y_cuped = y - theta * (x - mean_x_global)\n\n                y_c = y_cuped[c_mask]\n                y_t = y_cuped[t_mask]\n\n                mean_c = float(np.mean(y_c))\n                mean_t = float(np.mean(y_t))\n\n                var_c = float(np.var(y_c, ddof=1))\n                var_t = float(np.var(y_t, ddof=1))\n\n                cuped_achieved = True\n\n                orig_var = np.var(y, ddof=1)\n                adjusted_var = np.var(y_cuped, ddof=1)\n                if orig_var &gt; 0:\n                    variance_reduction = max(0.0, (orig_var - adjusted_var) / orig_var)\n            else:\n                mean_c = float(np.mean(y[c_mask]))\n                mean_t = float(np.mean(y[t_mask]))\n                var_c = float(np.var(y[c_mask], ddof=1))\n                var_t = float(np.var(y[t_mask], ddof=1))\n        else:\n            mean_c = float(np.mean(y[c_mask]))\n            mean_t = float(np.mean(y[t_mask]))\n            var_c = float(np.var(y[c_mask], ddof=1))\n            var_t = float(np.var(y[t_mask], ddof=1))\n\n        stats_dict = self._calculate_p_and_ci(\n            mean_c=mean_c,\n            mean_t=mean_t,\n            var_c=var_c,\n            var_t=var_t,\n            n_c=n_c,\n            n_t=n_t,\n            alpha=alpha,\n        )\n\n        diff = mean_t - mean_c\n        relative_lift = diff / mean_c if mean_c != 0 else 0.0\n\n        results = {\n            \"metric_name\": self.name,\n            \"metric_type\": \"Mean\",\n            \"control_mean\": mean_c,\n            \"treatment_mean\": mean_t,\n            \"control_var\": var_c,\n            \"treatment_var\": var_t,\n            \"control_n\": n_c,\n            \"treatment_n\": n_t,\n            \"absolute_difference\": diff,\n            \"relative_lift\": relative_lift,\n            \"cuped_applied\": cuped_achieved,\n            \"variance_reduction\": variance_reduction,\n            **stats_dict,\n        }\n\n        return results\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.ProportionMetric","title":"<code>ProportionMetric</code>","text":"<p>               Bases: <code>MeanMetric</code></p> <p>A metric representing a binary/proportion rate (e.g., conversion rate).</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class ProportionMetric(MeanMetric):\n    \"\"\"A metric representing a binary/proportion rate (e.g., conversion rate).\"\"\"\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        res = super().calculate(df, treatment_col, control, treatment, alpha)\n        res[\"metric_type\"] = \"Proportion\"\n        return res\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.RatioMetric","title":"<code>RatioMetric</code>","text":"<p>               Bases: <code>BaseMetric</code></p> <p>A metric calculated as the ratio: sum(numerator) / sum(denominator) (e.g., CTR).</p> <p>Uses Delta Method for variance estimation. Supports Ratio-level CUPED.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class RatioMetric(BaseMetric):\n    \"\"\"A metric calculated as the ratio: sum(numerator) / sum(denominator) (e.g., CTR).\n\n    Uses Delta Method for variance estimation. Supports Ratio-level CUPED.\n    \"\"\"\n\n    def __init__(\n        self,\n        name: str,\n        numerator_col: str,\n        denominator_col: str,\n        pre_numerator_col: Optional[str] = None,\n        pre_denominator_col: Optional[str] = None,\n    ):\n        super().__init__(name)\n        self.numerator_col = numerator_col\n        self.denominator_col = denominator_col\n        self.pre_numerator_col = pre_numerator_col\n        self.pre_denominator_col = pre_denominator_col\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        df_clean = df.dropna(subset=[self.numerator_col, self.denominator_col]).copy()\n        df_clean = df_clean[df_clean[self.denominator_col] &gt; 0]\n\n        c_mask = df_clean[treatment_col] == control\n        t_mask = df_clean[treatment_col] == treatment\n\n        n_c = int(np.sum(c_mask))\n        n_t = int(np.sum(t_mask))\n\n        if n_c == 0 or n_t == 0:\n            raise ValueError(f\"Control or treatment group is empty for ratio metric {self.name}.\")\n\n        cuped_achieved = False\n        variance_reduction = 0.0\n\n        num = df_clean[self.numerator_col].to_numpy()\n        den = df_clean[self.denominator_col].to_numpy()\n\n        if (\n            self.pre_numerator_col\n            and self.pre_denominator_col\n            and self.pre_numerator_col in df_clean.columns\n            and self.pre_denominator_col in df_clean.columns\n        ):\n            df_clean = df_clean.dropna(subset=[self.pre_numerator_col, self.pre_denominator_col])\n            df_clean = df_clean[df_clean[self.pre_denominator_col] &gt; 0]\n\n            c_mask = df_clean[treatment_col] == control\n            t_mask = df_clean[treatment_col] == treatment\n            n_c = int(np.sum(c_mask))\n            n_t = int(np.sum(t_mask))\n\n            num = df_clean[self.numerator_col].to_numpy()\n            den = df_clean[self.denominator_col].to_numpy()\n            pre_num = df_clean[self.pre_numerator_col].to_numpy()\n            pre_den = df_clean[self.pre_denominator_col].to_numpy()\n\n            var_pre_num = np.var(pre_num, ddof=1)\n            var_pre_den = np.var(pre_den, ddof=1)\n\n            if var_pre_num &gt; 0 and var_pre_den &gt; 0:\n                cov_num = np.cov(num, pre_num, ddof=1)[0, 1]\n                theta_num = cov_num / var_pre_num\n                mean_pre_num_global = np.mean(pre_num)\n                num_cuped = num - theta_num * (pre_num - mean_pre_num_global)\n\n                cov_den = np.cov(den, pre_den, ddof=1)[0, 1]\n                theta_den = cov_den / var_pre_den\n                mean_pre_den_global = np.mean(pre_den)\n                den_cuped = den - theta_den * (pre_den - mean_pre_den_global)\n\n                num_c, num_t = num_cuped[c_mask], num_cuped[t_mask]\n                den_c, den_t = den_cuped[c_mask], den_cuped[t_mask]\n\n                mean_num_c, mean_num_t = np.mean(num_c), np.mean(num_t)\n                mean_den_c, mean_den_t = np.mean(den_c), np.mean(den_t)\n\n                ratio_c = mean_num_c / mean_den_c\n                ratio_t = mean_num_t / mean_den_t\n\n                var_num_c = np.var(num_c, ddof=1)\n                var_den_c = np.var(den_c, ddof=1)\n                cov_num_den_c = np.cov(num_c, den_c, ddof=1)[0, 1]\n\n                var_ratio_c = (1 / (mean_den_c**2)) * (\n                    var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n                )\n\n                var_num_t = np.var(num_t, ddof=1)\n                var_den_t = np.var(den_t, ddof=1)\n                cov_num_den_t = np.cov(num_t, den_t, ddof=1)[0, 1]\n\n                var_ratio_t = (1 / (mean_den_t**2)) * (\n                    var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n                )\n\n                cuped_achieved = True\n\n                orig_ratio_global = np.mean(num) / np.mean(den)\n                orig_var_ratio = (1 / (np.mean(den) ** 2)) * (\n                    np.var(num, ddof=1)\n                    + (orig_ratio_global**2) * np.var(den, ddof=1)\n                    - 2 * orig_ratio_global * np.cov(num, den, ddof=1)[0, 1]\n                )\n\n                adj_ratio_global = np.mean(num_cuped) / np.mean(den_cuped)\n                adj_var_ratio = (1 / (np.mean(den_cuped) ** 2)) * (\n                    np.var(num_cuped, ddof=1)\n                    + (adj_ratio_global**2) * np.var(den_cuped, ddof=1)\n                    - 2 * adj_ratio_global * np.cov(num_cuped, den_cuped, ddof=1)[0, 1]\n                )\n\n                if orig_var_ratio &gt; 0:\n                    variance_reduction = max(0.0, (orig_var_ratio - adj_var_ratio) / orig_var_ratio)\n            else:\n                mean_num_c, mean_num_t = np.mean(num[c_mask]), np.mean(num[t_mask])\n                mean_den_c, mean_den_t = np.mean(den[c_mask]), np.mean(den[t_mask])\n                ratio_c = mean_num_c / mean_den_c\n                ratio_t = mean_num_t / mean_den_t\n\n                var_num_c = np.var(num[c_mask], ddof=1)\n                var_den_c = np.var(den[c_mask], ddof=1)\n                cov_num_den_c = np.cov(num[c_mask], den[c_mask], ddof=1)[0, 1]\n                var_ratio_c = (1 / (mean_den_c**2)) * (\n                    var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n                )\n\n                var_num_t = np.var(num[t_mask], ddof=1)\n                var_den_t = np.var(den[t_mask], ddof=1)\n                cov_num_den_t = np.cov(num[t_mask], den[t_mask], ddof=1)[0, 1]\n                var_ratio_t = (1 / (mean_den_t**2)) * (\n                    var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n                )\n        else:\n            mean_num_c, mean_num_t = np.mean(num[c_mask]), np.mean(num[t_mask])\n            mean_den_c, mean_den_t = np.mean(den[c_mask]), np.mean(den[t_mask])\n            ratio_c = mean_num_c / mean_den_c\n            ratio_t = mean_num_t / mean_den_t\n\n            var_num_c = np.var(num[c_mask], ddof=1)\n            var_den_c = np.var(den[c_mask], ddof=1)\n            cov_num_den_c = np.cov(num[c_mask], den[c_mask], ddof=1)[0, 1]\n            var_ratio_c = (1 / (mean_den_c**2)) * (\n                var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n            )\n\n            var_num_t = np.var(num[t_mask], ddof=1)\n            var_den_t = np.var(den[t_mask], ddof=1)\n            cov_num_den_t = np.cov(num[t_mask], den[t_mask], ddof=1)[0, 1]\n            var_ratio_t = (1 / (mean_den_t**2)) * (\n                var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n            )\n\n        stats_dict = self._calculate_p_and_ci(\n            mean_c=ratio_c,\n            mean_t=ratio_t,\n            var_c=var_ratio_c,\n            var_t=var_ratio_t,\n            n_c=n_c,\n            n_t=n_t,\n            alpha=alpha,\n        )\n\n        diff = ratio_t - ratio_c\n        relative_lift = diff / ratio_c if ratio_c != 0 else 0.0\n\n        results = {\n            \"metric_name\": self.name,\n            \"metric_type\": \"Ratio\",\n            \"control_mean\": ratio_c,\n            \"treatment_mean\": ratio_t,\n            \"control_var\": var_ratio_c,\n            \"treatment_var\": var_ratio_t,\n            \"control_n\": n_c,\n            \"treatment_n\": n_t,\n            \"absolute_difference\": diff,\n            \"relative_lift\": relative_lift,\n            \"cuped_applied\": cuped_achieved,\n            \"variance_reduction\": variance_reduction,\n            **stats_dict,\n        }\n\n        return results\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.delta_normalization","title":"<code>delta_normalization(df, col)</code>","text":"<p>Normalizes metrics using the delta expansion (Stub/Scaffolding).</p> Source code in <code>src\\xpyrment\\metrics\\transformations.py</code> <pre><code>def delta_normalization(df: pd.DataFrame, col: str) -&gt; pd.Series:\n    \"\"\"Normalizes metrics using the delta expansion (Stub/Scaffolding).\"\"\"\n    # TODO: Implement full delta normalization\n    return df[col]\n</code></pre>"},{"location":"api/metrics/#xpyrment.metrics.log_transform","title":"<code>log_transform(df, col)</code>","text":"<p>Transforms continuous metrics using log transformation for skewed distributions.</p> <p>y_transformed = log(y + 1)</p> Source code in <code>src\\xpyrment\\metrics\\transformations.py</code> <pre><code>def log_transform(df: pd.DataFrame, col: str) -&gt; pd.Series:\n    \"\"\"Transforms continuous metrics using log transformation for skewed distributions.\n\n    y_transformed = log(y + 1)\n    \"\"\"\n    return np.log1p(df[col])\n</code></pre>"},{"location":"api/metrics/guardrails/","title":"Guardrails","text":""},{"location":"api/metrics/guardrails/#xpyrment.metrics.guardrails","title":"<code>xpyrment.metrics.guardrails</code>","text":""},{"location":"api/metrics/guardrails/#xpyrment.metrics.guardrails.GuardrailMetric","title":"<code>GuardrailMetric</code>","text":"<p>Defines a guardrail metric with specific breach thresholds.</p> Source code in <code>src\\xpyrment\\metrics\\guardrails.py</code> <pre><code>class GuardrailMetric:\n    \"\"\"Defines a guardrail metric with specific breach thresholds.\"\"\"\n\n    def __init__(self, metric: BaseMetric, max_allowed_change: float = 0.01):\n        self.metric = metric\n        self.max_allowed_change = max_allowed_change\n\n    def check_breach(self, calculation_result: Dict[str, Any]) -&gt; bool:\n        \"\"\"Determines if the calculated lift breaches the guardrail thresholds.\n\n        Args:\n            calculation_result (dict): Output from metric.calculate().\n\n        Returns:\n            bool: True if there is a breach, False otherwise.\n        \"\"\"\n        lift = calculation_result.get(\"relative_lift\", 0.0)\n        # Breach occurs if metric deteriorates beyond max_allowed_change\n        return abs(lift) &gt; self.max_allowed_change\n</code></pre>"},{"location":"api/metrics/guardrails/#xpyrment.metrics.guardrails.GuardrailMetric.check_breach","title":"<code>check_breach(calculation_result)</code>","text":"<p>Determines if the calculated lift breaches the guardrail thresholds.</p> <p>Parameters:</p> Name Type Description Default <code>calculation_result</code> <code>dict</code> <p>Output from metric.calculate().</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there is a breach, False otherwise.</p> Source code in <code>src\\xpyrment\\metrics\\guardrails.py</code> <pre><code>def check_breach(self, calculation_result: Dict[str, Any]) -&gt; bool:\n    \"\"\"Determines if the calculated lift breaches the guardrail thresholds.\n\n    Args:\n        calculation_result (dict): Output from metric.calculate().\n\n    Returns:\n        bool: True if there is a breach, False otherwise.\n    \"\"\"\n    lift = calculation_result.get(\"relative_lift\", 0.0)\n    # Breach occurs if metric deteriorates beyond max_allowed_change\n    return abs(lift) &gt; self.max_allowed_change\n</code></pre>"},{"location":"api/metrics/taxonomy/","title":"Taxonomy","text":""},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy","title":"<code>xpyrment.metrics.taxonomy</code>","text":""},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy.BaseMetric","title":"<code>BaseMetric</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for all experiment metrics.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class BaseMetric(ABC):\n    \"\"\"Abstract base class for all experiment metrics.\"\"\"\n\n    def __init__(self, name: str):\n        self.name = name\n\n    @abstractmethod\n    def calculate(\n        self, df: pd.DataFrame, treatment_col: str, control: str, treatment: str\n    ) -&gt; Dict[str, Any]:\n        \"\"\"Calculates statistics for control and treatment groups.\"\"\"\n        pass\n\n    def _calculate_p_and_ci(\n        self,\n        mean_c: float,\n        mean_t: float,\n        var_c: float,\n        var_t: float,\n        n_c: int,\n        n_t: int,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, float]:\n        \"\"\"Computes statistical p-value, confidence intervals, and power using Welch's t-test.\"\"\"\n        num = (var_c / n_c + var_t / n_t) ** 2\n        den = ((var_c / n_c) ** 2) / (n_c - 1) + ((var_t / n_t) ** 2) / (n_t - 1)\n        df = num / den if den &gt; 0 else (n_c + n_t - 2)\n\n        se_diff = np.sqrt(var_c / n_c + var_t / n_t)\n        diff = mean_t - mean_c\n\n        if se_diff &gt; 0:\n            t_stat = diff / se_diff\n            p_val = 2 * (1 - stats.t.cdf(np.abs(t_stat), df=df))\n\n            t_crit = stats.t.ppf(1 - alpha / 2, df=df)\n            ci_lower = diff - t_crit * se_diff\n            ci_upper = diff + t_crit * se_diff\n\n            ncp = np.abs(diff) / se_diff\n            t_crit_alpha = stats.t.ppf(1 - alpha / 2, df=df)\n            power = 1 - stats.t.cdf(t_crit_alpha, df=df, loc=ncp) + stats.t.cdf(-t_crit_alpha, df=df, loc=ncp)\n        else:\n            p_val = 1.0\n            ci_lower = diff\n            ci_upper = diff\n            power = 0.0\n\n        relative_lift = diff / mean_c if mean_c != 0 else 0.0\n        rel_ci_lower = ci_lower / mean_c if mean_c != 0 else 0.0\n        rel_ci_upper = ci_upper / mean_c if mean_c != 0 else 0.0\n\n        return {\n            \"p_value\": p_val,\n            \"ci_lower\": ci_lower,\n            \"ci_upper\": ci_upper,\n            \"rel_ci_lower\": rel_ci_lower,\n            \"rel_ci_upper\": rel_ci_upper,\n            \"power\": power,\n        }\n</code></pre>"},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy.BaseMetric.calculate","title":"<code>calculate(df, treatment_col, control, treatment)</code>  <code>abstractmethod</code>","text":"<p>Calculates statistics for control and treatment groups.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>@abstractmethod\ndef calculate(\n    self, df: pd.DataFrame, treatment_col: str, control: str, treatment: str\n) -&gt; Dict[str, Any]:\n    \"\"\"Calculates statistics for control and treatment groups.\"\"\"\n    pass\n</code></pre>"},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy.MeanMetric","title":"<code>MeanMetric</code>","text":"<p>               Bases: <code>BaseMetric</code></p> <p>A metric representing a continuous or numeric value (e.g., average revenue, sessions).</p> <p>Supports CUPED adjustment if pre-period data is provided.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class MeanMetric(BaseMetric):\n    \"\"\"A metric representing a continuous or numeric value (e.g., average revenue, sessions).\n\n    Supports CUPED adjustment if pre-period data is provided.\n    \"\"\"\n\n    def __init__(\n        self,\n        name: str,\n        value_col: str,\n        pre_period_col: Optional[str] = None,\n    ):\n        super().__init__(name)\n        self.value_col = value_col\n        self.pre_period_col = pre_period_col\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        df_clean = df.dropna(subset=[self.value_col]).copy()\n\n        c_mask = df_clean[treatment_col] == control\n        t_mask = df_clean[treatment_col] == treatment\n\n        n_c = int(np.sum(c_mask))\n        n_t = int(np.sum(t_mask))\n\n        if n_c == 0 or n_t == 0:\n            raise ValueError(f\"Control or treatment group is empty for metric {self.name}.\")\n\n        y = df_clean[self.value_col].to_numpy()\n\n        cuped_achieved = False\n        variance_reduction = 0.0\n\n        if self.pre_period_col and self.pre_period_col in df_clean.columns:\n            df_clean = df_clean.dropna(subset=[self.pre_period_col])\n            c_mask = df_clean[treatment_col] == control\n            t_mask = df_clean[treatment_col] == treatment\n            n_c = int(np.sum(c_mask))\n            n_t = int(np.sum(t_mask))\n\n            y = df_clean[self.value_col].to_numpy()\n            x = df_clean[self.pre_period_col].to_numpy()\n\n            var_x = np.var(x, ddof=1)\n            if var_x &gt; 0:\n                cov_yx = np.cov(y, x, ddof=1)[0, 1]\n                theta = cov_yx / var_x\n                mean_x_global = np.mean(x)\n\n                y_cuped = y - theta * (x - mean_x_global)\n\n                y_c = y_cuped[c_mask]\n                y_t = y_cuped[t_mask]\n\n                mean_c = float(np.mean(y_c))\n                mean_t = float(np.mean(y_t))\n\n                var_c = float(np.var(y_c, ddof=1))\n                var_t = float(np.var(y_t, ddof=1))\n\n                cuped_achieved = True\n\n                orig_var = np.var(y, ddof=1)\n                adjusted_var = np.var(y_cuped, ddof=1)\n                if orig_var &gt; 0:\n                    variance_reduction = max(0.0, (orig_var - adjusted_var) / orig_var)\n            else:\n                mean_c = float(np.mean(y[c_mask]))\n                mean_t = float(np.mean(y[t_mask]))\n                var_c = float(np.var(y[c_mask], ddof=1))\n                var_t = float(np.var(y[t_mask], ddof=1))\n        else:\n            mean_c = float(np.mean(y[c_mask]))\n            mean_t = float(np.mean(y[t_mask]))\n            var_c = float(np.var(y[c_mask], ddof=1))\n            var_t = float(np.var(y[t_mask], ddof=1))\n\n        stats_dict = self._calculate_p_and_ci(\n            mean_c=mean_c,\n            mean_t=mean_t,\n            var_c=var_c,\n            var_t=var_t,\n            n_c=n_c,\n            n_t=n_t,\n            alpha=alpha,\n        )\n\n        diff = mean_t - mean_c\n        relative_lift = diff / mean_c if mean_c != 0 else 0.0\n\n        results = {\n            \"metric_name\": self.name,\n            \"metric_type\": \"Mean\",\n            \"control_mean\": mean_c,\n            \"treatment_mean\": mean_t,\n            \"control_var\": var_c,\n            \"treatment_var\": var_t,\n            \"control_n\": n_c,\n            \"treatment_n\": n_t,\n            \"absolute_difference\": diff,\n            \"relative_lift\": relative_lift,\n            \"cuped_applied\": cuped_achieved,\n            \"variance_reduction\": variance_reduction,\n            **stats_dict,\n        }\n\n        return results\n</code></pre>"},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy.ProportionMetric","title":"<code>ProportionMetric</code>","text":"<p>               Bases: <code>MeanMetric</code></p> <p>A metric representing a binary/proportion rate (e.g., conversion rate).</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class ProportionMetric(MeanMetric):\n    \"\"\"A metric representing a binary/proportion rate (e.g., conversion rate).\"\"\"\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        res = super().calculate(df, treatment_col, control, treatment, alpha)\n        res[\"metric_type\"] = \"Proportion\"\n        return res\n</code></pre>"},{"location":"api/metrics/taxonomy/#xpyrment.metrics.taxonomy.RatioMetric","title":"<code>RatioMetric</code>","text":"<p>               Bases: <code>BaseMetric</code></p> <p>A metric calculated as the ratio: sum(numerator) / sum(denominator) (e.g., CTR).</p> <p>Uses Delta Method for variance estimation. Supports Ratio-level CUPED.</p> Source code in <code>src\\xpyrment\\metrics\\taxonomy.py</code> <pre><code>class RatioMetric(BaseMetric):\n    \"\"\"A metric calculated as the ratio: sum(numerator) / sum(denominator) (e.g., CTR).\n\n    Uses Delta Method for variance estimation. Supports Ratio-level CUPED.\n    \"\"\"\n\n    def __init__(\n        self,\n        name: str,\n        numerator_col: str,\n        denominator_col: str,\n        pre_numerator_col: Optional[str] = None,\n        pre_denominator_col: Optional[str] = None,\n    ):\n        super().__init__(name)\n        self.numerator_col = numerator_col\n        self.denominator_col = denominator_col\n        self.pre_numerator_col = pre_numerator_col\n        self.pre_denominator_col = pre_denominator_col\n\n    def calculate(\n        self,\n        df: pd.DataFrame,\n        treatment_col: str,\n        control: str,\n        treatment: str,\n        alpha: float = 0.05,\n    ) -&gt; Dict[str, Any]:\n        df_clean = df.dropna(subset=[self.numerator_col, self.denominator_col]).copy()\n        df_clean = df_clean[df_clean[self.denominator_col] &gt; 0]\n\n        c_mask = df_clean[treatment_col] == control\n        t_mask = df_clean[treatment_col] == treatment\n\n        n_c = int(np.sum(c_mask))\n        n_t = int(np.sum(t_mask))\n\n        if n_c == 0 or n_t == 0:\n            raise ValueError(f\"Control or treatment group is empty for ratio metric {self.name}.\")\n\n        cuped_achieved = False\n        variance_reduction = 0.0\n\n        num = df_clean[self.numerator_col].to_numpy()\n        den = df_clean[self.denominator_col].to_numpy()\n\n        if (\n            self.pre_numerator_col\n            and self.pre_denominator_col\n            and self.pre_numerator_col in df_clean.columns\n            and self.pre_denominator_col in df_clean.columns\n        ):\n            df_clean = df_clean.dropna(subset=[self.pre_numerator_col, self.pre_denominator_col])\n            df_clean = df_clean[df_clean[self.pre_denominator_col] &gt; 0]\n\n            c_mask = df_clean[treatment_col] == control\n            t_mask = df_clean[treatment_col] == treatment\n            n_c = int(np.sum(c_mask))\n            n_t = int(np.sum(t_mask))\n\n            num = df_clean[self.numerator_col].to_numpy()\n            den = df_clean[self.denominator_col].to_numpy()\n            pre_num = df_clean[self.pre_numerator_col].to_numpy()\n            pre_den = df_clean[self.pre_denominator_col].to_numpy()\n\n            var_pre_num = np.var(pre_num, ddof=1)\n            var_pre_den = np.var(pre_den, ddof=1)\n\n            if var_pre_num &gt; 0 and var_pre_den &gt; 0:\n                cov_num = np.cov(num, pre_num, ddof=1)[0, 1]\n                theta_num = cov_num / var_pre_num\n                mean_pre_num_global = np.mean(pre_num)\n                num_cuped = num - theta_num * (pre_num - mean_pre_num_global)\n\n                cov_den = np.cov(den, pre_den, ddof=1)[0, 1]\n                theta_den = cov_den / var_pre_den\n                mean_pre_den_global = np.mean(pre_den)\n                den_cuped = den - theta_den * (pre_den - mean_pre_den_global)\n\n                num_c, num_t = num_cuped[c_mask], num_cuped[t_mask]\n                den_c, den_t = den_cuped[c_mask], den_cuped[t_mask]\n\n                mean_num_c, mean_num_t = np.mean(num_c), np.mean(num_t)\n                mean_den_c, mean_den_t = np.mean(den_c), np.mean(den_t)\n\n                ratio_c = mean_num_c / mean_den_c\n                ratio_t = mean_num_t / mean_den_t\n\n                var_num_c = np.var(num_c, ddof=1)\n                var_den_c = np.var(den_c, ddof=1)\n                cov_num_den_c = np.cov(num_c, den_c, ddof=1)[0, 1]\n\n                var_ratio_c = (1 / (mean_den_c**2)) * (\n                    var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n                )\n\n                var_num_t = np.var(num_t, ddof=1)\n                var_den_t = np.var(den_t, ddof=1)\n                cov_num_den_t = np.cov(num_t, den_t, ddof=1)[0, 1]\n\n                var_ratio_t = (1 / (mean_den_t**2)) * (\n                    var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n                )\n\n                cuped_achieved = True\n\n                orig_ratio_global = np.mean(num) / np.mean(den)\n                orig_var_ratio = (1 / (np.mean(den) ** 2)) * (\n                    np.var(num, ddof=1)\n                    + (orig_ratio_global**2) * np.var(den, ddof=1)\n                    - 2 * orig_ratio_global * np.cov(num, den, ddof=1)[0, 1]\n                )\n\n                adj_ratio_global = np.mean(num_cuped) / np.mean(den_cuped)\n                adj_var_ratio = (1 / (np.mean(den_cuped) ** 2)) * (\n                    np.var(num_cuped, ddof=1)\n                    + (adj_ratio_global**2) * np.var(den_cuped, ddof=1)\n                    - 2 * adj_ratio_global * np.cov(num_cuped, den_cuped, ddof=1)[0, 1]\n                )\n\n                if orig_var_ratio &gt; 0:\n                    variance_reduction = max(0.0, (orig_var_ratio - adj_var_ratio) / orig_var_ratio)\n            else:\n                mean_num_c, mean_num_t = np.mean(num[c_mask]), np.mean(num[t_mask])\n                mean_den_c, mean_den_t = np.mean(den[c_mask]), np.mean(den[t_mask])\n                ratio_c = mean_num_c / mean_den_c\n                ratio_t = mean_num_t / mean_den_t\n\n                var_num_c = np.var(num[c_mask], ddof=1)\n                var_den_c = np.var(den[c_mask], ddof=1)\n                cov_num_den_c = np.cov(num[c_mask], den[c_mask], ddof=1)[0, 1]\n                var_ratio_c = (1 / (mean_den_c**2)) * (\n                    var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n                )\n\n                var_num_t = np.var(num[t_mask], ddof=1)\n                var_den_t = np.var(den[t_mask], ddof=1)\n                cov_num_den_t = np.cov(num[t_mask], den[t_mask], ddof=1)[0, 1]\n                var_ratio_t = (1 / (mean_den_t**2)) * (\n                    var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n                )\n        else:\n            mean_num_c, mean_num_t = np.mean(num[c_mask]), np.mean(num[t_mask])\n            mean_den_c, mean_den_t = np.mean(den[c_mask]), np.mean(den[t_mask])\n            ratio_c = mean_num_c / mean_den_c\n            ratio_t = mean_num_t / mean_den_t\n\n            var_num_c = np.var(num[c_mask], ddof=1)\n            var_den_c = np.var(den[c_mask], ddof=1)\n            cov_num_den_c = np.cov(num[c_mask], den[c_mask], ddof=1)[0, 1]\n            var_ratio_c = (1 / (mean_den_c**2)) * (\n                var_num_c + (ratio_c**2) * var_den_c - 2 * ratio_c * cov_num_den_c\n            )\n\n            var_num_t = np.var(num[t_mask], ddof=1)\n            var_den_t = np.var(den[t_mask], ddof=1)\n            cov_num_den_t = np.cov(num[t_mask], den[t_mask], ddof=1)[0, 1]\n            var_ratio_t = (1 / (mean_den_t**2)) * (\n                var_num_t + (ratio_t**2) * var_den_t - 2 * ratio_t * cov_num_den_t\n            )\n\n        stats_dict = self._calculate_p_and_ci(\n            mean_c=ratio_c,\n            mean_t=ratio_t,\n            var_c=var_ratio_c,\n            var_t=var_ratio_t,\n            n_c=n_c,\n            n_t=n_t,\n            alpha=alpha,\n        )\n\n        diff = ratio_t - ratio_c\n        relative_lift = diff / ratio_c if ratio_c != 0 else 0.0\n\n        results = {\n            \"metric_name\": self.name,\n            \"metric_type\": \"Ratio\",\n            \"control_mean\": ratio_c,\n            \"treatment_mean\": ratio_t,\n            \"control_var\": var_ratio_c,\n            \"treatment_var\": var_ratio_t,\n            \"control_n\": n_c,\n            \"treatment_n\": n_t,\n            \"absolute_difference\": diff,\n            \"relative_lift\": relative_lift,\n            \"cuped_applied\": cuped_achieved,\n            \"variance_reduction\": variance_reduction,\n            **stats_dict,\n        }\n\n        return results\n</code></pre>"},{"location":"api/metrics/transformations/","title":"Transformations","text":""},{"location":"api/metrics/transformations/#xpyrment.metrics.transformations","title":"<code>xpyrment.metrics.transformations</code>","text":""},{"location":"api/metrics/transformations/#xpyrment.metrics.transformations.delta_normalization","title":"<code>delta_normalization(df, col)</code>","text":"<p>Normalizes metrics using the delta expansion (Stub/Scaffolding).</p> Source code in <code>src\\xpyrment\\metrics\\transformations.py</code> <pre><code>def delta_normalization(df: pd.DataFrame, col: str) -&gt; pd.Series:\n    \"\"\"Normalizes metrics using the delta expansion (Stub/Scaffolding).\"\"\"\n    # TODO: Implement full delta normalization\n    return df[col]\n</code></pre>"},{"location":"api/metrics/transformations/#xpyrment.metrics.transformations.log_transform","title":"<code>log_transform(df, col)</code>","text":"<p>Transforms continuous metrics using log transformation for skewed distributions.</p> <p>y_transformed = log(y + 1)</p> Source code in <code>src\\xpyrment\\metrics\\transformations.py</code> <pre><code>def log_transform(df: pd.DataFrame, col: str) -&gt; pd.Series:\n    \"\"\"Transforms continuous metrics using log transformation for skewed distributions.\n\n    y_transformed = log(y + 1)\n    \"\"\"\n    return np.log1p(df[col])\n</code></pre>"},{"location":"api/plan/","title":"Plan Module","text":"<p>The <code>xpyrment.plan</code> module contains submodules and components for plan.</p>"},{"location":"api/plan/#xpyrment.plan","title":"<code>xpyrment.plan</code>","text":""},{"location":"api/plan/#xpyrment.plan.ExperimentDesignResult","title":"<code>ExperimentDesignResult</code>","text":"<p>Class to hold and format experiment design and power analysis results.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>class ExperimentDesignResult:\n    \"\"\"Class to hold and format experiment design and power analysis results.\"\"\"\n\n    def __init__(self, details: Dict[str, Any]):\n        self.details = details\n\n    def summary(self) -&gt; Dict[str, list]:\n        \"\"\"Returns a summary of the experiment design parameters.\"\"\"\n        summary = {\n            \"Parameter\": [\n                \"Metric Type\",\n                \"Baseline Value\",\n                \"Target MDE (Absolute)\",\n                \"Target MDE (Relative)\",\n                \"Significance Level (Alpha)\",\n                \"Statistical Power (1-Beta)\",\n                \"Sample Size Per Variant\",\n                \"Total Sample Size Required\",\n            ],\n            \"Value\": [\n                self.details[\"metric_type\"].capitalize(),\n                f\"{self.details['baseline_value']:.4f}\",\n                f\"{self.details['mde_absolute']:.4f}\",\n                f\"{self.details['mde_relative']:.2%}\",\n                f\"{self.details['alpha']:.2%}\",\n                f\"{self.details['power']:.2%}\",\n                f\"{int(np.ceil(self.details['sample_size_per_variant'])):,}\",\n                f\"{int(np.ceil(self.details['total_sample_size'])):,}\",\n            ],\n        }\n\n        if self.details.get(\"pre_post_correlation\"):\n            corr = self.details[\"pre_post_correlation\"]\n            reduced_size = self.details[\"cuped_sample_size_per_variant\"]\n            summary[\"Parameter\"].extend([\n                \"Pre-Post Correlation\",\n                \"CUPED Sample Size Per Variant\",\n                \"CUPED Total Sample Size\",\n                \"CUPED Sample Size Savings\",\n            ])\n            summary[\"Value\"].extend([\n                f\"{corr:.2f}\",\n                f\"{int(np.ceil(reduced_size)):,}\",\n                f\"{int(np.ceil(reduced_size * 2)):,}\",\n                f\"{self.details['cuped_savings']:.1%}\",\n            ])\n\n        if self.details.get(\"daily_traffic\"):\n            summary[\"Parameter\"].extend([\n                \"Daily Traffic\",\n                \"Estimated Duration (Standard)\",\n            ])\n            summary[\"Value\"].extend([\n                f\"{int(self.details['daily_traffic']):,}/day\",\n                f\"{self.details['duration_days_standard']:.1f} days\",\n            ])\n            if self.details.get(\"pre_post_correlation\"):\n                summary[\"Parameter\"].append(\"Estimated Duration (CUPED)\")\n                summary[\"Value\"].append(f\"{self.details['duration_days_cuped']:.1f} days\")\n\n        return summary\n\n    def __repr__(self) -&gt; str:\n        s = \"=========================================\\n\"\n        s += \"       Experiment Design Summary        \\n\"\n        s += \"=========================================\\n\"\n        summary_dict = self.summary()\n        for param, val in zip(summary_dict[\"Parameter\"], summary_dict[\"Value\"]):\n            s += f\"{param:&lt;30}: {val}\\n\"\n        s += \"=========================================\\n\"\n        return s\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.ExperimentDesignResult.summary","title":"<code>summary()</code>","text":"<p>Returns a summary of the experiment design parameters.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def summary(self) -&gt; Dict[str, list]:\n    \"\"\"Returns a summary of the experiment design parameters.\"\"\"\n    summary = {\n        \"Parameter\": [\n            \"Metric Type\",\n            \"Baseline Value\",\n            \"Target MDE (Absolute)\",\n            \"Target MDE (Relative)\",\n            \"Significance Level (Alpha)\",\n            \"Statistical Power (1-Beta)\",\n            \"Sample Size Per Variant\",\n            \"Total Sample Size Required\",\n        ],\n        \"Value\": [\n            self.details[\"metric_type\"].capitalize(),\n            f\"{self.details['baseline_value']:.4f}\",\n            f\"{self.details['mde_absolute']:.4f}\",\n            f\"{self.details['mde_relative']:.2%}\",\n            f\"{self.details['alpha']:.2%}\",\n            f\"{self.details['power']:.2%}\",\n            f\"{int(np.ceil(self.details['sample_size_per_variant'])):,}\",\n            f\"{int(np.ceil(self.details['total_sample_size'])):,}\",\n        ],\n    }\n\n    if self.details.get(\"pre_post_correlation\"):\n        corr = self.details[\"pre_post_correlation\"]\n        reduced_size = self.details[\"cuped_sample_size_per_variant\"]\n        summary[\"Parameter\"].extend([\n            \"Pre-Post Correlation\",\n            \"CUPED Sample Size Per Variant\",\n            \"CUPED Total Sample Size\",\n            \"CUPED Sample Size Savings\",\n        ])\n        summary[\"Value\"].extend([\n            f\"{corr:.2f}\",\n            f\"{int(np.ceil(reduced_size)):,}\",\n            f\"{int(np.ceil(reduced_size * 2)):,}\",\n            f\"{self.details['cuped_savings']:.1%}\",\n        ])\n\n    if self.details.get(\"daily_traffic\"):\n        summary[\"Parameter\"].extend([\n            \"Daily Traffic\",\n            \"Estimated Duration (Standard)\",\n        ])\n        summary[\"Value\"].extend([\n            f\"{int(self.details['daily_traffic']):,}/day\",\n            f\"{self.details['duration_days_standard']:.1f} days\",\n        ])\n        if self.details.get(\"pre_post_correlation\"):\n            summary[\"Parameter\"].append(\"Estimated Duration (CUPED)\")\n            summary[\"Value\"].append(f\"{self.details['duration_days_cuped']:.1f} days\")\n\n    return summary\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.HypothesisSpec","title":"<code>HypothesisSpec</code>","text":"<p>Specifies the hypothesis under test and binds it to a primary metric.</p> Source code in <code>src\\xpyrment\\plan\\hypothesis.py</code> <pre><code>class HypothesisSpec:\n    \"\"\"Specifies the hypothesis under test and binds it to a primary metric.\"\"\"\n\n    def __init__(\n        self,\n        primary_metric: BaseMetric,\n        direction: Literal[\"two-sided\", \"greater\", \"less\"] = \"two-sided\",\n        description: str = \"\",\n    ):\n        self.primary_metric = primary_metric\n        self.direction = direction\n        self.description = description\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.PreregistrationCard","title":"<code>PreregistrationCard</code>","text":"<p>Represents an immutable spec card registered prior to running the experiment.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>class PreregistrationCard:\n    \"\"\"Represents an immutable spec card registered prior to running the experiment.\"\"\"\n\n    def __init__(self, experiment_id: str, spec: Dict[str, Any]):\n        self.experiment_id = experiment_id\n        self.spec = spec\n        self._registry = ExperimentRegistry()\n        self.hash_signature = self._registry.register_spec(experiment_id, spec)\n\n    def verify(self, current_spec: Dict[str, Any]) -&gt; bool:\n        \"\"\"Verifies if the current spec matches the registered immutable signature.\"\"\"\n        return self._registry.verify_spec(self.experiment_id, current_spec)\n\n    def to_json(self) -&gt; str:\n        \"\"\"Returns registered spec and signature as JSON.\"\"\"\n        return json.dumps({\n            \"experiment_id\": self.experiment_id,\n            \"spec\": self.spec,\n            \"signature\": self.hash_signature\n        }, indent=2)\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.PreregistrationCard.to_json","title":"<code>to_json()</code>","text":"<p>Returns registered spec and signature as JSON.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>def to_json(self) -&gt; str:\n    \"\"\"Returns registered spec and signature as JSON.\"\"\"\n    return json.dumps({\n        \"experiment_id\": self.experiment_id,\n        \"spec\": self.spec,\n        \"signature\": self.hash_signature\n    }, indent=2)\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.PreregistrationCard.verify","title":"<code>verify(current_spec)</code>","text":"<p>Verifies if the current spec matches the registered immutable signature.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>def verify(self, current_spec: Dict[str, Any]) -&gt; bool:\n    \"\"\"Verifies if the current spec matches the registered immutable signature.\"\"\"\n    return self._registry.verify_spec(self.experiment_id, current_spec)\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.design_experiment","title":"<code>design_experiment(metric_type, baseline_value, standard_deviation=None, mde=0.05, mde_type='relative', alpha=0.05, power=0.8, pre_post_correlation=None, daily_traffic=None)</code>","text":"<p>Computes the required sample size for an experiment based on design constraints.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def design_experiment(\n    metric_type: str,\n    baseline_value: float,\n    standard_deviation: Optional[float] = None,\n    mde: float = 0.05,\n    mde_type: str = \"relative\",\n    alpha: float = 0.05,\n    power: float = 0.80,\n    pre_post_correlation: Optional[float] = None,\n    daily_traffic: Optional[int] = None,\n) -&gt; ExperimentDesignResult:\n    \"\"\"Computes the required sample size for an experiment based on design constraints.\"\"\"\n    metric_type = metric_type.lower()\n    mde_type = mde_type.lower()\n\n    if metric_type not in [\"mean\", \"proportion\", \"ratio\"]:\n        raise ValueError(\"metric_type must be one of: 'mean', 'proportion', 'ratio'.\")\n    if mde_type not in [\"relative\", \"absolute\"]:\n        raise ValueError(\"mde_type must be 'relative' or 'absolute'.\")\n\n    if mde_type == \"relative\":\n        mde_absolute = baseline_value * mde\n        mde_relative = mde\n    else:\n        mde_absolute = mde\n        mde_relative = mde / baseline_value if baseline_value != 0 else 0.0\n\n    if metric_type == \"proportion\":\n        if baseline_value &lt;= 0 or baseline_value &gt;= 1:\n            raise ValueError(\"For proportions, baseline_value must be strictly between 0 and 1.\")\n        variance = baseline_value * (1 - baseline_value)\n    else:\n        if standard_deviation is None:\n            raise ValueError(f\"standard_deviation is required for metric type '{metric_type}'.\")\n        variance = standard_deviation**2\n\n    z_alpha = stats.norm.ppf(1 - alpha / 2)\n    z_beta = stats.norm.ppf(power)\n\n    factor = 2 * (z_alpha + z_beta) ** 2\n    sample_size = factor * variance / (mde_absolute**2)\n\n    details = {\n        \"metric_type\": metric_type,\n        \"baseline_value\": baseline_value,\n        \"standard_deviation\": standard_deviation if metric_type != \"proportion\" else np.sqrt(variance),\n        \"mde_absolute\": mde_absolute,\n        \"mde_relative\": mde_relative,\n        \"alpha\": alpha,\n        \"power\": power,\n        \"sample_size_per_variant\": sample_size,\n        \"total_sample_size\": sample_size * 2,\n    }\n\n    if pre_post_correlation is not None:\n        if not (-1.0 &lt;= pre_post_correlation &lt;= 1.0):\n            raise ValueError(\"pre_post_correlation must be between -1.0 and 1.0.\")\n\n        vr_factor = 1.0 - (pre_post_correlation**2)\n        cuped_sample_size = sample_size * vr_factor\n\n        details[\"pre_post_correlation\"] = pre_post_correlation\n        details[\"cuped_sample_size_per_variant\"] = cuped_sample_size\n        details[\"cuped_savings\"] = 1.0 - vr_factor\n\n    if daily_traffic is not None:\n        if daily_traffic &lt;= 0:\n            raise ValueError(\"daily_traffic must be positive.\")\n        details[\"daily_traffic\"] = daily_traffic\n        details[\"duration_days_standard\"] = (sample_size * 2) / daily_traffic\n        if pre_post_correlation is not None:\n            details[\"duration_days_cuped\"] = (cuped_sample_size * 2) / daily_traffic\n\n    return ExperimentDesignResult(details)\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.estimate_duration_days","title":"<code>estimate_duration_days(required_sample_size, daily_traffic)</code>","text":"<p>Estimates the experiment duration in days based on required size and traffic.</p> <p>Parameters:</p> Name Type Description Default <code>required_sample_size</code> <code>int</code> <p>Total samples needed.</p> required <code>daily_traffic</code> <code>int</code> <p>Daily active users/units.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Estimated duration in days.</p> Source code in <code>src\\xpyrment\\plan\\duration.py</code> <pre><code>def estimate_duration_days(required_sample_size: int, daily_traffic: int) -&gt; float:\n    \"\"\"Estimates the experiment duration in days based on required size and traffic.\n\n    Args:\n        required_sample_size (int): Total samples needed.\n        daily_traffic (int): Daily active users/units.\n\n    Returns:\n        float: Estimated duration in days.\n    \"\"\"\n    if daily_traffic &lt;= 0:\n        raise ValueError(\"daily_traffic must be greater than zero.\")\n    return required_sample_size / daily_traffic\n</code></pre>"},{"location":"api/plan/#xpyrment.plan.generate_power_curve_data","title":"<code>generate_power_curve_data(metric_type, baseline_value, standard_deviation=None, alpha=0.05, power=0.8, mde_range=None, pre_post_correlation=None)</code>","text":"<p>Generates sample size coordinates across a range of MDE values to plot a power curve.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def generate_power_curve_data(\n    metric_type: str,\n    baseline_value: float,\n    standard_deviation: Optional[float] = None,\n    alpha: float = 0.05,\n    power: float = 0.80,\n    mde_range: Optional[np.ndarray] = None,\n    pre_post_correlation: Optional[float] = None,\n) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Generates sample size coordinates across a range of MDE values to plot a power curve.\"\"\"\n    if mde_range is None:\n        mde_range = np.linspace(0.01, 0.15, 50)\n\n    sample_sizes = []\n    cuped_sample_sizes = []\n\n    for mde_val in mde_range:\n        res = design_experiment(\n            metric_type=metric_type,\n            baseline_value=baseline_value,\n            standard_deviation=standard_deviation,\n            mde=mde_val,\n            mde_type=\"relative\",\n            alpha=alpha,\n            power=power,\n            pre_post_correlation=pre_post_correlation,\n        )\n        sample_sizes.append(res.details[\"sample_size_per_variant\"])\n        if pre_post_correlation is not None:\n            cuped_sample_sizes.append(res.details[\"cuped_sample_size_per_variant\"])\n\n    ret = {\n        \"mde_relative\": mde_range,\n        \"sample_size_per_variant\": np.array(sample_sizes),\n    }\n\n    if pre_post_correlation is not None:\n        ret[\"cuped_sample_size_per_variant\"] = np.array(cuped_sample_sizes)\n\n    return ret\n</code></pre>"},{"location":"api/plan/duration/","title":"Duration","text":""},{"location":"api/plan/duration/#xpyrment.plan.duration","title":"<code>xpyrment.plan.duration</code>","text":""},{"location":"api/plan/duration/#xpyrment.plan.duration.estimate_duration_days","title":"<code>estimate_duration_days(required_sample_size, daily_traffic)</code>","text":"<p>Estimates the experiment duration in days based on required size and traffic.</p> <p>Parameters:</p> Name Type Description Default <code>required_sample_size</code> <code>int</code> <p>Total samples needed.</p> required <code>daily_traffic</code> <code>int</code> <p>Daily active users/units.</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>Estimated duration in days.</p> Source code in <code>src\\xpyrment\\plan\\duration.py</code> <pre><code>def estimate_duration_days(required_sample_size: int, daily_traffic: int) -&gt; float:\n    \"\"\"Estimates the experiment duration in days based on required size and traffic.\n\n    Args:\n        required_sample_size (int): Total samples needed.\n        daily_traffic (int): Daily active users/units.\n\n    Returns:\n        float: Estimated duration in days.\n    \"\"\"\n    if daily_traffic &lt;= 0:\n        raise ValueError(\"daily_traffic must be greater than zero.\")\n    return required_sample_size / daily_traffic\n</code></pre>"},{"location":"api/plan/hypothesis/","title":"Hypothesis","text":""},{"location":"api/plan/hypothesis/#xpyrment.plan.hypothesis","title":"<code>xpyrment.plan.hypothesis</code>","text":""},{"location":"api/plan/hypothesis/#xpyrment.plan.hypothesis.HypothesisSpec","title":"<code>HypothesisSpec</code>","text":"<p>Specifies the hypothesis under test and binds it to a primary metric.</p> Source code in <code>src\\xpyrment\\plan\\hypothesis.py</code> <pre><code>class HypothesisSpec:\n    \"\"\"Specifies the hypothesis under test and binds it to a primary metric.\"\"\"\n\n    def __init__(\n        self,\n        primary_metric: BaseMetric,\n        direction: Literal[\"two-sided\", \"greater\", \"less\"] = \"two-sided\",\n        description: str = \"\",\n    ):\n        self.primary_metric = primary_metric\n        self.direction = direction\n        self.description = description\n</code></pre>"},{"location":"api/plan/power/","title":"Power","text":""},{"location":"api/plan/power/#xpyrment.plan.power","title":"<code>xpyrment.plan.power</code>","text":""},{"location":"api/plan/power/#xpyrment.plan.power.ExperimentDesignResult","title":"<code>ExperimentDesignResult</code>","text":"<p>Class to hold and format experiment design and power analysis results.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>class ExperimentDesignResult:\n    \"\"\"Class to hold and format experiment design and power analysis results.\"\"\"\n\n    def __init__(self, details: Dict[str, Any]):\n        self.details = details\n\n    def summary(self) -&gt; Dict[str, list]:\n        \"\"\"Returns a summary of the experiment design parameters.\"\"\"\n        summary = {\n            \"Parameter\": [\n                \"Metric Type\",\n                \"Baseline Value\",\n                \"Target MDE (Absolute)\",\n                \"Target MDE (Relative)\",\n                \"Significance Level (Alpha)\",\n                \"Statistical Power (1-Beta)\",\n                \"Sample Size Per Variant\",\n                \"Total Sample Size Required\",\n            ],\n            \"Value\": [\n                self.details[\"metric_type\"].capitalize(),\n                f\"{self.details['baseline_value']:.4f}\",\n                f\"{self.details['mde_absolute']:.4f}\",\n                f\"{self.details['mde_relative']:.2%}\",\n                f\"{self.details['alpha']:.2%}\",\n                f\"{self.details['power']:.2%}\",\n                f\"{int(np.ceil(self.details['sample_size_per_variant'])):,}\",\n                f\"{int(np.ceil(self.details['total_sample_size'])):,}\",\n            ],\n        }\n\n        if self.details.get(\"pre_post_correlation\"):\n            corr = self.details[\"pre_post_correlation\"]\n            reduced_size = self.details[\"cuped_sample_size_per_variant\"]\n            summary[\"Parameter\"].extend([\n                \"Pre-Post Correlation\",\n                \"CUPED Sample Size Per Variant\",\n                \"CUPED Total Sample Size\",\n                \"CUPED Sample Size Savings\",\n            ])\n            summary[\"Value\"].extend([\n                f\"{corr:.2f}\",\n                f\"{int(np.ceil(reduced_size)):,}\",\n                f\"{int(np.ceil(reduced_size * 2)):,}\",\n                f\"{self.details['cuped_savings']:.1%}\",\n            ])\n\n        if self.details.get(\"daily_traffic\"):\n            summary[\"Parameter\"].extend([\n                \"Daily Traffic\",\n                \"Estimated Duration (Standard)\",\n            ])\n            summary[\"Value\"].extend([\n                f\"{int(self.details['daily_traffic']):,}/day\",\n                f\"{self.details['duration_days_standard']:.1f} days\",\n            ])\n            if self.details.get(\"pre_post_correlation\"):\n                summary[\"Parameter\"].append(\"Estimated Duration (CUPED)\")\n                summary[\"Value\"].append(f\"{self.details['duration_days_cuped']:.1f} days\")\n\n        return summary\n\n    def __repr__(self) -&gt; str:\n        s = \"=========================================\\n\"\n        s += \"       Experiment Design Summary        \\n\"\n        s += \"=========================================\\n\"\n        summary_dict = self.summary()\n        for param, val in zip(summary_dict[\"Parameter\"], summary_dict[\"Value\"]):\n            s += f\"{param:&lt;30}: {val}\\n\"\n        s += \"=========================================\\n\"\n        return s\n</code></pre>"},{"location":"api/plan/power/#xpyrment.plan.power.ExperimentDesignResult.summary","title":"<code>summary()</code>","text":"<p>Returns a summary of the experiment design parameters.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def summary(self) -&gt; Dict[str, list]:\n    \"\"\"Returns a summary of the experiment design parameters.\"\"\"\n    summary = {\n        \"Parameter\": [\n            \"Metric Type\",\n            \"Baseline Value\",\n            \"Target MDE (Absolute)\",\n            \"Target MDE (Relative)\",\n            \"Significance Level (Alpha)\",\n            \"Statistical Power (1-Beta)\",\n            \"Sample Size Per Variant\",\n            \"Total Sample Size Required\",\n        ],\n        \"Value\": [\n            self.details[\"metric_type\"].capitalize(),\n            f\"{self.details['baseline_value']:.4f}\",\n            f\"{self.details['mde_absolute']:.4f}\",\n            f\"{self.details['mde_relative']:.2%}\",\n            f\"{self.details['alpha']:.2%}\",\n            f\"{self.details['power']:.2%}\",\n            f\"{int(np.ceil(self.details['sample_size_per_variant'])):,}\",\n            f\"{int(np.ceil(self.details['total_sample_size'])):,}\",\n        ],\n    }\n\n    if self.details.get(\"pre_post_correlation\"):\n        corr = self.details[\"pre_post_correlation\"]\n        reduced_size = self.details[\"cuped_sample_size_per_variant\"]\n        summary[\"Parameter\"].extend([\n            \"Pre-Post Correlation\",\n            \"CUPED Sample Size Per Variant\",\n            \"CUPED Total Sample Size\",\n            \"CUPED Sample Size Savings\",\n        ])\n        summary[\"Value\"].extend([\n            f\"{corr:.2f}\",\n            f\"{int(np.ceil(reduced_size)):,}\",\n            f\"{int(np.ceil(reduced_size * 2)):,}\",\n            f\"{self.details['cuped_savings']:.1%}\",\n        ])\n\n    if self.details.get(\"daily_traffic\"):\n        summary[\"Parameter\"].extend([\n            \"Daily Traffic\",\n            \"Estimated Duration (Standard)\",\n        ])\n        summary[\"Value\"].extend([\n            f\"{int(self.details['daily_traffic']):,}/day\",\n            f\"{self.details['duration_days_standard']:.1f} days\",\n        ])\n        if self.details.get(\"pre_post_correlation\"):\n            summary[\"Parameter\"].append(\"Estimated Duration (CUPED)\")\n            summary[\"Value\"].append(f\"{self.details['duration_days_cuped']:.1f} days\")\n\n    return summary\n</code></pre>"},{"location":"api/plan/power/#xpyrment.plan.power.design_experiment","title":"<code>design_experiment(metric_type, baseline_value, standard_deviation=None, mde=0.05, mde_type='relative', alpha=0.05, power=0.8, pre_post_correlation=None, daily_traffic=None)</code>","text":"<p>Computes the required sample size for an experiment based on design constraints.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def design_experiment(\n    metric_type: str,\n    baseline_value: float,\n    standard_deviation: Optional[float] = None,\n    mde: float = 0.05,\n    mde_type: str = \"relative\",\n    alpha: float = 0.05,\n    power: float = 0.80,\n    pre_post_correlation: Optional[float] = None,\n    daily_traffic: Optional[int] = None,\n) -&gt; ExperimentDesignResult:\n    \"\"\"Computes the required sample size for an experiment based on design constraints.\"\"\"\n    metric_type = metric_type.lower()\n    mde_type = mde_type.lower()\n\n    if metric_type not in [\"mean\", \"proportion\", \"ratio\"]:\n        raise ValueError(\"metric_type must be one of: 'mean', 'proportion', 'ratio'.\")\n    if mde_type not in [\"relative\", \"absolute\"]:\n        raise ValueError(\"mde_type must be 'relative' or 'absolute'.\")\n\n    if mde_type == \"relative\":\n        mde_absolute = baseline_value * mde\n        mde_relative = mde\n    else:\n        mde_absolute = mde\n        mde_relative = mde / baseline_value if baseline_value != 0 else 0.0\n\n    if metric_type == \"proportion\":\n        if baseline_value &lt;= 0 or baseline_value &gt;= 1:\n            raise ValueError(\"For proportions, baseline_value must be strictly between 0 and 1.\")\n        variance = baseline_value * (1 - baseline_value)\n    else:\n        if standard_deviation is None:\n            raise ValueError(f\"standard_deviation is required for metric type '{metric_type}'.\")\n        variance = standard_deviation**2\n\n    z_alpha = stats.norm.ppf(1 - alpha / 2)\n    z_beta = stats.norm.ppf(power)\n\n    factor = 2 * (z_alpha + z_beta) ** 2\n    sample_size = factor * variance / (mde_absolute**2)\n\n    details = {\n        \"metric_type\": metric_type,\n        \"baseline_value\": baseline_value,\n        \"standard_deviation\": standard_deviation if metric_type != \"proportion\" else np.sqrt(variance),\n        \"mde_absolute\": mde_absolute,\n        \"mde_relative\": mde_relative,\n        \"alpha\": alpha,\n        \"power\": power,\n        \"sample_size_per_variant\": sample_size,\n        \"total_sample_size\": sample_size * 2,\n    }\n\n    if pre_post_correlation is not None:\n        if not (-1.0 &lt;= pre_post_correlation &lt;= 1.0):\n            raise ValueError(\"pre_post_correlation must be between -1.0 and 1.0.\")\n\n        vr_factor = 1.0 - (pre_post_correlation**2)\n        cuped_sample_size = sample_size * vr_factor\n\n        details[\"pre_post_correlation\"] = pre_post_correlation\n        details[\"cuped_sample_size_per_variant\"] = cuped_sample_size\n        details[\"cuped_savings\"] = 1.0 - vr_factor\n\n    if daily_traffic is not None:\n        if daily_traffic &lt;= 0:\n            raise ValueError(\"daily_traffic must be positive.\")\n        details[\"daily_traffic\"] = daily_traffic\n        details[\"duration_days_standard\"] = (sample_size * 2) / daily_traffic\n        if pre_post_correlation is not None:\n            details[\"duration_days_cuped\"] = (cuped_sample_size * 2) / daily_traffic\n\n    return ExperimentDesignResult(details)\n</code></pre>"},{"location":"api/plan/power/#xpyrment.plan.power.generate_power_curve_data","title":"<code>generate_power_curve_data(metric_type, baseline_value, standard_deviation=None, alpha=0.05, power=0.8, mde_range=None, pre_post_correlation=None)</code>","text":"<p>Generates sample size coordinates across a range of MDE values to plot a power curve.</p> Source code in <code>src\\xpyrment\\plan\\power.py</code> <pre><code>def generate_power_curve_data(\n    metric_type: str,\n    baseline_value: float,\n    standard_deviation: Optional[float] = None,\n    alpha: float = 0.05,\n    power: float = 0.80,\n    mde_range: Optional[np.ndarray] = None,\n    pre_post_correlation: Optional[float] = None,\n) -&gt; Dict[str, np.ndarray]:\n    \"\"\"Generates sample size coordinates across a range of MDE values to plot a power curve.\"\"\"\n    if mde_range is None:\n        mde_range = np.linspace(0.01, 0.15, 50)\n\n    sample_sizes = []\n    cuped_sample_sizes = []\n\n    for mde_val in mde_range:\n        res = design_experiment(\n            metric_type=metric_type,\n            baseline_value=baseline_value,\n            standard_deviation=standard_deviation,\n            mde=mde_val,\n            mde_type=\"relative\",\n            alpha=alpha,\n            power=power,\n            pre_post_correlation=pre_post_correlation,\n        )\n        sample_sizes.append(res.details[\"sample_size_per_variant\"])\n        if pre_post_correlation is not None:\n            cuped_sample_sizes.append(res.details[\"cuped_sample_size_per_variant\"])\n\n    ret = {\n        \"mde_relative\": mde_range,\n        \"sample_size_per_variant\": np.array(sample_sizes),\n    }\n\n    if pre_post_correlation is not None:\n        ret[\"cuped_sample_size_per_variant\"] = np.array(cuped_sample_sizes)\n\n    return ret\n</code></pre>"},{"location":"api/plan/preregistration/","title":"Preregistration","text":""},{"location":"api/plan/preregistration/#xpyrment.plan.preregistration","title":"<code>xpyrment.plan.preregistration</code>","text":""},{"location":"api/plan/preregistration/#xpyrment.plan.preregistration.PreregistrationCard","title":"<code>PreregistrationCard</code>","text":"<p>Represents an immutable spec card registered prior to running the experiment.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>class PreregistrationCard:\n    \"\"\"Represents an immutable spec card registered prior to running the experiment.\"\"\"\n\n    def __init__(self, experiment_id: str, spec: Dict[str, Any]):\n        self.experiment_id = experiment_id\n        self.spec = spec\n        self._registry = ExperimentRegistry()\n        self.hash_signature = self._registry.register_spec(experiment_id, spec)\n\n    def verify(self, current_spec: Dict[str, Any]) -&gt; bool:\n        \"\"\"Verifies if the current spec matches the registered immutable signature.\"\"\"\n        return self._registry.verify_spec(self.experiment_id, current_spec)\n\n    def to_json(self) -&gt; str:\n        \"\"\"Returns registered spec and signature as JSON.\"\"\"\n        return json.dumps({\n            \"experiment_id\": self.experiment_id,\n            \"spec\": self.spec,\n            \"signature\": self.hash_signature\n        }, indent=2)\n</code></pre>"},{"location":"api/plan/preregistration/#xpyrment.plan.preregistration.PreregistrationCard.to_json","title":"<code>to_json()</code>","text":"<p>Returns registered spec and signature as JSON.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>def to_json(self) -&gt; str:\n    \"\"\"Returns registered spec and signature as JSON.\"\"\"\n    return json.dumps({\n        \"experiment_id\": self.experiment_id,\n        \"spec\": self.spec,\n        \"signature\": self.hash_signature\n    }, indent=2)\n</code></pre>"},{"location":"api/plan/preregistration/#xpyrment.plan.preregistration.PreregistrationCard.verify","title":"<code>verify(current_spec)</code>","text":"<p>Verifies if the current spec matches the registered immutable signature.</p> Source code in <code>src\\xpyrment\\plan\\preregistration.py</code> <pre><code>def verify(self, current_spec: Dict[str, Any]) -&gt; bool:\n    \"\"\"Verifies if the current spec matches the registered immutable signature.\"\"\"\n    return self._registry.verify_spec(self.experiment_id, current_spec)\n</code></pre>"},{"location":"api/report/","title":"Report Module","text":"<p>The <code>xpyrment.report</code> module contains submodules and components for report.</p>"},{"location":"api/report/#xpyrment.report","title":"<code>xpyrment.report</code>","text":""},{"location":"api/report/#xpyrment.report.AuditTrail","title":"<code>AuditTrail</code>","text":"<p>Maintains an immutable, compliance-ready audit trail of experiment phase transition events.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>class AuditTrail:\n    \"\"\"Maintains an immutable, compliance-ready audit trail of experiment phase transition events.\"\"\"\n\n    def __init__(self, experiment_id: str):\n        self.experiment_id = experiment_id\n        self.logs: List[Dict[str, str]] = []\n\n    def log_event(self, action: str, details: str):\n        \"\"\"Appends a new event with an active timestamp to the audit trail log.\"\"\"\n        self.logs.append({\n            \"timestamp\": datetime.datetime.utcnow().isoformat(),\n            \"action\": action,\n            \"details\": details\n        })\n\n    def get_logs(self) -&gt; List[Dict[str, str]]:\n        \"\"\"Returns log lists.\"\"\"\n        return self.logs\n</code></pre>"},{"location":"api/report/#xpyrment.report.AuditTrail.get_logs","title":"<code>get_logs()</code>","text":"<p>Returns log lists.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>def get_logs(self) -&gt; List[Dict[str, str]]:\n    \"\"\"Returns log lists.\"\"\"\n    return self.logs\n</code></pre>"},{"location":"api/report/#xpyrment.report.AuditTrail.log_event","title":"<code>log_event(action, details)</code>","text":"<p>Appends a new event with an active timestamp to the audit trail log.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>def log_event(self, action: str, details: str):\n    \"\"\"Appends a new event with an active timestamp to the audit trail log.\"\"\"\n    self.logs.append({\n        \"timestamp\": datetime.datetime.utcnow().isoformat(),\n        \"action\": action,\n        \"details\": details\n    })\n</code></pre>"},{"location":"api/report/#xpyrment.report.ExperimentCard","title":"<code>ExperimentCard</code>","text":"<p>Consumes metadata, planning state, and calculations to compile a unified report card.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>class ExperimentCard:\n    \"\"\"Consumes metadata, planning state, and calculations to compile a unified report card.\"\"\"\n\n    def __init__(self, experiment_id: str, plan_spec: dict, validation_spec: dict, analysis_summary: dict):\n        self.experiment_id = experiment_id\n        self.plan_spec = plan_spec\n        self.validation_spec = validation_spec\n        self.analysis_summary = analysis_summary\n\n    def to_dict(self) -&gt; dict:\n        \"\"\"Serializes the experiment card metadata.\"\"\"\n        return {\n            \"experiment_id\": self.experiment_id,\n            \"plan_spec\": self.plan_spec,\n            \"validation_spec\": self.validation_spec,\n            \"analysis_summary\": self.analysis_summary\n        }\n\n    def to_json(self) -&gt; str:\n        \"\"\"Dumps the card as a formatted JSON document.\"\"\"\n        return json.dumps(self.to_dict(), indent=2)\n</code></pre>"},{"location":"api/report/#xpyrment.report.ExperimentCard.to_dict","title":"<code>to_dict()</code>","text":"<p>Serializes the experiment card metadata.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>def to_dict(self) -&gt; dict:\n    \"\"\"Serializes the experiment card metadata.\"\"\"\n    return {\n        \"experiment_id\": self.experiment_id,\n        \"plan_spec\": self.plan_spec,\n        \"validation_spec\": self.validation_spec,\n        \"analysis_summary\": self.analysis_summary\n    }\n</code></pre>"},{"location":"api/report/#xpyrment.report.ExperimentCard.to_json","title":"<code>to_json()</code>","text":"<p>Dumps the card as a formatted JSON document.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>def to_json(self) -&gt; str:\n    \"\"\"Dumps the card as a formatted JSON document.\"\"\"\n    return json.dumps(self.to_dict(), indent=2)\n</code></pre>"},{"location":"api/report/#xpyrment.report.plot_forest","title":"<code>plot_forest(df_raw, alpha=0.05, title='A/B Test Results - Relative Lift &amp; 95% CIs', figsize=(10, 5))</code>","text":"<p>Generates a horizontal forest plot visualizing relative lift and confidence intervals.</p> Source code in <code>src\\xpyrment\\report\\export.py</code> <pre><code>def plot_forest(\n    df_raw: pd.DataFrame,\n    alpha: float = 0.05,\n    title: str = \"A/B Test Results - Relative Lift &amp; 95% CIs\",\n    figsize: tuple = (10, 5),\n) -&gt; tuple:\n    \"\"\"Generates a horizontal forest plot visualizing relative lift and confidence intervals.\"\"\"\n    df = df_raw.copy().sort_values(by=\"metric_name\")\n\n    sns.set_theme(style=\"whitegrid\")\n    fig, ax = plt.subplots(figsize=figsize)\n\n    ax.axvline(0, color=\"#d32f2f\", linestyle=\"--\", linewidth=1.5, label=\"No Effect\")\n\n    y_positions = np.arange(len(df))\n\n    sig_color = \"#009688\"\n    nonsig_color = \"#78909c\"\n\n    for idx, (_, row) in enumerate(df.iterrows()):\n        lift = row[\"relative_lift\"]\n        ci_lower = row[\"rel_ci_lower\"]\n        ci_upper = row[\"rel_ci_upper\"]\n        p_val = row[\"p_value\"]\n\n        is_significant = p_val &lt; alpha\n        color = sig_color if is_significant else nonsig_color\n\n        ax.plot([ci_lower, ci_upper], [idx, idx], color=color, linewidth=2.5, zorder=2)\n\n        ax.scatter(\n            lift,\n            idx,\n            color=color,\n            s=120,\n            edgecolors=\"black\",\n            linewidths=1.2,\n            zorder=3,\n        )\n\n        text_label = f\" {lift:+.2%} (p={p_val:.4f})\"\n        ax.text(\n            max(ci_upper, 0) + 0.005,\n            idx,\n            text_label,\n            va=\"center\",\n            ha=\"left\",\n            fontsize=10,\n            fontweight=\"bold\" if is_significant else \"normal\",\n            color=color,\n        )\n\n    ax.set_yticks(y_positions)\n    ax.set_yticklabels(df[\"metric_name\"], fontsize=12, fontweight=\"bold\")\n    ax.set_xlabel(\"Relative Lift (%)\", fontsize=12, fontweight=\"bold\")\n    ax.set_title(title, fontsize=14, fontweight=\"bold\", pad=20)\n\n    import matplotlib.ticker as mtick\n    ax.xaxis.set_major_formatter(mtick.PercentFormatter(1.0))\n\n    sns.despine(left=True, bottom=True)\n    ax.grid(True, axis=\"x\", linestyle=\":\", alpha=0.6)\n    ax.grid(False, axis=\"y\")\n\n    x_min, x_max = ax.get_xlim()\n    ax.set_xlim(x_min - 0.01, x_max + 0.03)\n\n    plt.tight_layout()\n    return fig, ax\n</code></pre>"},{"location":"api/report/#xpyrment.report.plot_power_curve","title":"<code>plot_power_curve(power_curve_data, title='A/B Test Design - Required Sample Size vs. MDE', figsize=(10, 6))</code>","text":"<p>Plots required sample size per variant across a range of Minimum Detectable Effects (MDE).</p> Source code in <code>src\\xpyrment\\report\\export.py</code> <pre><code>def plot_power_curve(\n    power_curve_data: Dict[str, np.ndarray],\n    title: str = \"A/B Test Design - Required Sample Size vs. MDE\",\n    figsize: tuple = (10, 6),\n) -&gt; tuple:\n    \"\"\"Plots required sample size per variant across a range of Minimum Detectable Effects (MDE).\"\"\"\n    sns.set_theme(style=\"darkgrid\")\n    fig, ax = plt.subplots(figsize=figsize)\n\n    mde_pct = power_curve_data[\"mde_relative\"]\n    standard_n = power_curve_data[\"sample_size_per_variant\"]\n\n    ax.plot(\n        mde_pct,\n        standard_n,\n        color=\"#e53935\",\n        linewidth=2.5,\n        marker=\"o\",\n        markersize=5,\n        label=\"Standard A/B Design\",\n    )\n\n    if \"cuped_sample_size_per_variant\" in power_curve_data:\n        cuped_n = power_curve_data[\"cuped_sample_size_per_variant\"]\n        ax.plot(\n            mde_pct,\n            cuped_n,\n            color=\"#1e88e5\",\n            linewidth=2.5,\n            marker=\"s\",\n            markersize=5,\n            label=\"CUPED Design (Variance Reduced)\",\n        )\n\n        ax.fill_between(\n            mde_pct,\n            cuped_n,\n            standard_n,\n            color=\"#bbdefb\",\n            alpha=0.3,\n            label=\"Sample Size Savings via CUPED\",\n        )\n\n    import matplotlib.ticker as mtick\n    ax.xaxis.set_major_formatter(mtick.PercentFormatter(1.0))\n    ax.get_yaxis().set_major_formatter(mtick.FuncFormatter(lambda x, p: f\"{int(x):,}\"))\n\n    ax.set_xlabel(\"Relative Minimum Detectable Effect (MDE)\", fontsize=12, fontweight=\"bold\")\n    ax.set_ylabel(\"Required Sample Size (Per Variant)\", fontsize=12, fontweight=\"bold\")\n    ax.set_title(title, fontsize=14, fontweight=\"bold\", pad=20)\n\n    ax.legend(fontsize=11, frameon=True, facecolor=\"white\")\n    sns.despine()\n\n    plt.tight_layout()\n    return fig, ax\n</code></pre>"},{"location":"api/report/audit/","title":"Audit","text":""},{"location":"api/report/audit/#xpyrment.report.audit","title":"<code>xpyrment.report.audit</code>","text":""},{"location":"api/report/audit/#xpyrment.report.audit.AuditTrail","title":"<code>AuditTrail</code>","text":"<p>Maintains an immutable, compliance-ready audit trail of experiment phase transition events.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>class AuditTrail:\n    \"\"\"Maintains an immutable, compliance-ready audit trail of experiment phase transition events.\"\"\"\n\n    def __init__(self, experiment_id: str):\n        self.experiment_id = experiment_id\n        self.logs: List[Dict[str, str]] = []\n\n    def log_event(self, action: str, details: str):\n        \"\"\"Appends a new event with an active timestamp to the audit trail log.\"\"\"\n        self.logs.append({\n            \"timestamp\": datetime.datetime.utcnow().isoformat(),\n            \"action\": action,\n            \"details\": details\n        })\n\n    def get_logs(self) -&gt; List[Dict[str, str]]:\n        \"\"\"Returns log lists.\"\"\"\n        return self.logs\n</code></pre>"},{"location":"api/report/audit/#xpyrment.report.audit.AuditTrail.get_logs","title":"<code>get_logs()</code>","text":"<p>Returns log lists.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>def get_logs(self) -&gt; List[Dict[str, str]]:\n    \"\"\"Returns log lists.\"\"\"\n    return self.logs\n</code></pre>"},{"location":"api/report/audit/#xpyrment.report.audit.AuditTrail.log_event","title":"<code>log_event(action, details)</code>","text":"<p>Appends a new event with an active timestamp to the audit trail log.</p> Source code in <code>src\\xpyrment\\report\\audit.py</code> <pre><code>def log_event(self, action: str, details: str):\n    \"\"\"Appends a new event with an active timestamp to the audit trail log.\"\"\"\n    self.logs.append({\n        \"timestamp\": datetime.datetime.utcnow().isoformat(),\n        \"action\": action,\n        \"details\": details\n    })\n</code></pre>"},{"location":"api/report/card/","title":"Card","text":""},{"location":"api/report/card/#xpyrment.report.card","title":"<code>xpyrment.report.card</code>","text":""},{"location":"api/report/card/#xpyrment.report.card.ExperimentCard","title":"<code>ExperimentCard</code>","text":"<p>Consumes metadata, planning state, and calculations to compile a unified report card.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>class ExperimentCard:\n    \"\"\"Consumes metadata, planning state, and calculations to compile a unified report card.\"\"\"\n\n    def __init__(self, experiment_id: str, plan_spec: dict, validation_spec: dict, analysis_summary: dict):\n        self.experiment_id = experiment_id\n        self.plan_spec = plan_spec\n        self.validation_spec = validation_spec\n        self.analysis_summary = analysis_summary\n\n    def to_dict(self) -&gt; dict:\n        \"\"\"Serializes the experiment card metadata.\"\"\"\n        return {\n            \"experiment_id\": self.experiment_id,\n            \"plan_spec\": self.plan_spec,\n            \"validation_spec\": self.validation_spec,\n            \"analysis_summary\": self.analysis_summary\n        }\n\n    def to_json(self) -&gt; str:\n        \"\"\"Dumps the card as a formatted JSON document.\"\"\"\n        return json.dumps(self.to_dict(), indent=2)\n</code></pre>"},{"location":"api/report/card/#xpyrment.report.card.ExperimentCard.to_dict","title":"<code>to_dict()</code>","text":"<p>Serializes the experiment card metadata.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>def to_dict(self) -&gt; dict:\n    \"\"\"Serializes the experiment card metadata.\"\"\"\n    return {\n        \"experiment_id\": self.experiment_id,\n        \"plan_spec\": self.plan_spec,\n        \"validation_spec\": self.validation_spec,\n        \"analysis_summary\": self.analysis_summary\n    }\n</code></pre>"},{"location":"api/report/card/#xpyrment.report.card.ExperimentCard.to_json","title":"<code>to_json()</code>","text":"<p>Dumps the card as a formatted JSON document.</p> Source code in <code>src\\xpyrment\\report\\card.py</code> <pre><code>def to_json(self) -&gt; str:\n    \"\"\"Dumps the card as a formatted JSON document.\"\"\"\n    return json.dumps(self.to_dict(), indent=2)\n</code></pre>"},{"location":"api/report/export/","title":"Export","text":""},{"location":"api/report/export/#xpyrment.report.export","title":"<code>xpyrment.report.export</code>","text":""},{"location":"api/report/export/#xpyrment.report.export.plot_forest","title":"<code>plot_forest(df_raw, alpha=0.05, title='A/B Test Results - Relative Lift &amp; 95% CIs', figsize=(10, 5))</code>","text":"<p>Generates a horizontal forest plot visualizing relative lift and confidence intervals.</p> Source code in <code>src\\xpyrment\\report\\export.py</code> <pre><code>def plot_forest(\n    df_raw: pd.DataFrame,\n    alpha: float = 0.05,\n    title: str = \"A/B Test Results - Relative Lift &amp; 95% CIs\",\n    figsize: tuple = (10, 5),\n) -&gt; tuple:\n    \"\"\"Generates a horizontal forest plot visualizing relative lift and confidence intervals.\"\"\"\n    df = df_raw.copy().sort_values(by=\"metric_name\")\n\n    sns.set_theme(style=\"whitegrid\")\n    fig, ax = plt.subplots(figsize=figsize)\n\n    ax.axvline(0, color=\"#d32f2f\", linestyle=\"--\", linewidth=1.5, label=\"No Effect\")\n\n    y_positions = np.arange(len(df))\n\n    sig_color = \"#009688\"\n    nonsig_color = \"#78909c\"\n\n    for idx, (_, row) in enumerate(df.iterrows()):\n        lift = row[\"relative_lift\"]\n        ci_lower = row[\"rel_ci_lower\"]\n        ci_upper = row[\"rel_ci_upper\"]\n        p_val = row[\"p_value\"]\n\n        is_significant = p_val &lt; alpha\n        color = sig_color if is_significant else nonsig_color\n\n        ax.plot([ci_lower, ci_upper], [idx, idx], color=color, linewidth=2.5, zorder=2)\n\n        ax.scatter(\n            lift,\n            idx,\n            color=color,\n            s=120,\n            edgecolors=\"black\",\n            linewidths=1.2,\n            zorder=3,\n        )\n\n        text_label = f\" {lift:+.2%} (p={p_val:.4f})\"\n        ax.text(\n            max(ci_upper, 0) + 0.005,\n            idx,\n            text_label,\n            va=\"center\",\n            ha=\"left\",\n            fontsize=10,\n            fontweight=\"bold\" if is_significant else \"normal\",\n            color=color,\n        )\n\n    ax.set_yticks(y_positions)\n    ax.set_yticklabels(df[\"metric_name\"], fontsize=12, fontweight=\"bold\")\n    ax.set_xlabel(\"Relative Lift (%)\", fontsize=12, fontweight=\"bold\")\n    ax.set_title(title, fontsize=14, fontweight=\"bold\", pad=20)\n\n    import matplotlib.ticker as mtick\n    ax.xaxis.set_major_formatter(mtick.PercentFormatter(1.0))\n\n    sns.despine(left=True, bottom=True)\n    ax.grid(True, axis=\"x\", linestyle=\":\", alpha=0.6)\n    ax.grid(False, axis=\"y\")\n\n    x_min, x_max = ax.get_xlim()\n    ax.set_xlim(x_min - 0.01, x_max + 0.03)\n\n    plt.tight_layout()\n    return fig, ax\n</code></pre>"},{"location":"api/report/export/#xpyrment.report.export.plot_power_curve","title":"<code>plot_power_curve(power_curve_data, title='A/B Test Design - Required Sample Size vs. MDE', figsize=(10, 6))</code>","text":"<p>Plots required sample size per variant across a range of Minimum Detectable Effects (MDE).</p> Source code in <code>src\\xpyrment\\report\\export.py</code> <pre><code>def plot_power_curve(\n    power_curve_data: Dict[str, np.ndarray],\n    title: str = \"A/B Test Design - Required Sample Size vs. MDE\",\n    figsize: tuple = (10, 6),\n) -&gt; tuple:\n    \"\"\"Plots required sample size per variant across a range of Minimum Detectable Effects (MDE).\"\"\"\n    sns.set_theme(style=\"darkgrid\")\n    fig, ax = plt.subplots(figsize=figsize)\n\n    mde_pct = power_curve_data[\"mde_relative\"]\n    standard_n = power_curve_data[\"sample_size_per_variant\"]\n\n    ax.plot(\n        mde_pct,\n        standard_n,\n        color=\"#e53935\",\n        linewidth=2.5,\n        marker=\"o\",\n        markersize=5,\n        label=\"Standard A/B Design\",\n    )\n\n    if \"cuped_sample_size_per_variant\" in power_curve_data:\n        cuped_n = power_curve_data[\"cuped_sample_size_per_variant\"]\n        ax.plot(\n            mde_pct,\n            cuped_n,\n            color=\"#1e88e5\",\n            linewidth=2.5,\n            marker=\"s\",\n            markersize=5,\n            label=\"CUPED Design (Variance Reduced)\",\n        )\n\n        ax.fill_between(\n            mde_pct,\n            cuped_n,\n            standard_n,\n            color=\"#bbdefb\",\n            alpha=0.3,\n            label=\"Sample Size Savings via CUPED\",\n        )\n\n    import matplotlib.ticker as mtick\n    ax.xaxis.set_major_formatter(mtick.PercentFormatter(1.0))\n    ax.get_yaxis().set_major_formatter(mtick.FuncFormatter(lambda x, p: f\"{int(x):,}\"))\n\n    ax.set_xlabel(\"Relative Minimum Detectable Effect (MDE)\", fontsize=12, fontweight=\"bold\")\n    ax.set_ylabel(\"Required Sample Size (Per Variant)\", fontsize=12, fontweight=\"bold\")\n    ax.set_title(title, fontsize=14, fontweight=\"bold\", pad=20)\n\n    ax.legend(fontsize=11, frameon=True, facecolor=\"white\")\n    sns.despine()\n\n    plt.tight_layout()\n    return fig, ax\n</code></pre>"},{"location":"api/run/","title":"Run Module","text":"<p>The <code>xpyrment.run</code> module contains submodules and components for run.</p>"},{"location":"api/run/#xpyrment.run","title":"<code>xpyrment.run</code>","text":""},{"location":"api/run/#xpyrment.run.AssignmentLogger","title":"<code>AssignmentLogger</code>","text":"<p>Manages tracking live assignment exposures and deduplicating multiple logs.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>class AssignmentLogger:\n    \"\"\"Manages tracking live assignment exposures and deduplicating multiple logs.\"\"\"\n\n    def __init__(self):\n        self._exposures = []\n\n    def log_assignment(self, unit_id: str, variant: str, timestamp: str):\n        \"\"\"Logs an assignment exposure.\"\"\"\n        self._exposures.append({\n            \"unit_id\": unit_id,\n            \"variant\": variant,\n            \"timestamp\": timestamp\n        })\n\n    def get_deduplicated_exposures(self) -&gt; pd.DataFrame:\n        \"\"\"Deduplicates multiple logs of the same unit_id, taking the earliest exposure.\"\"\"\n        df = pd.DataFrame(self._exposures)\n        if df.empty:\n            return df\n        return df.sort_values(\"timestamp\").drop_duplicates(subset=[\"unit_id\"], keep=\"first\")\n</code></pre>"},{"location":"api/run/#xpyrment.run.AssignmentLogger.get_deduplicated_exposures","title":"<code>get_deduplicated_exposures()</code>","text":"<p>Deduplicates multiple logs of the same unit_id, taking the earliest exposure.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>def get_deduplicated_exposures(self) -&gt; pd.DataFrame:\n    \"\"\"Deduplicates multiple logs of the same unit_id, taking the earliest exposure.\"\"\"\n    df = pd.DataFrame(self._exposures)\n    if df.empty:\n        return df\n    return df.sort_values(\"timestamp\").drop_duplicates(subset=[\"unit_id\"], keep=\"first\")\n</code></pre>"},{"location":"api/run/#xpyrment.run.AssignmentLogger.log_assignment","title":"<code>log_assignment(unit_id, variant, timestamp)</code>","text":"<p>Logs an assignment exposure.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>def log_assignment(self, unit_id: str, variant: str, timestamp: str):\n    \"\"\"Logs an assignment exposure.\"\"\"\n    self._exposures.append({\n        \"unit_id\": unit_id,\n        \"variant\": variant,\n        \"timestamp\": timestamp\n    })\n</code></pre>"},{"location":"api/run/#xpyrment.run.LiveMonitor","title":"<code>LiveMonitor</code>","text":"<p>Provides active monitoring of experimental groups to build diagnostics dashboards.</p> Source code in <code>src\\xpyrment\\run\\monitor.py</code> <pre><code>class LiveMonitor:\n    \"\"\"Provides active monitoring of experimental groups to build diagnostics dashboards.\"\"\"\n\n    def __init__(self, df: pd.DataFrame, time_col: str):\n        self.df = df\n        self.time_col = time_col\n\n    def get_cumulative_traffic(self) -&gt; pd.DataFrame:\n        \"\"\"Calculates cumulative traffic counts over time.\"\"\"\n        # TODO: Implement cumulative plotting helper data\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/run/#xpyrment.run.LiveMonitor.get_cumulative_traffic","title":"<code>get_cumulative_traffic()</code>","text":"<p>Calculates cumulative traffic counts over time.</p> Source code in <code>src\\xpyrment\\run\\monitor.py</code> <pre><code>def get_cumulative_traffic(self) -&gt; pd.DataFrame:\n    \"\"\"Calculates cumulative traffic counts over time.\"\"\"\n    # TODO: Implement cumulative plotting helper data\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/run/#xpyrment.run.StoppingRules","title":"<code>StoppingRules</code>","text":"<p>Implements early-stopping rules like mixture sequential probability ratio tests (mSPRT).</p> Source code in <code>src\\xpyrment\\run\\stopping.py</code> <pre><code>class StoppingRules:\n    \"\"\"Implements early-stopping rules like mixture sequential probability ratio tests (mSPRT).\"\"\"\n\n    def __init__(self, alpha: float = 0.05):\n        self.alpha = alpha\n\n    def check_msprt_stop(self, lambda_value: float) -&gt; bool:\n        \"\"\"Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.\"\"\"\n        # Boundaries typically equal 1 / alpha\n        boundary = 1.0 / self.alpha\n        return lambda_value &gt; boundary\n</code></pre>"},{"location":"api/run/#xpyrment.run.StoppingRules.check_msprt_stop","title":"<code>check_msprt_stop(lambda_value)</code>","text":"<p>Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.</p> Source code in <code>src\\xpyrment\\run\\stopping.py</code> <pre><code>def check_msprt_stop(self, lambda_value: float) -&gt; bool:\n    \"\"\"Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.\"\"\"\n    # Boundaries typically equal 1 / alpha\n    boundary = 1.0 / self.alpha\n    return lambda_value &gt; boundary\n</code></pre>"},{"location":"api/run/#xpyrment.run.ingest_dataframe","title":"<code>ingest_dataframe(df)</code>","text":"<p>Ingests a pandas DataFrame into the xpyrment lifecycle.</p> Source code in <code>src\\xpyrment\\run\\ingestion.py</code> <pre><code>def ingest_dataframe(df: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Ingests a pandas DataFrame into the xpyrment lifecycle.\"\"\"\n    return df.copy()\n</code></pre>"},{"location":"api/run/#xpyrment.run.load_from_sql","title":"<code>load_from_sql(query, connection_string)</code>","text":"<p>Simulates loading data from an external SQL database.</p> Source code in <code>src\\xpyrment\\run\\ingestion.py</code> <pre><code>def load_from_sql(query: str, connection_string: str) -&gt; pd.DataFrame:\n    \"\"\"Simulates loading data from an external SQL database.\"\"\"\n    # TODO: Implement SQL adapter\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/run/assignment/","title":"Assignment","text":""},{"location":"api/run/assignment/#xpyrment.run.assignment","title":"<code>xpyrment.run.assignment</code>","text":""},{"location":"api/run/assignment/#xpyrment.run.assignment.AssignmentLogger","title":"<code>AssignmentLogger</code>","text":"<p>Manages tracking live assignment exposures and deduplicating multiple logs.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>class AssignmentLogger:\n    \"\"\"Manages tracking live assignment exposures and deduplicating multiple logs.\"\"\"\n\n    def __init__(self):\n        self._exposures = []\n\n    def log_assignment(self, unit_id: str, variant: str, timestamp: str):\n        \"\"\"Logs an assignment exposure.\"\"\"\n        self._exposures.append({\n            \"unit_id\": unit_id,\n            \"variant\": variant,\n            \"timestamp\": timestamp\n        })\n\n    def get_deduplicated_exposures(self) -&gt; pd.DataFrame:\n        \"\"\"Deduplicates multiple logs of the same unit_id, taking the earliest exposure.\"\"\"\n        df = pd.DataFrame(self._exposures)\n        if df.empty:\n            return df\n        return df.sort_values(\"timestamp\").drop_duplicates(subset=[\"unit_id\"], keep=\"first\")\n</code></pre>"},{"location":"api/run/assignment/#xpyrment.run.assignment.AssignmentLogger.get_deduplicated_exposures","title":"<code>get_deduplicated_exposures()</code>","text":"<p>Deduplicates multiple logs of the same unit_id, taking the earliest exposure.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>def get_deduplicated_exposures(self) -&gt; pd.DataFrame:\n    \"\"\"Deduplicates multiple logs of the same unit_id, taking the earliest exposure.\"\"\"\n    df = pd.DataFrame(self._exposures)\n    if df.empty:\n        return df\n    return df.sort_values(\"timestamp\").drop_duplicates(subset=[\"unit_id\"], keep=\"first\")\n</code></pre>"},{"location":"api/run/assignment/#xpyrment.run.assignment.AssignmentLogger.log_assignment","title":"<code>log_assignment(unit_id, variant, timestamp)</code>","text":"<p>Logs an assignment exposure.</p> Source code in <code>src\\xpyrment\\run\\assignment.py</code> <pre><code>def log_assignment(self, unit_id: str, variant: str, timestamp: str):\n    \"\"\"Logs an assignment exposure.\"\"\"\n    self._exposures.append({\n        \"unit_id\": unit_id,\n        \"variant\": variant,\n        \"timestamp\": timestamp\n    })\n</code></pre>"},{"location":"api/run/ingestion/","title":"Ingestion","text":""},{"location":"api/run/ingestion/#xpyrment.run.ingestion","title":"<code>xpyrment.run.ingestion</code>","text":""},{"location":"api/run/ingestion/#xpyrment.run.ingestion.ingest_dataframe","title":"<code>ingest_dataframe(df)</code>","text":"<p>Ingests a pandas DataFrame into the xpyrment lifecycle.</p> Source code in <code>src\\xpyrment\\run\\ingestion.py</code> <pre><code>def ingest_dataframe(df: pd.DataFrame) -&gt; pd.DataFrame:\n    \"\"\"Ingests a pandas DataFrame into the xpyrment lifecycle.\"\"\"\n    return df.copy()\n</code></pre>"},{"location":"api/run/ingestion/#xpyrment.run.ingestion.load_from_sql","title":"<code>load_from_sql(query, connection_string)</code>","text":"<p>Simulates loading data from an external SQL database.</p> Source code in <code>src\\xpyrment\\run\\ingestion.py</code> <pre><code>def load_from_sql(query: str, connection_string: str) -&gt; pd.DataFrame:\n    \"\"\"Simulates loading data from an external SQL database.\"\"\"\n    # TODO: Implement SQL adapter\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/run/monitor/","title":"Monitor","text":""},{"location":"api/run/monitor/#xpyrment.run.monitor","title":"<code>xpyrment.run.monitor</code>","text":""},{"location":"api/run/monitor/#xpyrment.run.monitor.LiveMonitor","title":"<code>LiveMonitor</code>","text":"<p>Provides active monitoring of experimental groups to build diagnostics dashboards.</p> Source code in <code>src\\xpyrment\\run\\monitor.py</code> <pre><code>class LiveMonitor:\n    \"\"\"Provides active monitoring of experimental groups to build diagnostics dashboards.\"\"\"\n\n    def __init__(self, df: pd.DataFrame, time_col: str):\n        self.df = df\n        self.time_col = time_col\n\n    def get_cumulative_traffic(self) -&gt; pd.DataFrame:\n        \"\"\"Calculates cumulative traffic counts over time.\"\"\"\n        # TODO: Implement cumulative plotting helper data\n        return pd.DataFrame()\n</code></pre>"},{"location":"api/run/monitor/#xpyrment.run.monitor.LiveMonitor.get_cumulative_traffic","title":"<code>get_cumulative_traffic()</code>","text":"<p>Calculates cumulative traffic counts over time.</p> Source code in <code>src\\xpyrment\\run\\monitor.py</code> <pre><code>def get_cumulative_traffic(self) -&gt; pd.DataFrame:\n    \"\"\"Calculates cumulative traffic counts over time.\"\"\"\n    # TODO: Implement cumulative plotting helper data\n    return pd.DataFrame()\n</code></pre>"},{"location":"api/run/stopping/","title":"Stopping","text":""},{"location":"api/run/stopping/#xpyrment.run.stopping","title":"<code>xpyrment.run.stopping</code>","text":""},{"location":"api/run/stopping/#xpyrment.run.stopping.StoppingRules","title":"<code>StoppingRules</code>","text":"<p>Implements early-stopping rules like mixture sequential probability ratio tests (mSPRT).</p> Source code in <code>src\\xpyrment\\run\\stopping.py</code> <pre><code>class StoppingRules:\n    \"\"\"Implements early-stopping rules like mixture sequential probability ratio tests (mSPRT).\"\"\"\n\n    def __init__(self, alpha: float = 0.05):\n        self.alpha = alpha\n\n    def check_msprt_stop(self, lambda_value: float) -&gt; bool:\n        \"\"\"Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.\"\"\"\n        # Boundaries typically equal 1 / alpha\n        boundary = 1.0 / self.alpha\n        return lambda_value &gt; boundary\n</code></pre>"},{"location":"api/run/stopping/#xpyrment.run.stopping.StoppingRules.check_msprt_stop","title":"<code>check_msprt_stop(lambda_value)</code>","text":"<p>Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.</p> Source code in <code>src\\xpyrment\\run\\stopping.py</code> <pre><code>def check_msprt_stop(self, lambda_value: float) -&gt; bool:\n    \"\"\"Determines if the mSPRT likelihood ratio exceeds the safety stopping bounds.\"\"\"\n    # Boundaries typically equal 1 / alpha\n    boundary = 1.0 / self.alpha\n    return lambda_value &gt; boundary\n</code></pre>"},{"location":"api/validate/","title":"Validate Module","text":"<p>The <code>xpyrment.validate</code> module contains submodules and components for validate.</p>"},{"location":"api/validate/#xpyrment.validate","title":"<code>xpyrment.validate</code>","text":""},{"location":"api/validate/#xpyrment.validate.check_covariate_balance","title":"<code>check_covariate_balance(df, treatment_col, covariate_cols)</code>","text":"<p>Computes Normalized Differences and t-tests to evaluate balance of pre-period covariates.</p> Source code in <code>src\\xpyrment\\validate\\balance.py</code> <pre><code>def check_covariate_balance(df: pd.DataFrame, treatment_col: str, covariate_cols: list) -&gt; dict:\n    \"\"\"Computes Normalized Differences and t-tests to evaluate balance of pre-period covariates.\"\"\"\n    # TODO: Implement balance checks\n    return {}\n</code></pre>"},{"location":"api/validate/#xpyrment.validate.check_novelty_effects","title":"<code>check_novelty_effects(df, treatment_col, metric_col, time_col)</code>","text":"<p>Detects novelty or primacy effects by tracking treatment effect size evolution over time.</p> Source code in <code>src\\xpyrment\\validate\\novelty.py</code> <pre><code>def check_novelty_effects(df: pd.DataFrame, treatment_col: str, metric_col: str, time_col: str) -&gt; dict:\n    \"\"\"Detects novelty or primacy effects by tracking treatment effect size evolution over time.\"\"\"\n    # TODO: Implement time-series slope/interaction checks\n    return {}\n</code></pre>"},{"location":"api/validate/#xpyrment.validate.check_srm","title":"<code>check_srm(observed_counts, expected_ratios)</code>","text":"<p>Calculates the Chi-square p-value to check for Sample Ratio Mismatch (SRM).</p> <p>Parameters:</p> Name Type Description Default <code>observed_counts</code> <code>List[int]</code> <p>Number of units allocated to each variant (e.g. [5012, 4988]).</p> required <code>expected_ratios</code> <code>List[float]</code> <p>Expected allocation ratios (e.g. [0.5, 0.5]).</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>p-value of the goodness-of-fit test. If p &lt; 0.001, SRM is highly likely.</p> Source code in <code>src\\xpyrment\\validate\\srm.py</code> <pre><code>def check_srm(observed_counts: List[int], expected_ratios: List[float]) -&gt; float:\n    \"\"\"Calculates the Chi-square p-value to check for Sample Ratio Mismatch (SRM).\n\n    Args:\n        observed_counts: Number of units allocated to each variant (e.g. [5012, 4988]).\n        expected_ratios: Expected allocation ratios (e.g. [0.5, 0.5]).\n\n    Returns:\n        float: p-value of the goodness-of-fit test. If p &lt; 0.001, SRM is highly likely.\n    \"\"\"\n    total_observed = sum(observed_counts)\n    sum_ratios = sum(expected_ratios)\n\n    expected_counts = [ratio * total_observed / sum_ratios for ratio in expected_ratios]\n\n    # Perform chi-square goodness-of-fit test\n    _, p_value = stats.chisquare(f_obs=observed_counts, f_exp=expected_counts)\n\n    if p_value &lt; 0.001:\n        raise SRMError(\n            f\"Sample Ratio Mismatch detected (p={p_value:.4e}). \"\n            f\"Observed counts: {observed_counts}, Expected ratios: {expected_ratios}\"\n        )\n\n    return p_value\n</code></pre>"},{"location":"api/validate/#xpyrment.validate.run_aa_test_validation","title":"<code>run_aa_test_validation(df, treatment_col, metric_col)</code>","text":"<p>Runs a mock A/A test validation check, asserting that the baseline splits have no effect.</p> Source code in <code>src\\xpyrment\\validate\\aa_test.py</code> <pre><code>def run_aa_test_validation(df: pd.DataFrame, treatment_col: str, metric_col: str) -&gt; float:\n    \"\"\"Runs a mock A/A test validation check, asserting that the baseline splits have no effect.\"\"\"\n    # TODO: Implement multi-run A/A simulations or a simple t-test under A/A conditions\n    return 1.0\n</code></pre>"},{"location":"api/validate/aa_test/","title":"A/A Test","text":""},{"location":"api/validate/aa_test/#xpyrment.validate.aa_test","title":"<code>xpyrment.validate.aa_test</code>","text":""},{"location":"api/validate/aa_test/#xpyrment.validate.aa_test.run_aa_test_validation","title":"<code>run_aa_test_validation(df, treatment_col, metric_col)</code>","text":"<p>Runs a mock A/A test validation check, asserting that the baseline splits have no effect.</p> Source code in <code>src\\xpyrment\\validate\\aa_test.py</code> <pre><code>def run_aa_test_validation(df: pd.DataFrame, treatment_col: str, metric_col: str) -&gt; float:\n    \"\"\"Runs a mock A/A test validation check, asserting that the baseline splits have no effect.\"\"\"\n    # TODO: Implement multi-run A/A simulations or a simple t-test under A/A conditions\n    return 1.0\n</code></pre>"},{"location":"api/validate/balance/","title":"Balance","text":""},{"location":"api/validate/balance/#xpyrment.validate.balance","title":"<code>xpyrment.validate.balance</code>","text":""},{"location":"api/validate/balance/#xpyrment.validate.balance.check_covariate_balance","title":"<code>check_covariate_balance(df, treatment_col, covariate_cols)</code>","text":"<p>Computes Normalized Differences and t-tests to evaluate balance of pre-period covariates.</p> Source code in <code>src\\xpyrment\\validate\\balance.py</code> <pre><code>def check_covariate_balance(df: pd.DataFrame, treatment_col: str, covariate_cols: list) -&gt; dict:\n    \"\"\"Computes Normalized Differences and t-tests to evaluate balance of pre-period covariates.\"\"\"\n    # TODO: Implement balance checks\n    return {}\n</code></pre>"},{"location":"api/validate/novelty/","title":"Novelty","text":""},{"location":"api/validate/novelty/#xpyrment.validate.novelty","title":"<code>xpyrment.validate.novelty</code>","text":""},{"location":"api/validate/novelty/#xpyrment.validate.novelty.check_novelty_effects","title":"<code>check_novelty_effects(df, treatment_col, metric_col, time_col)</code>","text":"<p>Detects novelty or primacy effects by tracking treatment effect size evolution over time.</p> Source code in <code>src\\xpyrment\\validate\\novelty.py</code> <pre><code>def check_novelty_effects(df: pd.DataFrame, treatment_col: str, metric_col: str, time_col: str) -&gt; dict:\n    \"\"\"Detects novelty or primacy effects by tracking treatment effect size evolution over time.\"\"\"\n    # TODO: Implement time-series slope/interaction checks\n    return {}\n</code></pre>"},{"location":"api/validate/srm/","title":"SRM","text":""},{"location":"api/validate/srm/#xpyrment.validate.srm","title":"<code>xpyrment.validate.srm</code>","text":""},{"location":"api/validate/srm/#xpyrment.validate.srm.check_srm","title":"<code>check_srm(observed_counts, expected_ratios)</code>","text":"<p>Calculates the Chi-square p-value to check for Sample Ratio Mismatch (SRM).</p> <p>Parameters:</p> Name Type Description Default <code>observed_counts</code> <code>List[int]</code> <p>Number of units allocated to each variant (e.g. [5012, 4988]).</p> required <code>expected_ratios</code> <code>List[float]</code> <p>Expected allocation ratios (e.g. [0.5, 0.5]).</p> required <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>p-value of the goodness-of-fit test. If p &lt; 0.001, SRM is highly likely.</p> Source code in <code>src\\xpyrment\\validate\\srm.py</code> <pre><code>def check_srm(observed_counts: List[int], expected_ratios: List[float]) -&gt; float:\n    \"\"\"Calculates the Chi-square p-value to check for Sample Ratio Mismatch (SRM).\n\n    Args:\n        observed_counts: Number of units allocated to each variant (e.g. [5012, 4988]).\n        expected_ratios: Expected allocation ratios (e.g. [0.5, 0.5]).\n\n    Returns:\n        float: p-value of the goodness-of-fit test. If p &lt; 0.001, SRM is highly likely.\n    \"\"\"\n    total_observed = sum(observed_counts)\n    sum_ratios = sum(expected_ratios)\n\n    expected_counts = [ratio * total_observed / sum_ratios for ratio in expected_ratios]\n\n    # Perform chi-square goodness-of-fit test\n    _, p_value = stats.chisquare(f_obs=observed_counts, f_exp=expected_counts)\n\n    if p_value &lt; 0.001:\n        raise SRMError(\n            f\"Sample Ratio Mismatch detected (p={p_value:.4e}). \"\n            f\"Observed counts: {observed_counts}, Expected ratios: {expected_ratios}\"\n        )\n\n    return p_value\n</code></pre>"}]}